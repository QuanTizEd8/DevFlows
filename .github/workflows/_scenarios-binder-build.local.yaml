# GENERATED by devflows test-generate — do not edit; source: workflows/*/devflow.yaml.
# This workflow only works under `act`. Run it with `task scenarios-local`; the
# require-act guard job fails fast if it is dispatched on GitHub-hosted runners.
name: 'DevFlows Local Scenarios: Binder Build'
'on':
  workflow_dispatch: null
permissions:
  contents: read
jobs:
  require_act:
    name: Require act
    runs-on: ubuntu-latest
    steps:
      - name: Fail unless running under act
        shell: bash
        run: |-
          if [ "${ACT:-}" != "true" ]; then
            echo "::error::This local scenario workflow runs only under act. Run 'task scenarios-local' locally; do not dispatch it on GitHub."
            exit 1
          fi
          echo "Running under act."
  binder_build_missing_image_name_validate:
    name: 'Binder Build: Empty image-name fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ''
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: ''
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: image-name is required
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_invalid_image_name_validate:
    name: 'Binder Build: image-name carrying a tag fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo:latest
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: ''
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: WITHOUT a tag
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_empty_image_tags_validate:
    name: 'Binder Build: image-tags resolving to an empty list fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo-binder
          IMAGE_TAGS: ''
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: ''
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: non-empty newline-separated list
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_push_without_environment_validate:
    name: 'Binder Build: A real push with no push-environment-name fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo-binder
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: ''
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'false'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: push-environment-name is required
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_arguments_owned_push_flag_validate:
    name: 'Binder Build: A --push smuggled through repo2docker-arguments fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo-binder
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: --push
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: owned by typed inputs
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_arguments_owned_image_name_validate:
    name: 'Binder Build: An attached --image-name=value fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo-binder
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: --image-name=attacker/evil
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: owned by typed inputs
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_arguments_attached_short_option_validate:
    name: 'Binder Build: An attached short option fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo-binder
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: -eSECRET=1
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: only the allowlisted passthrough flags
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_arguments_bare_positional_validate:
    name: 'Binder Build: A bare positional source fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo-binder
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: ./attacker-repo
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: only the allowlisted passthrough flags
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_arguments_invalid_shlex_validate:
    name: 'Binder Build: An unbalanced quote in repo2docker-arguments fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo-binder
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: --label='unbalanced
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: could not be parsed as shell arguments
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_unsafe_source_path_validate:
    name: 'Binder Build: A repo2docker-source-path escaping the workspace fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo-binder
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: ../outside
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: ''
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: workspace-relative path
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_invalid_repo2docker_version_validate:
    name: 'Binder Build: A repo2docker-version with a smuggled specifier fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo-binder
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0; os.system('x')
          REPO2DOCKER_ARGUMENTS: ''
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: binder-dockerfile
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: date-based jupyter-repo2docker version
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  binder_build_unsafe_dockerfile_artifact_name_validate:
    name: 'Binder Build: An artifact-unsafe dockerfile-artifact-name fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/binder-build/scripts/validate-inputs.py
          IMAGE_NAME: ghcr.io/owner/repo-binder
          IMAGE_TAGS: latest
          IMAGE_SHA_TAG_ENABLED: 'false'
          IMAGE_SHA_TAG_PREFIX: sha-
          REPO2DOCKER_SOURCE_PATH: .
          REPO2DOCKER_VERSION: 2026.4.0
          REPO2DOCKER_ARGUMENTS: ''
          REPO2DOCKER_TIMEOUT_MINUTES: '30'
          PUSH_TIMEOUT_MINUTES: '30'
          PUBLISH_DRY_RUN_ENABLED: 'true'
          PUSH_ENVIRONMENT_NAME: ''
          DOCKERFILE_ARTIFACT_ENABLED: 'true'
          DOCKERFILE_ARTIFACT_NAME: bad/name
          BINDER_CACHE_WARM_ENABLED: 'false'
          BINDER_CACHE_PROVIDER: gh
          BINDER_CACHE_REPOSITORY: ${{ github.repository }}
          BINDER_CACHE_REF: ${{ github.sha }}
          BINDER_CACHE_ENDPOINT: https://mybinder.org
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: artifact-safe token
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act

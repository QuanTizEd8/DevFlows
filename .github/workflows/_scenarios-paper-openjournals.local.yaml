# GENERATED by devflows test-generate — do not edit; source: workflows/*/devflow.yaml.
# This workflow only works under `act`. Run it with `task scenarios-local`; the
# require-act guard job fails fast if it is dispatched on GitHub-hosted runners.
name: 'DevFlows Local Scenarios: Open Journals Paper Build'
'on':
  workflow_dispatch: null
permissions:
  contents: read
jobs:
  require_act:
    name: Require act
    runs-on: ubuntu-latest
    steps:
      - name: Fail unless running under act
        shell: bash
        run: |-
          if [ "${ACT:-}" != "true" ]; then
            echo "::error::This local scenario workflow runs only under act. Run 'task scenarios-local' locally; do not dispatch it on GitHub."
            exit 1
          fi
          echo "Running under act."
  paper_openjournals_invalid_journal_validate:
    name: 'Open Journals Paper Build: Unknown paper-journal fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/paper-openjournals/scripts/validate-inputs.py
          PAPER_JOURNAL: elsevier
          PAPER_SOURCE_PATH: tests/scenarios/paper-openjournals/minimal/paper.md
          PAPER_FLAVORS: |
            draft-pdf
            jats
          PAPER_IMAGE: ghcr.io/openjournals/inara:1.3.1
          PAPER_OUTPUT_DIRECTORY: paper-build
          PAPER_ARGUMENTS: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: must be one of
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  paper_openjournals_missing_source_validate:
    name: 'Open Journals Paper Build: Empty paper-source-path fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/paper-openjournals/scripts/validate-inputs.py
          PAPER_JOURNAL: joss
          PAPER_SOURCE_PATH: ''
          PAPER_FLAVORS: |
            draft-pdf
            jats
          PAPER_IMAGE: ghcr.io/openjournals/inara:1.3.1
          PAPER_OUTPUT_DIRECTORY: paper-build
          PAPER_ARGUMENTS: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: paper-source-path is required
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  paper_openjournals_unsafe_source_validate:
    name: 'Open Journals Paper Build: A paper-source-path that escapes the workspace fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/paper-openjournals/scripts/validate-inputs.py
          PAPER_JOURNAL: joss
          PAPER_SOURCE_PATH: ../../../etc/passwd
          PAPER_FLAVORS: |
            draft-pdf
            jats
          PAPER_IMAGE: ghcr.io/openjournals/inara:1.3.1
          PAPER_OUTPUT_DIRECTORY: paper-build
          PAPER_ARGUMENTS: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: must stay inside
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  paper_openjournals_empty_flavors_validate:
    name: 'Open Journals Paper Build: Empty paper-flavors fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/paper-openjournals/scripts/validate-inputs.py
          PAPER_JOURNAL: joss
          PAPER_SOURCE_PATH: tests/scenarios/paper-openjournals/minimal/paper.md
          PAPER_FLAVORS: ''
          PAPER_IMAGE: ghcr.io/openjournals/inara:1.3.1
          PAPER_OUTPUT_DIRECTORY: paper-build
          PAPER_ARGUMENTS: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: at least one paper flavor
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  paper_openjournals_unknown_flavor_validate:
    name: 'Open Journals Paper Build: An unknown flavor name fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/paper-openjournals/scripts/validate-inputs.py
          PAPER_JOURNAL: joss
          PAPER_SOURCE_PATH: tests/scenarios/paper-openjournals/minimal/paper.md
          PAPER_FLAVORS: |
            draft-pdf
            gif
          PAPER_IMAGE: ghcr.io/openjournals/inara:1.3.1
          PAPER_OUTPUT_DIRECTORY: paper-build
          PAPER_ARGUMENTS: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: 'unknown paper flavor: gif'
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  paper_openjournals_invalid_image_validate:
    name: 'Open Journals Paper Build: A malformed paper-image reference fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/paper-openjournals/scripts/validate-inputs.py
          PAPER_JOURNAL: joss
          PAPER_SOURCE_PATH: tests/scenarios/paper-openjournals/minimal/paper.md
          PAPER_FLAVORS: |
            draft-pdf
            jats
          PAPER_IMAGE: inara:bad:ref
          PAPER_OUTPUT_DIRECTORY: paper-build
          PAPER_ARGUMENTS: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: valid image reference
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  paper_openjournals_owned_flag_argument_validate:
    name: 'Open Journals Paper Build: An output-selection flag in paper-arguments fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/paper-openjournals/scripts/validate-inputs.py
          PAPER_JOURNAL: joss
          PAPER_SOURCE_PATH: tests/scenarios/paper-openjournals/minimal/paper.md
          PAPER_FLAVORS: |
            draft-pdf
            jats
          PAPER_IMAGE: ghcr.io/openjournals/inara:1.3.1
          PAPER_OUTPUT_DIRECTORY: paper-build
          PAPER_ARGUMENTS: -o pdf
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: controlled by paper-flavors
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  paper_openjournals_positional_argument_validate:
    name: 'Open Journals Paper Build: A bare positional in paper-arguments fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/paper-openjournals/scripts/validate-inputs.py
          PAPER_JOURNAL: joss
          PAPER_SOURCE_PATH: tests/scenarios/paper-openjournals/minimal/paper.md
          PAPER_FLAVORS: |
            draft-pdf
            jats
          PAPER_IMAGE: ghcr.io/openjournals/inara:1.3.1
          PAPER_OUTPUT_DIRECTORY: paper-build
          PAPER_ARGUMENTS: paper2.md
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: positional
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act

# GENERATED by devflows test-generate — do not edit; source: workflows/*/devflow.yaml.
# Scenarios whose reusable-workflow call needs a write permission are gated with a
# fork-safe `if:` (and so are the jobs that depend on them): fork pull requests skip
# them because GitHub caps the fork token to read and would fail the nested-permission
# check at startup. Same-repo pull requests, pushes to main, and workflow_dispatch
# still run every scenario.
name: 'DevFlows Hosted Scenarios: Open Journals Paper Build'
'on':
  pull_request: null
  push:
    branches:
      - main
  workflow_dispatch: null
permissions:
  contents: read
  actions: read
jobs:
  paper_openjournals_cff_jats_build_call:
    name: 'Open Journals Paper Build: Build the CFF and JATS flavors from a minimal fixture paper'
    uses: ./.github/workflows/paper-openjournals.yaml
    with:
      paper-journal: joss
      paper-source-path: tests/scenarios/paper-openjournals/minimal/paper.md
      paper-flavors: |
        cff
        jats
      paper-output-directory: paper-build
      artifact-upload-enabled: true
      artifact-upload-name: paper-openjournals-build
      artifact-upload-path: paper-build
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: read
  paper_openjournals_cff_jats_build_assert:
    name: 'Open Journals Paper Build: Build the CFF and JATS flavors from a minimal fixture paper assertions'
    runs-on: ubuntu-latest
    needs: paper_openjournals_cff_jats_build_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.paper_openjournals_cff_jats_build_call.result }}
        run: python .dev/harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: paper-openjournals-build
          path: .devflows-test-artifacts/paper-openjournals/build
      - name: 'Assert file matches glob: cff/CITATION.cff'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/paper-openjournals/build/cff/CITATION.cff
          ASSERT_GLOB: '1'
        run: python .dev/harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: cff/CITATION.cff'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/paper-openjournals/build/cff/CITATION.cff
          ASSERT_TEXT: DevFlows fixture paper
        run: python .dev/harness/scenarios/assert-file-contains.py
      - name: 'Assert file matches glob: jats/jats/paper.jats'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/paper-openjournals/build/jats/jats/paper.jats
          ASSERT_GLOB: '1'
        run: python .dev/harness/scenarios/assert-file-exists.py

# GENERATED by devflows test-generate — do not edit; source: workflows/*/devflow.yaml.
# This workflow only works under `act`. Run it with `task scenarios-local`; the
# require-act guard job fails fast if it is dispatched on GitHub-hosted runners.
name: 'DevFlows Local Scenarios: Python Build'
'on':
  workflow_dispatch: null
permissions:
  contents: read
jobs:
  require_act:
    name: Require act
    runs-on: ubuntu-latest
    steps:
      - name: Fail unless running under act
        shell: bash
        run: |-
          if [ "${ACT:-}" != "true" ]; then
            echo "::error::This local scenario workflow runs only under act. Run 'task scenarios-local' locally; do not dispatch it on GitHub."
            exit 1
          fi
          echo "Running under act."
  python_build_nothing_to_build_validate:
    name: 'Python Build: All build flavors disabled fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: uv
          BUILD_SDIST_ENABLED: 'false'
          BUILD_WHEEL_ENABLED: 'false'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'false'
          CIBW_MATRIX: '[]'
          CONDA_ENABLED: 'false'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: python-build
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: Nothing to build
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  python_build_cibw_empty_matrix_validate:
    name: 'Python Build: cibw enabled with an empty matrix fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: uv
          BUILD_SDIST_ENABLED: 'true'
          BUILD_WHEEL_ENABLED: 'true'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'true'
          CIBW_MATRIX: '[]'
          CONDA_ENABLED: 'false'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: python-build
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: cibw-enabled is true but cibw-matrix is empty
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  python_build_conda_missing_recipe_validate:
    name: 'Python Build: conda enabled without a recipe path fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: uv
          BUILD_SDIST_ENABLED: 'true'
          BUILD_WHEEL_ENABLED: 'true'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'false'
          CIBW_MATRIX: '[]'
          CONDA_ENABLED: 'true'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: python-build
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: conda-recipe-path is empty
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  python_build_unknown_build_tool_validate:
    name: 'Python Build: Unknown build tool fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: cargo
          BUILD_SDIST_ENABLED: 'true'
          BUILD_WHEEL_ENABLED: 'true'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'false'
          CIBW_MATRIX: '[]'
          CONDA_ENABLED: 'false'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: python-build
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: build-tool must be 'uv' or 'python-build'
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  python_build_malformed_matrix_json_validate:
    name: 'Python Build: Malformed cibw-matrix JSON fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: uv
          BUILD_SDIST_ENABLED: 'true'
          BUILD_WHEEL_ENABLED: 'true'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'false'
          CIBW_MATRIX: '{not json'
          CONDA_ENABLED: 'false'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: python-build
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: cibw-matrix is not valid JSON
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act
  python_build_unsafe_artifact_prefix_validate:
    name: 'Python Build: Unsafe dist-artifact-prefix fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: uv
          BUILD_SDIST_ENABLED: 'true'
          BUILD_WHEEL_ENABLED: 'true'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'false'
          CIBW_MATRIX: '[]'
          CONDA_ENABLED: 'false'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: has space
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: dist-artifact-prefix must be a non-empty artifact-name-safe
            string
        run: python .dev/harness/scenarios/assert-validation-failure.py
    needs: require_act

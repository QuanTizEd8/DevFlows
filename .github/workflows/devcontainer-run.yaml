# GENERATED by devflows sync — do not edit; source: workflows/devcontainer-run/
name: '[Reusable]: Devcontainer Run'
'on':
  workflow_call:
    inputs:
      devcontainer-image:
        description: OCI image reference of a PREBUILT devcontainer to run, [registry/]name[:tag][@digest]. Required unless devcontainer-config-file supplies an image. Validated syntactically and pulled without a rebuild; when it carries a devcontainer.metadata label (every build-devcontainer image does) its features, lifecycle hooks, remoteUser, and env are applied automatically. When devcontainer-config-file is also set, this overrides that config's image.
        type: string
        required: false
        default: ''
      devcontainer-config-file:
        description: Optional workspace-relative path to a caller .devcontainer/devcontainer.json for full control (its features and lifecycle hooks are preserved). Validated relative, no '..', inside GITHUB_WORKSPACE. When empty, a minimal image-only config is synthesized from devcontainer-image. The resolved config must yield an image (this workflow runs a prebuilt image and never builds from a Dockerfile), else the run fails loudly.
        type: string
        required: false
        default: ''
      run-command:
        description: The command line executed inside the container, as `<run-shell> -c "<run-command>"`. Injection-safe - delivered to the container shell env-mediated as a single literal argv token, never interpolated into this workflow's own run block, so a `$(...)`, `;`, or space is inert at the workflow layer (the container shell still interprets it, which is the intended, contained behavior). Its exit code becomes the step's exit code.
        type: string
        required: true
      run-shell:
        description: Shell used to run run-command inside the container; one of bash or sh.
        type: string
        required: false
        default: bash
      run-working-directory:
        description: Optional workspace-relative subdirectory to cd into (inside the container's workspace mount) before run-command. Validated relative, no '..', inside GITHUB_WORKSPACE. Empty runs at the container's default workspace folder.
        type: string
        required: false
        default: ''
      remote-user:
        description: Optional user to run the command as inside the container (injected as "remoteUser" into the override config, e.g. root). Empty keeps the config/label remoteUser.
        type: string
        required: false
        default: ''
      skip-post-create:
        description: Whether to pass --skip-post-create to `devcontainer up` (start the container and entrypoint without running the creation lifecycle hooks).
        type: boolean
        required: false
        default: false
      update-remote-user-uid:
        description: --update-remote-user-uid-default for `devcontainer up`; one of never, on, or off. Rewrites the remote user's UID/GID to match the host user so bind-mounted files stay writable; set never for images that misbehave with the update.
        type: string
        required: false
        default: 'on'
      container-env:
        description: Newline-separated KEY=VALUE pairs passed to the container as repeated --remote-env for both `up` (so lifecycle hooks get them) and `exec` (so the command gets them). Each key must be a valid environment variable name; the value is taken literally after the first '=' (so '=' and spaces are fine) and an embedded control character is rejected. For NON-secret values only - --remote-env places KEY=VALUE in the up/exec argv, which is visible in the runner's process list, so do not put secrets here.
        type: string
        required: false
        default: ''
      devcontainer-features:
        description: Optional JSON object of additional dev container Features (feature-id -> options) passed to `devcontainer up` as --additional-features. Validated as a JSON object.
        type: string
        required: false
        default: ''
      remove-container:
        description: Whether the always() cleanup step removes the container (docker rm -f) at the end of the run. Files the command wrote into the workspace persist on the host bind mount regardless.
        type: boolean
        required: false
        default: true
      devcontainer-cli-version:
        description: The pinned @devcontainers/cli npm version installed with `npm install -g`. Kept current by Renovate.
        type: string
        required: false
        default: 0.87.0
      run-timeout-minutes:
        description: timeout-minutes for the validate and run jobs.
        type: number
        required: false
        default: 30
      docker-login-enabled:
        description: Whether to log in to a Docker registry before pulling and running the image.
        type: boolean
        required: false
        default: false
      docker-registry:
        description: Docker registry hostname. Leave empty for Docker Hub.
        type: string
        required: false
        default: ghcr.io
      docker-username:
        description: Docker registry username.
        type: string
        required: false
        default: ${{ github.repository_owner }}
      cache-enabled:
        description: Whether to restore (before `devcontainer up`) and save (in the post-job hook) a workspace cache with actions/cache. Restoring before `up` lets the creation lifecycle hooks (e.g. a pixi install) reuse a previously cached environment instead of rebuilding it every run. When true, cache-paths and cache-key are required.
        type: boolean
        required: false
        default: false
      cache-paths:
        description: Newline-separated paths to cache, passed verbatim to actions/cache's `path`. These are HOST paths - a workspace-relative path (e.g. .pixi) is bind-mounted into the container so it caches directly; a cache directory that lives only INSIDE the container must be bind-mounted to a workspace/host path (via the config's mounts) to be cacheable. Required when cache-enabled is true.
        type: string
        required: false
        default: ''
      cache-key:
        description: The exact actions/cache key to restore and save under. Required when cache-enabled is true; typically includes a lockfile hash the caller computes with hashFiles (e.g. a key like pixi-<hash-of-pixi.lock>).
        type: string
        required: false
        default: ''
      cache-restore-keys:
        description: Optional newline-separated fallback key prefixes for actions/cache's `restore-keys`, tried in order when cache-key misses so a run can still restore a close-enough older cache.
        type: string
        required: false
        default: ''
      checkout-enabled:
        description: Whether to run actions/checkout before workflow execution.
        type: boolean
        required: false
        default: true
      checkout-repository:
        description: Repository name with owner to checkout.
        type: string
        required: false
        default: ${{ github.repository }}
      checkout-ref:
        description: Branch, tag, or SHA to checkout.
        type: string
        required: false
        default: ${{ github.ref }}
      checkout-ssh-known-hosts:
        description: Known hosts in addition to the user and global host key database.
        type: string
        required: false
        default: ''
      checkout-ssh-strict:
        description: Whether to perform strict host key checking.
        type: boolean
        required: false
        default: true
      checkout-ssh-user:
        description: SSH user to use when connecting to the remote SSH host.
        type: string
        required: false
        default: git
      checkout-persist-credentials:
        description: Whether to configure the token or SSH key with local git config.
        type: boolean
        required: false
        default: true
      checkout-path:
        description: Relative path under GITHUB_WORKSPACE to place the repository.
        type: string
        required: false
        default: ''
      checkout-clean:
        description: Whether to clean and reset before fetching.
        type: boolean
        required: false
        default: true
      checkout-filter:
        description: Partial clone filter. Overrides sparse checkout when set.
        type: string
        required: false
        default: ''
      checkout-sparse-checkout:
        description: Sparse checkout patterns separated by new lines.
        type: string
        required: false
        default: ''
      checkout-sparse-checkout-cone-mode:
        description: Whether to use cone mode for sparse checkout.
        type: boolean
        required: false
        default: true
      checkout-fetch-depth:
        description: Number of commits to fetch. Use 0 for all history.
        type: number
        required: false
        default: 1
      checkout-fetch-tags:
        description: Whether to fetch tags when fetch-depth is greater than 0.
        type: boolean
        required: false
        default: false
      checkout-show-progress:
        description: Whether to show fetch progress output.
        type: boolean
        required: false
        default: true
      checkout-lfs:
        description: Whether to download Git LFS files.
        type: boolean
        required: false
        default: false
      checkout-submodules:
        description: Whether to checkout submodules; use recursive for nested submodules.
        type: string
        required: false
        default: 'false'
      checkout-set-safe-directory:
        description: Whether to add the repository path as a safe Git directory.
        type: boolean
        required: false
        default: true
      checkout-github-server-url:
        description: GitHub server URL to fetch from.
        type: string
        required: false
        default: ''
      checkout-allow-unsafe-pr-checkout:
        description: Allow checkout of fork pull request code in trusted contexts.
        type: boolean
        required: false
        default: false
      artifact-download-enabled:
        description: Whether to download artifacts before workflow execution.
        type: boolean
        required: false
        default: false
      artifact-download-name:
        description: Name of the artifact to download. Leave empty to download all artifacts.
        type: string
        required: false
        default: ''
      artifact-download-ids:
        description: Comma-separated artifact IDs to download. Mutually exclusive with artifact-download-name.
        type: string
        required: false
        default: ''
      artifact-download-path:
        description: Destination path for downloaded artifacts.
        type: string
        required: false
        default: .
      artifact-download-pattern:
        description: Glob pattern matching artifacts to download when name is unset.
        type: string
        required: false
        default: ''
      artifact-download-merge-multiple:
        description: Whether multiple matched artifacts are extracted into one directory.
        type: boolean
        required: false
        default: false
      artifact-download-repository:
        description: Repository to download artifacts from when a token is set.
        type: string
        required: false
        default: ${{ github.repository }}
      artifact-download-run-id:
        description: Workflow run ID to download artifacts from when a token is set.
        type: string
        required: false
        default: ${{ github.run_id }}
      artifact-download-skip-decompress:
        description: Whether to download the artifact archive without extracting it.
        type: boolean
        required: false
        default: false
      artifact-download-digest-mismatch:
        description: Behavior when a downloaded artifact digest does not match.
        type: string
        required: false
        default: error
      artifact-upload-enabled:
        description: Whether to upload artifacts after workflow execution.
        type: boolean
        required: false
        default: false
      artifact-upload-path:
        description: File, directory, or wildcard pattern to upload after workflow execution.
        type: string
        required: false
        default: ''
      artifact-upload-name:
        description: Artifact name.
        type: string
        required: false
        default: artifact
      artifact-upload-if-no-files-found:
        description: Behavior when no files match artifact-upload-path.
        type: string
        required: false
        default: warn
      artifact-upload-retention-days:
        description: Artifact retention in days. Use 0 for repository default.
        type: number
        required: false
        default: 0
      artifact-upload-compression-level:
        description: Compression level from 0 to 9.
        type: number
        required: false
        default: 6
      artifact-upload-overwrite:
        description: Whether to overwrite an existing artifact with the same name.
        type: boolean
        required: false
        default: false
      artifact-upload-include-hidden-files:
        description: Whether hidden files under artifact-upload-path are included.
        type: boolean
        required: false
        default: false
      artifact-upload-archive:
        description: Whether upload-artifact archives files before upload.
        type: boolean
        required: false
        default: true
    secrets:
      docker-password:
        description: Docker registry password or token. Defaults to github.token for ghcr.io only.
        required: false
      run-secrets:
        description: Optional bundle of caller SECRETS injected into the run so they reach the creation lifecycle hooks (via `devcontainer up --secrets-file`) and the command (via the exec override-config remoteEnv, a transient docker exec -e, never a process-list-visible --remote-env). Declared as a secret so its value is GitHub-masked; NEVER pass secrets through the non-secret container-env input. Two forms are accepted. Primary least-privilege form is a JSON object the caller assembles with per-value toJSON so each value stays individually masked and correctly escaped (an object mapping each NAME to its own toJSON(secrets.NAME) expression). Advanced forward-all form sets the caller job secrets to inherit and passes run-secrets as a toJSON(secrets) expression (the workflow strips github_token defensively so the Actions token is never injected). A newline KEY=VALUE form (# comments and blank lines skipped) is also accepted. Each key must be a POSIX env-var name; values are literal. github_token
          and GITHUB_TOKEN are always dropped.
        required: false
      checkout-token:
        description: Token used by actions/checkout. Defaults to github.token.
        required: false
      checkout-ssh-key:
        description: SSH key used by actions/checkout.
        required: false
      artifact-download-token:
        description: Token used to download artifacts from another repository or run.
        required: false
    outputs:
      container-id:
        description: The container ID `devcontainer up` started for this run.
        value: ${{ jobs.run.outputs.container-id }}
      remote-user:
        description: The remoteUser the command ran as (from the config or the image metadata label).
        value: ${{ jobs.run.outputs.remote-user }}
permissions: {}
jobs:
  validate:
    name: Validate inputs
    runs-on: ubuntu-latest
    permissions: {}
    timeout-minutes: ${{ inputs.run-timeout-minutes }}
    steps:
      - name: Materialize DevFlows runtime scripts
        id: devflows-runtime
        shell: bash
        run: |-
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/devflows/devcontainer-run"
          cat > "$RUNNER_TEMP/devflows/devcontainer-run/dcrun.py" <<'DEVFLOWS_SCRIPT_EOF'
          """Shared validation, config-synthesis, and argv helpers for devcontainer-run.

          validate-inputs.py (the fail-fast validate job), check-registry-auth.py (the
          registry-auth preflight), run-devcontainer.py, and cleanup.py all resolve the
          caller inputs through this module so every job agrees exactly on what is legal.

          Every input reaches this module only through os.environ (mapped from inputs.* by
          the workflow), never interpolated into a shell string. run-devcontainer.py builds
          the `devcontainer up`/`devcontainer exec` argv programmatically and calls
          subprocess.run without a shell; the caller's run-command is delivered as a single
          literal argv token to the container shell's -c (the intended, contained place for
          shell semantics), so a `$(...)`, `;`, or space in it is never re-expanded by the
          workflow. This is the whole point of the env-mediated design.
          """

          from __future__ import annotations

          import json
          import os
          import re
          import shlex
          from collections.abc import Mapping
          from dataclasses import dataclass
          from pathlib import Path

          # Syntactic image-reference grammar, identical to paper-openjournals/pandoc: a
          # well-formed [registry/]name[:tag][@digest] reference. Validation is syntactic
          # only; the caller is trusted to supply an image they trust (it runs with the
          # workspace bind-mounted).
          _ALNUM = r"[a-z0-9]+"
          _SEPARATOR = r"(?:[._]|__|[-]+)"
          _PATH_COMPONENT = rf"{_ALNUM}(?:{_SEPARATOR}{_ALNUM})*"
          _NAME = rf"{_PATH_COMPONENT}(?:/{_PATH_COMPONENT})*"
          _DOMAIN_COMPONENT = r"(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])"
          _DOMAIN = rf"{_DOMAIN_COMPONENT}(?:\.{_DOMAIN_COMPONENT})*(?::[0-9]+)?"
          _TAG = r"[A-Za-z0-9_][A-Za-z0-9_.-]{0,127}"
          _DIGEST = r"[A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*:[0-9A-Fa-f]{32,}"
          _IMAGE_REFERENCE = re.compile(rf"^(?:{_DOMAIN}/)?{_NAME}(?::{_TAG})?(?:@{_DIGEST})?$")

          # run-shell enum: the shell the caller's run-command is executed under.
          SHELLS = ("bash", "sh")
          # --update-remote-user-uid-default enum.
          UID_MODES = ("never", "on", "off")
          # A POSIX-ish environment variable name for container-env keys.
          _ENV_NAME = re.compile(r"^[A-Za-z_][A-Za-z0-9_]*$")
          # ASCII control characters (C0 range plus DEL). container-env/cache entries are
          # split on newlines and then rejected if any control char survives, so an embedded
          # newline/escape/NUL in a single entry is a hard error rather than a silent split.
          _CONTROL = re.compile(r"[\x00-\x1f\x7f]")
          # A remoteUser value: a username or a bare numeric uid. It is written into JSON
          # (never a shell), so this only rejects whitespace/control and shell-looking junk.
          _REMOTE_USER = re.compile(r"^[A-Za-z0-9_][A-Za-z0-9_.-]*$")
          # A pinned @devcontainers/cli npm version. Constraining it here keeps the
          # env-mediated `npm install -g @devcontainers/cli@<version>` install safe.
          _CLI_VERSION = re.compile(r"^[0-9]+\.[0-9]+\.[0-9]+(?:[-+.][0-9A-Za-z.-]+)?$")
          # Keys defensively dropped from a run-secrets bundle: the ephemeral Actions token,
          # which a `toJSON(secrets)` + `secrets: inherit` bundle would otherwise carry into
          # the user command. GitHub exposes it as GITHUB_TOKEN and (in the serialized
          # secrets context) as github_token, so both spellings are stripped.
          _DROPPED_SECRET_KEYS = frozenset({"github_token", "GITHUB_TOKEN"})
          # RUNNER_TEMP subdirectory holding this run's generated override config and the
          # 0600 secret files. run-devcontainer.py writes them here; cleanup.py shreds the
          # secret files from the same place in its always() step.
          _STATE_SUBDIR = "devflows-devcontainer-run"


          @dataclass(frozen=True)
          class Config:
              """A fully validated invocation, shared by every job."""

              image: str  # '' when a devcontainer-config-file supplies the image instead
              config_file: str  # validated workspace-relative path, or ''
              run_command: str
              run_shell: str
              run_working_directory: str  # validated workspace-relative subdir, or ''
              remote_user: str  # '' means "leave the config/label remoteUser untouched"
              skip_post_create: bool
              update_remote_user_uid: str
              container_env: tuple[str, ...]  # validated KEY=VALUE lines
              features: str  # raw JSON object string for --additional-features, or ''
              cli_version: str
              cache_enabled: bool  # whether the actions/cache restore/save step is active
              cache_paths: tuple[str, ...]  # host/workspace paths cached, one per line
              cache_key: str  # exact restore/save key
              cache_restore_keys: tuple[str, ...]  # ordered fallback key prefixes


          def parse_and_validate(env: Mapping[str, str], *, require_workspace: bool = True) -> Config:
              """Validate every input and return the resolved Config.

              Runs in the validate job (before checkout) and in the run job (after
              checkout); neither path touches the filesystem here, so both agree exactly.
              The devcontainer-config-file is only checked lexically (containment); its
              existence and its JSON contents are validated by run-devcontainer.py once the
              checkout is present.
              """
              workspace = _workspace(env, require_workspace)

              run_command = env.get("RUN_COMMAND", "")
              if not run_command.strip():
                  raise SystemExit("run-command is required; it must not be empty.")

              run_shell = env.get("RUN_SHELL", "bash").strip() or "bash"
              if run_shell not in SHELLS:
                  raise SystemExit(f"run-shell must be one of {', '.join(SHELLS)}; got {run_shell!r}.")

              image = _validate_image(env.get("DEVCONTAINER_IMAGE", ""))
              config_file = _validate_config_file(env.get("DEVCONTAINER_CONFIG_FILE", ""), workspace)
              if not image and not config_file:
                  raise SystemExit(
                      "at least one of devcontainer-image or devcontainer-config-file is required "
                      "so the run has an image to start (no image => nothing to run)."
                  )

              working_directory = _validate_working_directory(env.get("RUN_WORKING_DIRECTORY", ""), workspace)
              remote_user = _validate_remote_user(env.get("REMOTE_USER", ""))
              update_uid = env.get("UPDATE_REMOTE_USER_UID", "on").strip() or "on"
              if update_uid not in UID_MODES:
                  raise SystemExit(
                      f"update-remote-user-uid must be one of {', '.join(UID_MODES)}; got {update_uid!r}."
                  )
              container_env = _validate_container_env(env.get("CONTAINER_ENV", ""))
              features = _validate_features(env.get("DEVCONTAINER_FEATURES", ""))
              cli_version = _validate_cli_version(env.get("DEVCONTAINER_CLI_VERSION", ""))
              cache_enabled, cache_paths, cache_key, cache_restore_keys = _validate_cache(env)

              return Config(
                  image=image,
                  config_file=config_file,
                  run_command=run_command,
                  run_shell=run_shell,
                  run_working_directory=working_directory,
                  remote_user=remote_user,
                  skip_post_create=_truthy(env.get("SKIP_POST_CREATE", "")),
                  update_remote_user_uid=update_uid,
                  container_env=container_env,
                  features=features,
                  cli_version=cli_version,
                  cache_enabled=cache_enabled,
                  cache_paths=cache_paths,
                  cache_key=cache_key,
                  cache_restore_keys=cache_restore_keys,
              )


          def _workspace(env: Mapping[str, str], require_workspace: bool) -> Path:
              value = env.get("GITHUB_WORKSPACE")
              if not value:
                  if not require_workspace:
                      return Path.cwd()
                  raise SystemExit(
                      "GITHUB_WORKSPACE is not set; devcontainer-run validation must run in a "
                      "GitHub Actions or act runner environment where it is defined."
                  )
              return Path(value).resolve()


          def _validate_image(value: str) -> str:
              image = value.strip()
              if not image:
                  return ""
              if not _IMAGE_REFERENCE.match(image):
                  raise SystemExit(
                      f"devcontainer-image is not a valid image reference: {value!r}. "
                      "Expected [registry/]name[:tag][@digest]."
                  )
              return image


          def _validate_config_file(value: str, workspace: Path) -> str:
              if not value.strip():
                  return ""
              return _workspace_relative(value, workspace, "devcontainer-config-file")


          def _validate_working_directory(value: str, workspace: Path) -> str:
              if not value.strip():
                  return ""
              return _workspace_relative(value, workspace, "run-working-directory")


          def _validate_remote_user(value: str) -> str:
              user = value.strip()
              if not user:
                  return ""
              if not _REMOTE_USER.match(user):
                  raise SystemExit(
                      f"remote-user must be a plain username or uid: {value!r}. "
                      "Allowed characters are letters, digits, '_', '.', and '-'."
                  )
              return user


          def _validate_container_env(value: str) -> tuple[str, ...]:
              """Validate the non-secret container-env bundle into KEY=VALUE lines.

              Split on newlines only (not the broader str.splitlines set) so an entry that
              smuggles an embedded control character is rejected here rather than silently
              torn into two entries. The key must be a POSIX env name; the value is taken
              literally after the first '=' (so '=' and spaces in a value are fine) and is
              never eval'd -- it is handed to `--remote-env KEY=VALUE` verbatim.
              """
              entries: list[str] = []
              for line in value.split("\n"):
                  item = line.strip()
                  if not item:
                      continue
                  if _CONTROL.search(item):
                      raise SystemExit(
                          f"container-env has a control character in an entry: {line!r}; entries must be "
                          "plain KEY=VALUE lines separated by newlines."
                      )
                  if "=" not in item:
                      raise SystemExit(
                          f"container-env entries must be KEY=VALUE (newline-separated); got {line!r}."
                      )
                  key = item.split("=", 1)[0]
                  if not _ENV_NAME.match(key):
                      raise SystemExit(f"container-env has an invalid environment variable name: {key!r}.")
                  entries.append(item)
              return tuple(entries)


          # --------------------------------------------------------------------------- #
          # run-secrets bundle (a DECLARED workflow_call secret, GitHub-masked)          #
          # --------------------------------------------------------------------------- #
          def parse_run_secrets(raw: str) -> dict[str, str]:
              """Parse the masked run-secrets bundle into a {KEY: VALUE} dict.

              Accepts a JSON object (primary; text starts with ``{``, values must be
              strings) or a ``KEY=VALUE``-per-line form (blank/``#`` lines skipped, split on
              the first ``=``). Each KEY must be a POSIX env name with no control character;
              a VALUE is an opaque literal. github_token / GITHUB_TOKEN are dropped so a
              ``toJSON(secrets)`` + ``inherit`` bundle never injects the Actions token.
              Errors quote only KEY names (never a value), and the JSON error is not chained,
              so no secret material leaks into the log.
              """
              text = raw.strip()
              if not text:
                  return {}
              pairs = _run_secrets_from_json(text) if text.startswith("{") else _run_secrets_from_lines(raw)
              result: dict[str, str] = {}
              for key, value in pairs:
                  if key in _DROPPED_SECRET_KEYS:
                      continue
                  if _CONTROL.search(key):
                      raise SystemExit("run-secrets has a control character in a key name.")
                  if not _ENV_NAME.match(key):
                      raise SystemExit(
                          f"run-secrets has an invalid environment variable name: {key!r}. "
                          "Keys must match [A-Za-z_][A-Za-z0-9_]*."
                      )
                  result[key] = value
              return result


          def _run_secrets_from_json(text: str) -> list[tuple[str, str]]:
              try:
                  parsed = json.loads(text)
              except json.JSONDecodeError:
                  # `from None`: the decoder message can quote secret material; do not chain.
                  raise SystemExit(
                      "run-secrets starts with '{' but is not valid JSON. Provide a JSON object "
                      "of NAME -> value (assembled with per-value toJSON), or the KEY=VALUE form."
                  ) from None
              if not isinstance(parsed, dict):
                  raise SystemExit("run-secrets JSON must be an object of NAME -> value.")
              pairs: list[tuple[str, str]] = []
              for key, value in parsed.items():
                  if not isinstance(value, str):
                      raise SystemExit(
                          f"run-secrets value for {str(key)!r} must be a string; got {type(value).__name__}."
                      )
                  pairs.append((str(key), value))
              return pairs


          def _run_secrets_from_lines(raw: str) -> list[tuple[str, str]]:
              pairs: list[tuple[str, str]] = []
              for line in raw.split("\n"):
                  item = line.strip()
                  if not item or item.startswith("#"):
                      continue
                  if "=" not in item:
                      raise SystemExit(
                          "run-secrets has a KEY=VALUE line with no '='; each non-blank, non-# "
                          "line must be KEY=VALUE."
                      )
                  key, value = item.split("=", 1)
                  pairs.append((key, value))
              return pairs


          def _validate_cache(env: Mapping[str, str]) -> tuple[bool, tuple[str, ...], str, tuple[str, ...]]:
              """Validate the actions/cache inputs (only enforced when cache-enabled).

              cache-paths / cache-key / cache-restore-keys reach the SHA-pinned actions/cache
              step through its `with:` block (never a run: body), so they are injection-safe by
              construction; this still rejects control characters and requires the paths+key
              that make the step meaningful. Paths may be workspace-relative (e.g. .pixi, which
              the container sees through the bind mount) or absolute host paths. When
              cache-enabled is false the values are ignored (the step is `if:`-gated off), so
              validation is skipped entirely.
              """
              enabled = _truthy(env.get("CACHE_ENABLED", ""))
              if not enabled:
                  return False, (), "", ()
              paths = _split_cache_lines(env.get("CACHE_PATHS", ""), "cache-paths")
              if not paths:
                  raise SystemExit(
                      "cache-enabled is true but cache-paths is empty; provide at least one "
                      "newline-separated path to cache (e.g. .pixi)."
                  )
              key = env.get("CACHE_KEY", "").strip()
              if not key:
                  raise SystemExit(
                      "cache-enabled is true but cache-key is empty; provide a non-empty cache-key."
                  )
              if _CONTROL.search(key):
                  raise SystemExit("cache-key must not contain control characters (including newlines).")
              restore_keys = _split_cache_lines(env.get("CACHE_RESTORE_KEYS", ""), "cache-restore-keys")
              return True, paths, key, restore_keys


          def _split_cache_lines(value: str, field: str) -> tuple[str, ...]:
              """Split a newline list into stripped entries, rejecting control characters.

              Splitting on '\\n' only (then rejecting any surviving control char) means an
              embedded newline/escape inside a single path or restore-key is a hard error, not
              a silent extra entry.
              """
              items: list[str] = []
              for line in value.split("\n"):
                  item = line.strip()
                  if not item:
                      continue
                  if _CONTROL.search(item):
                      raise SystemExit(f"{field} has a control character in an entry: {line!r}.")
                  items.append(item)
              return tuple(items)


          def _validate_features(value: str) -> str:
              raw = value.strip()
              if not raw:
                  return ""
              try:
                  parsed = json.loads(raw)
              except json.JSONDecodeError as error:
                  raise SystemExit(f"devcontainer-features must be valid JSON: {error}.") from error
              if not isinstance(parsed, dict):
                  raise SystemExit("devcontainer-features must be a JSON object of feature-id -> options.")
              return raw


          def _validate_cli_version(value: str) -> str:
              version = value.strip()
              if not version:
                  raise SystemExit("devcontainer-cli-version is required (the pinned @devcontainers/cli).")
              if not _CLI_VERSION.match(version):
                  raise SystemExit(
                      f"devcontainer-cli-version must be a valid version (e.g. 0.87.0); got {value!r}."
                  )
              return version


          def _workspace_relative(value: str, workspace: Path, field: str) -> str:
              """Return the workspace-relative POSIX path, rejecting escapes.

              Rejects absolute paths and any '..' component before resolving, then confirms
              the resolved path stays inside GITHUB_WORKSPACE.
              """
              candidate = Path(value)
              if candidate.is_absolute() or ".." in candidate.parts:
                  raise SystemExit(
                      f"{field} must stay inside GITHUB_WORKSPACE (no absolute paths and no '..'): {value}"
                  )
              resolved = (workspace / candidate).resolve()
              if resolved != workspace and workspace not in resolved.parents:
                  raise SystemExit(f"{field} must stay inside GITHUB_WORKSPACE: {value}")
              return resolved.relative_to(workspace).as_posix()


          def validate_registry_auth(env: Mapping[str, str]) -> None:
              """Refuse to send github.token to a registry other than ghcr.io.

              Verbatim from build-devcontainer: the docker-login password falls back to
              github.token only for ghcr.io. Any other registry must supply an explicit
              docker-password secret, otherwise the caller could silently leak github.token
              to a third-party registry. This runs in its own validate-job step because it
              reads a secret-presence expression, which the validation-failure scenario
              harness (inputs-only) cannot reconstruct.
              """
              if not _truthy(env.get("DOCKER_LOGIN_ENABLED", "")):
                  return
              if _truthy(env.get("DOCKER_PASSWORD_SET", "")):
                  return
              registry = env.get("DOCKER_REGISTRY", "").strip().lower()
              if registry != "ghcr.io":
                  target = registry or "docker hub"
                  raise SystemExit(
                      f"docker-login-enabled is true for registry {target!r} but no docker-password "
                      "secret was provided. The github.token login fallback is restricted to ghcr.io; "
                      "pass docker-password to authenticate to any other registry."
                  )


          # --------------------------------------------------------------------------- #
          # Config synthesis + argv (run job)                                            #
          # --------------------------------------------------------------------------- #
          def parse_jsonc(text: str) -> object:
              """Parse a devcontainer.json, tolerating JSONC comments / trailing commas.

              Plain JSON parses directly (the common case and every checked-in fixture);
              only a JSONDecodeError falls back to comment/trailing-comma stripping, so a
              valid document is never mangled.
              """
              try:
                  return json.loads(text)
              except json.JSONDecodeError:
                  return json.loads(_strip_jsonc(text))


          def _strip_jsonc(text: str) -> str:
              out: list[str] = []
              i = 0
              n = len(text)
              in_string = False
              while i < n:
                  char = text[i]
                  if in_string:
                      out.append(char)
                      if char == "\\" and i + 1 < n:
                          out.append(text[i + 1])
                          i += 2
                          continue
                      if char == '"':
                          in_string = False
                      i += 1
                      continue
                  if char == '"':
                      in_string = True
                      out.append(char)
                      i += 1
                      continue
                  if char == "/" and i + 1 < n and text[i + 1] == "/":
                      i += 2
                      while i < n and text[i] not in "\r\n":
                          i += 1
                      continue
                  if char == "/" and i + 1 < n and text[i + 1] == "*":
                      i += 2
                      while i + 1 < n and not (text[i] == "*" and text[i + 1] == "/"):
                          i += 1
                      i += 2
                      continue
                  out.append(char)
                  i += 1
              return re.sub(r",(\s*[}\]])", r"\1", "".join(out))


          def load_caller_config(workspace: Path, config_file: str) -> dict | None:
              """Read and JSON-parse the caller's devcontainer.json, or None when unset."""
              if not config_file:
                  return None
              path = (workspace / config_file).resolve()
              if not path.is_file():
                  raise SystemExit(f"devcontainer-config-file does not exist or is not a file: {config_file}")
              parsed = parse_jsonc(path.read_text(encoding="utf-8"))
              if not isinstance(parsed, dict):
                  raise SystemExit("devcontainer-config-file must contain a JSON object.")
              return parsed


          def synthesize_override_config(config: Config, caller_config: dict | None) -> dict:
              """Build the override devcontainer config written to RUNNER_TEMP.

              When no config-file is given this is a minimal {"image": <ref>} that the
              image's devcontainer.metadata label enriches with features/hooks/user/env.
              When a config-file IS given it is the base, so its features and lifecycle
              hooks are preserved. devcontainer-image (when set) overrides the base image;
              remote-user (when set) forces the exec user. The resolved config MUST carry
              an image so the run never rebuilds from a Dockerfile.
              """
              base: dict = dict(caller_config) if caller_config else {}
              if config.image:
                  base["image"] = config.image
              resolved = base.get("image")
              if not isinstance(resolved, str) or not resolved.strip():
                  raise SystemExit(
                      "no image could be resolved: provide devcontainer-image, or a "
                      'devcontainer-config-file whose JSON sets "image". devcontainer-run '
                      "runs a prebuilt image and never builds from a Dockerfile."
                  )
              if config.remote_user:
                  base["remoteUser"] = config.remote_user
              return base


          def build_exec_override_config(base_override: dict, secrets: Mapping[str, str]) -> dict:
              """The exec-phase override config: the base plus the run-secrets in remoteEnv.

              Secrets go in remoteEnv only for ``exec`` (never ``up``): exec injects them via
              a transient ``docker exec -e``, so they never land in the persisted
              ``devcontainer.metadata`` label an ``up`` remoteEnv would leak. Any caller
              remoteEnv is preserved; a shallow copy leaves the base (for ``up``) unchanged.
              """
              exec_override = dict(base_override)
              existing = exec_override.get("remoteEnv")
              remote_env = dict(existing) if isinstance(existing, dict) else {}
              remote_env.update(secrets)
              exec_override["remoteEnv"] = remote_env
              return exec_override


          def state_dir(runner_temp: Path) -> Path:
              """RUNNER_TEMP subdirectory holding this run's generated override + secret files."""
              return runner_temp / _STATE_SUBDIR


          def secret_file_paths(runner_temp: Path) -> tuple[Path, Path]:
              """The two 0600 secret files (up --secrets-file, exec override); cleanup shreds them."""
              directory = state_dir(runner_temp)
              return directory / "secrets.json", directory / "exec-config.json"


          def write_secret_file(path: Path, data: object) -> None:
              """Serialize ``data`` to a 0600 JSON file, never briefly readable and never echoed."""
              text = json.dumps(data, indent=2) + "\n"
              fd = os.open(path, os.O_WRONLY | os.O_CREAT | os.O_TRUNC, 0o600)
              try:
                  os.write(fd, text.encode("utf-8"))
              finally:
                  os.close(fd)
              os.chmod(path, 0o600)


          def shred_file(path: Path) -> bool:
              """Overwrite with zeros, fsync, then unlink; True if removed, tolerant of absence."""
              try:
                  size = path.stat().st_size
              except FileNotFoundError:
                  return False
              try:
                  fd = os.open(path, os.O_WRONLY)
                  try:
                      os.write(fd, b"\0" * size)
                      os.fsync(fd)
                  finally:
                      os.close(fd)
              except OSError:
                  pass
              try:
                  path.unlink()
              except FileNotFoundError:
                  return False
              return True


          def cli_invocation(config: Config) -> list[str]:
              """The pinned @devcontainers/cli invocation prefix.

              Run ephemerally with npx (the npm peer of uvx), never `npm install -g`: the
              version is pinned, nothing is installed into a global prefix, and it keeps the
              generated workflow free of an ad-hoc package-install step.
              """
              return ["npx", "--yes", f"@devcontainers/cli@{config.cli_version}"]


          def build_up_argv(
              config: Config,
              workspace: Path,
              config_path: Path,
              id_label: str,
              secrets_file: Path | None = None,
          ) -> list[str]:
              """Assemble the `devcontainer up` argv (JSON result, no rebuild).

              When run-secrets carried values, ``secrets_file`` is the 0600 JSON file added
              as ``--secrets-file`` so the creation HOOKS get the secrets (only ``up`` accepts
              the flag; ``exec`` rejects it). Only the PATH lands in the argv, never a value,
              and secrets are kept out of this config's remoteEnv (which ``up`` would bake
              into the persisted devcontainer.metadata label).
              """
              argv = [
                  *cli_invocation(config),
                  "up",
                  "--workspace-folder",
                  str(workspace),
                  "--override-config",
                  str(config_path),
                  "--id-label",
                  id_label,
                  "--log-format",
                  "json",
                  "--update-remote-user-uid-default",
                  config.update_remote_user_uid,
                  "--remove-existing-container",
              ]
              if config.skip_post_create:
                  argv.append("--skip-post-create")
              if config.features:
                  argv += ["--additional-features", config.features]
              for entry in config.container_env:
                  argv += ["--remote-env", entry]
              if secrets_file is not None:
                  argv += ["--secrets-file", str(secrets_file)]
              return argv


          def build_exec_command(config: Config) -> str:
              """The single -c payload the container shell runs.

              With run-working-directory empty (the default) this is the caller's
              run-command verbatim, so it stays one literal argv token. When a working
              directory is set, a validated `cd` is prepended (the directory is
              shell-quoted; the caller command still owns everything after &&).
              """
              if config.run_working_directory:
                  return f"cd {shlex.quote(config.run_working_directory)} && {config.run_command}"
              return config.run_command


          def build_exec_argv(config: Config, workspace: Path, config_path: Path, id_label: str) -> list[str]:
              """Assemble the `devcontainer exec` argv for the caller's run-command.

              The same --override-config and --id-label as `up` correlate exec to the one
              container started this run. The run-command is passed env-mediated as a single
              argv token to `<run-shell> -c`, never interpolated into a workflow run: block.
              """
              argv = [
                  *cli_invocation(config),
                  "exec",
                  "--workspace-folder",
                  str(workspace),
                  "--override-config",
                  str(config_path),
                  "--id-label",
                  id_label,
              ]
              for entry in config.container_env:
                  argv += ["--remote-env", entry]
              argv += ["--", config.run_shell, "-c", build_exec_command(config)]
              return argv


          def parse_up_result(stdout: str) -> dict:
              """Return the JSON result object from `devcontainer up --log-format json`.

              The result is the last stdout line that parses as a JSON object carrying an
              "outcome" key; any preceding structured log lines are skipped.
              """
              result: dict | None = None
              for line in stdout.splitlines():
                  text = line.strip()
                  if not text.startswith("{"):
                      continue
                  try:
                      parsed = json.loads(text)
                  except json.JSONDecodeError:
                      continue
                  if isinstance(parsed, dict) and "outcome" in parsed:
                      result = parsed
              if result is None:
                  raise SystemExit("devcontainer up did not emit a JSON result object; cannot continue.")
              return result


          def build_cleanup_ids_command(id_label: str) -> list[str]:
              """The `docker ps` argv that lists containers labelled for this run."""
              return ["docker", "ps", "-aq", "--filter", f"label={id_label}"]


          def _truthy(value: str) -> bool:
              return value.strip().lower() in {"1", "true", "yes", "on"}
          DEVFLOWS_SCRIPT_EOF
          mkdir -p "$RUNNER_TEMP/devflows/devcontainer-run"
          cat > "$RUNNER_TEMP/devflows/devcontainer-run/validate-inputs.py" <<'DEVFLOWS_SCRIPT_EOF'
          """Fail-fast input validation for devcontainer-run (validate job).

          Env maps ONLY inputs.* so the expect: validation-failure scenario harness can
          reconstruct this step's environment from the scenario inputs and workflow
          defaults. All validation logic lives in the sibling dcrun module, so the
          validate job and the run job agree exactly on what is legal. The registry-auth
          preflight (which reads a secret-presence expression) lives in the separate
          check-registry-auth.py step, keeping this step inputs-only.
          """

          from __future__ import annotations

          import os
          import sys

          import dcrun


          def main() -> int:
              config = dcrun.parse_and_validate(os.environ, require_workspace=True)
              image = config.image or "(from devcontainer-config-file)"
              config_file = config.config_file or "(none)"
              print(
                  "devcontainer-run inputs are valid: "
                  f"image={image}, config-file={config_file}, shell={config.run_shell}, "
                  f"cli-version={config.cli_version}."
              )
              return 0


          if __name__ == "__main__":
              sys.exit(main())
          DEVFLOWS_SCRIPT_EOF
          mkdir -p "$RUNNER_TEMP/devflows/devcontainer-run"
          cat > "$RUNNER_TEMP/devflows/devcontainer-run/check-registry-auth.py" <<'DEVFLOWS_SCRIPT_EOF'
          """Registry-auth preflight for devcontainer-run (validate job, second step).

          Refuses to fall back to github.token for any registry other than ghcr.io when
          docker-login-enabled and no docker-password secret was provided (verbatim from
          build-devcontainer). This is a separate step from validate-inputs.py because it
          reads DOCKER_PASSWORD_SET (a `secrets.docker-password != ''` expression), which
          the inputs-only validation-failure scenario harness cannot reconstruct.
          """

          from __future__ import annotations

          import os
          import sys

          import dcrun


          def main() -> int:
              dcrun.validate_registry_auth(os.environ)
              print("devcontainer-run registry auth is consistent.")
              return 0


          if __name__ == "__main__":
              sys.exit(main())
          DEVFLOWS_SCRIPT_EOF
          echo "script-root=$RUNNER_TEMP/devflows" >> "$GITHUB_OUTPUT"
      - name: Validate inputs
        shell: bash
        env:
          DEVFLOWS_SCRIPT_ROOT: ${{ steps.devflows-runtime.outputs.script-root }}
          DEVCONTAINER_IMAGE: ${{ inputs.devcontainer-image }}
          DEVCONTAINER_CONFIG_FILE: ${{ inputs.devcontainer-config-file }}
          RUN_COMMAND: ${{ inputs.run-command }}
          RUN_SHELL: ${{ inputs.run-shell }}
          RUN_WORKING_DIRECTORY: ${{ inputs.run-working-directory }}
          REMOTE_USER: ${{ inputs.remote-user }}
          SKIP_POST_CREATE: ${{ inputs.skip-post-create }}
          UPDATE_REMOTE_USER_UID: ${{ inputs.update-remote-user-uid }}
          CONTAINER_ENV: ${{ inputs.container-env }}
          DEVCONTAINER_FEATURES: ${{ inputs.devcontainer-features }}
          DEVCONTAINER_CLI_VERSION: ${{ inputs.devcontainer-cli-version }}
          CACHE_ENABLED: ${{ inputs.cache-enabled }}
          CACHE_PATHS: ${{ inputs.cache-paths }}
          CACHE_KEY: ${{ inputs.cache-key }}
          CACHE_RESTORE_KEYS: ${{ inputs.cache-restore-keys }}
        run: |
          # imports ${DEVFLOWS_SCRIPT_ROOT}/devcontainer-run/dcrun.py
          python "${DEVFLOWS_SCRIPT_ROOT}/devcontainer-run/validate-inputs.py"
      - name: Check registry auth
        shell: bash
        env:
          DEVFLOWS_SCRIPT_ROOT: ${{ steps.devflows-runtime.outputs.script-root }}
          DOCKER_LOGIN_ENABLED: ${{ inputs.docker-login-enabled }}
          DOCKER_PASSWORD_SET: ${{ secrets.docker-password != '' }}
          DOCKER_REGISTRY: ${{ inputs.docker-registry }}
        run: |
          # imports ${DEVFLOWS_SCRIPT_ROOT}/devcontainer-run/dcrun.py
          python "${DEVFLOWS_SCRIPT_ROOT}/devcontainer-run/check-registry-auth.py"
  run:
    name: Run command in devcontainer
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: ${{ inputs.run-timeout-minutes }}
    permissions:
      contents: read
      packages: read
      actions: read
    outputs:
      container-id: ${{ steps.run.outputs.container-id }}
      remote-user: ${{ steps.run.outputs.remote-user }}
    steps:
      - name: Checkout repository
        if: inputs.checkout-enabled
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          repository: ${{ inputs.checkout-repository }}
          ref: ${{ inputs.checkout-ref }}
          token: ${{ secrets.checkout-token || github.token }}
          ssh-key: ${{ secrets.checkout-ssh-key }}
          ssh-known-hosts: ${{ inputs.checkout-ssh-known-hosts }}
          ssh-strict: ${{ inputs.checkout-ssh-strict }}
          ssh-user: ${{ inputs.checkout-ssh-user }}
          persist-credentials: ${{ inputs.checkout-persist-credentials }}
          path: ${{ inputs.checkout-path }}
          clean: ${{ inputs.checkout-clean }}
          filter: ${{ inputs.checkout-filter }}
          sparse-checkout: ${{ inputs.checkout-sparse-checkout }}
          sparse-checkout-cone-mode: ${{ inputs.checkout-sparse-checkout-cone-mode }}
          fetch-depth: ${{ inputs.checkout-fetch-depth }}
          fetch-tags: ${{ inputs.checkout-fetch-tags }}
          show-progress: ${{ inputs.checkout-show-progress }}
          lfs: ${{ inputs.checkout-lfs }}
          submodules: ${{ inputs.checkout-submodules }}
          set-safe-directory: ${{ inputs.checkout-set-safe-directory }}
          github-server-url: ${{ inputs.checkout-github-server-url }}
          allow-unsafe-pr-checkout: ${{ inputs.checkout-allow-unsafe-pr-checkout }}
      - name: Download artifacts
        if: inputs.artifact-download-enabled
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: ${{ inputs.artifact-download-name }}
          artifact-ids: ${{ inputs.artifact-download-ids }}
          path: ${{ inputs.artifact-download-path }}
          pattern: ${{ inputs.artifact-download-pattern }}
          merge-multiple: ${{ inputs.artifact-download-merge-multiple }}
          github-token: ${{ secrets.artifact-download-token }}
          repository: ${{ inputs.artifact-download-repository }}
          run-id: ${{ inputs.artifact-download-run-id }}
          skip-decompress: ${{ inputs.artifact-download-skip-decompress }}
          digest-mismatch: ${{ inputs.artifact-download-digest-mismatch }}
      - name: Materialize DevFlows runtime scripts
        id: devflows-runtime
        shell: bash
        run: |-
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/devflows/devcontainer-run"
          cat > "$RUNNER_TEMP/devflows/devcontainer-run/dcrun.py" <<'DEVFLOWS_SCRIPT_EOF'
          """Shared validation, config-synthesis, and argv helpers for devcontainer-run.

          validate-inputs.py (the fail-fast validate job), check-registry-auth.py (the
          registry-auth preflight), run-devcontainer.py, and cleanup.py all resolve the
          caller inputs through this module so every job agrees exactly on what is legal.

          Every input reaches this module only through os.environ (mapped from inputs.* by
          the workflow), never interpolated into a shell string. run-devcontainer.py builds
          the `devcontainer up`/`devcontainer exec` argv programmatically and calls
          subprocess.run without a shell; the caller's run-command is delivered as a single
          literal argv token to the container shell's -c (the intended, contained place for
          shell semantics), so a `$(...)`, `;`, or space in it is never re-expanded by the
          workflow. This is the whole point of the env-mediated design.
          """

          from __future__ import annotations

          import json
          import os
          import re
          import shlex
          from collections.abc import Mapping
          from dataclasses import dataclass
          from pathlib import Path

          # Syntactic image-reference grammar, identical to paper-openjournals/pandoc: a
          # well-formed [registry/]name[:tag][@digest] reference. Validation is syntactic
          # only; the caller is trusted to supply an image they trust (it runs with the
          # workspace bind-mounted).
          _ALNUM = r"[a-z0-9]+"
          _SEPARATOR = r"(?:[._]|__|[-]+)"
          _PATH_COMPONENT = rf"{_ALNUM}(?:{_SEPARATOR}{_ALNUM})*"
          _NAME = rf"{_PATH_COMPONENT}(?:/{_PATH_COMPONENT})*"
          _DOMAIN_COMPONENT = r"(?:[a-zA-Z0-9]|[a-zA-Z0-9][a-zA-Z0-9-]*[a-zA-Z0-9])"
          _DOMAIN = rf"{_DOMAIN_COMPONENT}(?:\.{_DOMAIN_COMPONENT})*(?::[0-9]+)?"
          _TAG = r"[A-Za-z0-9_][A-Za-z0-9_.-]{0,127}"
          _DIGEST = r"[A-Za-z][A-Za-z0-9]*(?:[-_+.][A-Za-z][A-Za-z0-9]*)*:[0-9A-Fa-f]{32,}"
          _IMAGE_REFERENCE = re.compile(rf"^(?:{_DOMAIN}/)?{_NAME}(?::{_TAG})?(?:@{_DIGEST})?$")

          # run-shell enum: the shell the caller's run-command is executed under.
          SHELLS = ("bash", "sh")
          # --update-remote-user-uid-default enum.
          UID_MODES = ("never", "on", "off")
          # A POSIX-ish environment variable name for container-env keys.
          _ENV_NAME = re.compile(r"^[A-Za-z_][A-Za-z0-9_]*$")
          # ASCII control characters (C0 range plus DEL). container-env/cache entries are
          # split on newlines and then rejected if any control char survives, so an embedded
          # newline/escape/NUL in a single entry is a hard error rather than a silent split.
          _CONTROL = re.compile(r"[\x00-\x1f\x7f]")
          # A remoteUser value: a username or a bare numeric uid. It is written into JSON
          # (never a shell), so this only rejects whitespace/control and shell-looking junk.
          _REMOTE_USER = re.compile(r"^[A-Za-z0-9_][A-Za-z0-9_.-]*$")
          # A pinned @devcontainers/cli npm version. Constraining it here keeps the
          # env-mediated `npm install -g @devcontainers/cli@<version>` install safe.
          _CLI_VERSION = re.compile(r"^[0-9]+\.[0-9]+\.[0-9]+(?:[-+.][0-9A-Za-z.-]+)?$")
          # Keys defensively dropped from a run-secrets bundle: the ephemeral Actions token,
          # which a `toJSON(secrets)` + `secrets: inherit` bundle would otherwise carry into
          # the user command. GitHub exposes it as GITHUB_TOKEN and (in the serialized
          # secrets context) as github_token, so both spellings are stripped.
          _DROPPED_SECRET_KEYS = frozenset({"github_token", "GITHUB_TOKEN"})
          # RUNNER_TEMP subdirectory holding this run's generated override config and the
          # 0600 secret files. run-devcontainer.py writes them here; cleanup.py shreds the
          # secret files from the same place in its always() step.
          _STATE_SUBDIR = "devflows-devcontainer-run"


          @dataclass(frozen=True)
          class Config:
              """A fully validated invocation, shared by every job."""

              image: str  # '' when a devcontainer-config-file supplies the image instead
              config_file: str  # validated workspace-relative path, or ''
              run_command: str
              run_shell: str
              run_working_directory: str  # validated workspace-relative subdir, or ''
              remote_user: str  # '' means "leave the config/label remoteUser untouched"
              skip_post_create: bool
              update_remote_user_uid: str
              container_env: tuple[str, ...]  # validated KEY=VALUE lines
              features: str  # raw JSON object string for --additional-features, or ''
              cli_version: str
              cache_enabled: bool  # whether the actions/cache restore/save step is active
              cache_paths: tuple[str, ...]  # host/workspace paths cached, one per line
              cache_key: str  # exact restore/save key
              cache_restore_keys: tuple[str, ...]  # ordered fallback key prefixes


          def parse_and_validate(env: Mapping[str, str], *, require_workspace: bool = True) -> Config:
              """Validate every input and return the resolved Config.

              Runs in the validate job (before checkout) and in the run job (after
              checkout); neither path touches the filesystem here, so both agree exactly.
              The devcontainer-config-file is only checked lexically (containment); its
              existence and its JSON contents are validated by run-devcontainer.py once the
              checkout is present.
              """
              workspace = _workspace(env, require_workspace)

              run_command = env.get("RUN_COMMAND", "")
              if not run_command.strip():
                  raise SystemExit("run-command is required; it must not be empty.")

              run_shell = env.get("RUN_SHELL", "bash").strip() or "bash"
              if run_shell not in SHELLS:
                  raise SystemExit(f"run-shell must be one of {', '.join(SHELLS)}; got {run_shell!r}.")

              image = _validate_image(env.get("DEVCONTAINER_IMAGE", ""))
              config_file = _validate_config_file(env.get("DEVCONTAINER_CONFIG_FILE", ""), workspace)
              if not image and not config_file:
                  raise SystemExit(
                      "at least one of devcontainer-image or devcontainer-config-file is required "
                      "so the run has an image to start (no image => nothing to run)."
                  )

              working_directory = _validate_working_directory(env.get("RUN_WORKING_DIRECTORY", ""), workspace)
              remote_user = _validate_remote_user(env.get("REMOTE_USER", ""))
              update_uid = env.get("UPDATE_REMOTE_USER_UID", "on").strip() or "on"
              if update_uid not in UID_MODES:
                  raise SystemExit(
                      f"update-remote-user-uid must be one of {', '.join(UID_MODES)}; got {update_uid!r}."
                  )
              container_env = _validate_container_env(env.get("CONTAINER_ENV", ""))
              features = _validate_features(env.get("DEVCONTAINER_FEATURES", ""))
              cli_version = _validate_cli_version(env.get("DEVCONTAINER_CLI_VERSION", ""))
              cache_enabled, cache_paths, cache_key, cache_restore_keys = _validate_cache(env)

              return Config(
                  image=image,
                  config_file=config_file,
                  run_command=run_command,
                  run_shell=run_shell,
                  run_working_directory=working_directory,
                  remote_user=remote_user,
                  skip_post_create=_truthy(env.get("SKIP_POST_CREATE", "")),
                  update_remote_user_uid=update_uid,
                  container_env=container_env,
                  features=features,
                  cli_version=cli_version,
                  cache_enabled=cache_enabled,
                  cache_paths=cache_paths,
                  cache_key=cache_key,
                  cache_restore_keys=cache_restore_keys,
              )


          def _workspace(env: Mapping[str, str], require_workspace: bool) -> Path:
              value = env.get("GITHUB_WORKSPACE")
              if not value:
                  if not require_workspace:
                      return Path.cwd()
                  raise SystemExit(
                      "GITHUB_WORKSPACE is not set; devcontainer-run validation must run in a "
                      "GitHub Actions or act runner environment where it is defined."
                  )
              return Path(value).resolve()


          def _validate_image(value: str) -> str:
              image = value.strip()
              if not image:
                  return ""
              if not _IMAGE_REFERENCE.match(image):
                  raise SystemExit(
                      f"devcontainer-image is not a valid image reference: {value!r}. "
                      "Expected [registry/]name[:tag][@digest]."
                  )
              return image


          def _validate_config_file(value: str, workspace: Path) -> str:
              if not value.strip():
                  return ""
              return _workspace_relative(value, workspace, "devcontainer-config-file")


          def _validate_working_directory(value: str, workspace: Path) -> str:
              if not value.strip():
                  return ""
              return _workspace_relative(value, workspace, "run-working-directory")


          def _validate_remote_user(value: str) -> str:
              user = value.strip()
              if not user:
                  return ""
              if not _REMOTE_USER.match(user):
                  raise SystemExit(
                      f"remote-user must be a plain username or uid: {value!r}. "
                      "Allowed characters are letters, digits, '_', '.', and '-'."
                  )
              return user


          def _validate_container_env(value: str) -> tuple[str, ...]:
              """Validate the non-secret container-env bundle into KEY=VALUE lines.

              Split on newlines only (not the broader str.splitlines set) so an entry that
              smuggles an embedded control character is rejected here rather than silently
              torn into two entries. The key must be a POSIX env name; the value is taken
              literally after the first '=' (so '=' and spaces in a value are fine) and is
              never eval'd -- it is handed to `--remote-env KEY=VALUE` verbatim.
              """
              entries: list[str] = []
              for line in value.split("\n"):
                  item = line.strip()
                  if not item:
                      continue
                  if _CONTROL.search(item):
                      raise SystemExit(
                          f"container-env has a control character in an entry: {line!r}; entries must be "
                          "plain KEY=VALUE lines separated by newlines."
                      )
                  if "=" not in item:
                      raise SystemExit(
                          f"container-env entries must be KEY=VALUE (newline-separated); got {line!r}."
                      )
                  key = item.split("=", 1)[0]
                  if not _ENV_NAME.match(key):
                      raise SystemExit(f"container-env has an invalid environment variable name: {key!r}.")
                  entries.append(item)
              return tuple(entries)


          # --------------------------------------------------------------------------- #
          # run-secrets bundle (a DECLARED workflow_call secret, GitHub-masked)          #
          # --------------------------------------------------------------------------- #
          def parse_run_secrets(raw: str) -> dict[str, str]:
              """Parse the masked run-secrets bundle into a {KEY: VALUE} dict.

              Accepts a JSON object (primary; text starts with ``{``, values must be
              strings) or a ``KEY=VALUE``-per-line form (blank/``#`` lines skipped, split on
              the first ``=``). Each KEY must be a POSIX env name with no control character;
              a VALUE is an opaque literal. github_token / GITHUB_TOKEN are dropped so a
              ``toJSON(secrets)`` + ``inherit`` bundle never injects the Actions token.
              Errors quote only KEY names (never a value), and the JSON error is not chained,
              so no secret material leaks into the log.
              """
              text = raw.strip()
              if not text:
                  return {}
              pairs = _run_secrets_from_json(text) if text.startswith("{") else _run_secrets_from_lines(raw)
              result: dict[str, str] = {}
              for key, value in pairs:
                  if key in _DROPPED_SECRET_KEYS:
                      continue
                  if _CONTROL.search(key):
                      raise SystemExit("run-secrets has a control character in a key name.")
                  if not _ENV_NAME.match(key):
                      raise SystemExit(
                          f"run-secrets has an invalid environment variable name: {key!r}. "
                          "Keys must match [A-Za-z_][A-Za-z0-9_]*."
                      )
                  result[key] = value
              return result


          def _run_secrets_from_json(text: str) -> list[tuple[str, str]]:
              try:
                  parsed = json.loads(text)
              except json.JSONDecodeError:
                  # `from None`: the decoder message can quote secret material; do not chain.
                  raise SystemExit(
                      "run-secrets starts with '{' but is not valid JSON. Provide a JSON object "
                      "of NAME -> value (assembled with per-value toJSON), or the KEY=VALUE form."
                  ) from None
              if not isinstance(parsed, dict):
                  raise SystemExit("run-secrets JSON must be an object of NAME -> value.")
              pairs: list[tuple[str, str]] = []
              for key, value in parsed.items():
                  if not isinstance(value, str):
                      raise SystemExit(
                          f"run-secrets value for {str(key)!r} must be a string; got {type(value).__name__}."
                      )
                  pairs.append((str(key), value))
              return pairs


          def _run_secrets_from_lines(raw: str) -> list[tuple[str, str]]:
              pairs: list[tuple[str, str]] = []
              for line in raw.split("\n"):
                  item = line.strip()
                  if not item or item.startswith("#"):
                      continue
                  if "=" not in item:
                      raise SystemExit(
                          "run-secrets has a KEY=VALUE line with no '='; each non-blank, non-# "
                          "line must be KEY=VALUE."
                      )
                  key, value = item.split("=", 1)
                  pairs.append((key, value))
              return pairs


          def _validate_cache(env: Mapping[str, str]) -> tuple[bool, tuple[str, ...], str, tuple[str, ...]]:
              """Validate the actions/cache inputs (only enforced when cache-enabled).

              cache-paths / cache-key / cache-restore-keys reach the SHA-pinned actions/cache
              step through its `with:` block (never a run: body), so they are injection-safe by
              construction; this still rejects control characters and requires the paths+key
              that make the step meaningful. Paths may be workspace-relative (e.g. .pixi, which
              the container sees through the bind mount) or absolute host paths. When
              cache-enabled is false the values are ignored (the step is `if:`-gated off), so
              validation is skipped entirely.
              """
              enabled = _truthy(env.get("CACHE_ENABLED", ""))
              if not enabled:
                  return False, (), "", ()
              paths = _split_cache_lines(env.get("CACHE_PATHS", ""), "cache-paths")
              if not paths:
                  raise SystemExit(
                      "cache-enabled is true but cache-paths is empty; provide at least one "
                      "newline-separated path to cache (e.g. .pixi)."
                  )
              key = env.get("CACHE_KEY", "").strip()
              if not key:
                  raise SystemExit(
                      "cache-enabled is true but cache-key is empty; provide a non-empty cache-key."
                  )
              if _CONTROL.search(key):
                  raise SystemExit("cache-key must not contain control characters (including newlines).")
              restore_keys = _split_cache_lines(env.get("CACHE_RESTORE_KEYS", ""), "cache-restore-keys")
              return True, paths, key, restore_keys


          def _split_cache_lines(value: str, field: str) -> tuple[str, ...]:
              """Split a newline list into stripped entries, rejecting control characters.

              Splitting on '\\n' only (then rejecting any surviving control char) means an
              embedded newline/escape inside a single path or restore-key is a hard error, not
              a silent extra entry.
              """
              items: list[str] = []
              for line in value.split("\n"):
                  item = line.strip()
                  if not item:
                      continue
                  if _CONTROL.search(item):
                      raise SystemExit(f"{field} has a control character in an entry: {line!r}.")
                  items.append(item)
              return tuple(items)


          def _validate_features(value: str) -> str:
              raw = value.strip()
              if not raw:
                  return ""
              try:
                  parsed = json.loads(raw)
              except json.JSONDecodeError as error:
                  raise SystemExit(f"devcontainer-features must be valid JSON: {error}.") from error
              if not isinstance(parsed, dict):
                  raise SystemExit("devcontainer-features must be a JSON object of feature-id -> options.")
              return raw


          def _validate_cli_version(value: str) -> str:
              version = value.strip()
              if not version:
                  raise SystemExit("devcontainer-cli-version is required (the pinned @devcontainers/cli).")
              if not _CLI_VERSION.match(version):
                  raise SystemExit(
                      f"devcontainer-cli-version must be a valid version (e.g. 0.87.0); got {value!r}."
                  )
              return version


          def _workspace_relative(value: str, workspace: Path, field: str) -> str:
              """Return the workspace-relative POSIX path, rejecting escapes.

              Rejects absolute paths and any '..' component before resolving, then confirms
              the resolved path stays inside GITHUB_WORKSPACE.
              """
              candidate = Path(value)
              if candidate.is_absolute() or ".." in candidate.parts:
                  raise SystemExit(
                      f"{field} must stay inside GITHUB_WORKSPACE (no absolute paths and no '..'): {value}"
                  )
              resolved = (workspace / candidate).resolve()
              if resolved != workspace and workspace not in resolved.parents:
                  raise SystemExit(f"{field} must stay inside GITHUB_WORKSPACE: {value}")
              return resolved.relative_to(workspace).as_posix()


          def validate_registry_auth(env: Mapping[str, str]) -> None:
              """Refuse to send github.token to a registry other than ghcr.io.

              Verbatim from build-devcontainer: the docker-login password falls back to
              github.token only for ghcr.io. Any other registry must supply an explicit
              docker-password secret, otherwise the caller could silently leak github.token
              to a third-party registry. This runs in its own validate-job step because it
              reads a secret-presence expression, which the validation-failure scenario
              harness (inputs-only) cannot reconstruct.
              """
              if not _truthy(env.get("DOCKER_LOGIN_ENABLED", "")):
                  return
              if _truthy(env.get("DOCKER_PASSWORD_SET", "")):
                  return
              registry = env.get("DOCKER_REGISTRY", "").strip().lower()
              if registry != "ghcr.io":
                  target = registry or "docker hub"
                  raise SystemExit(
                      f"docker-login-enabled is true for registry {target!r} but no docker-password "
                      "secret was provided. The github.token login fallback is restricted to ghcr.io; "
                      "pass docker-password to authenticate to any other registry."
                  )


          # --------------------------------------------------------------------------- #
          # Config synthesis + argv (run job)                                            #
          # --------------------------------------------------------------------------- #
          def parse_jsonc(text: str) -> object:
              """Parse a devcontainer.json, tolerating JSONC comments / trailing commas.

              Plain JSON parses directly (the common case and every checked-in fixture);
              only a JSONDecodeError falls back to comment/trailing-comma stripping, so a
              valid document is never mangled.
              """
              try:
                  return json.loads(text)
              except json.JSONDecodeError:
                  return json.loads(_strip_jsonc(text))


          def _strip_jsonc(text: str) -> str:
              out: list[str] = []
              i = 0
              n = len(text)
              in_string = False
              while i < n:
                  char = text[i]
                  if in_string:
                      out.append(char)
                      if char == "\\" and i + 1 < n:
                          out.append(text[i + 1])
                          i += 2
                          continue
                      if char == '"':
                          in_string = False
                      i += 1
                      continue
                  if char == '"':
                      in_string = True
                      out.append(char)
                      i += 1
                      continue
                  if char == "/" and i + 1 < n and text[i + 1] == "/":
                      i += 2
                      while i < n and text[i] not in "\r\n":
                          i += 1
                      continue
                  if char == "/" and i + 1 < n and text[i + 1] == "*":
                      i += 2
                      while i + 1 < n and not (text[i] == "*" and text[i + 1] == "/"):
                          i += 1
                      i += 2
                      continue
                  out.append(char)
                  i += 1
              return re.sub(r",(\s*[}\]])", r"\1", "".join(out))


          def load_caller_config(workspace: Path, config_file: str) -> dict | None:
              """Read and JSON-parse the caller's devcontainer.json, or None when unset."""
              if not config_file:
                  return None
              path = (workspace / config_file).resolve()
              if not path.is_file():
                  raise SystemExit(f"devcontainer-config-file does not exist or is not a file: {config_file}")
              parsed = parse_jsonc(path.read_text(encoding="utf-8"))
              if not isinstance(parsed, dict):
                  raise SystemExit("devcontainer-config-file must contain a JSON object.")
              return parsed


          def synthesize_override_config(config: Config, caller_config: dict | None) -> dict:
              """Build the override devcontainer config written to RUNNER_TEMP.

              When no config-file is given this is a minimal {"image": <ref>} that the
              image's devcontainer.metadata label enriches with features/hooks/user/env.
              When a config-file IS given it is the base, so its features and lifecycle
              hooks are preserved. devcontainer-image (when set) overrides the base image;
              remote-user (when set) forces the exec user. The resolved config MUST carry
              an image so the run never rebuilds from a Dockerfile.
              """
              base: dict = dict(caller_config) if caller_config else {}
              if config.image:
                  base["image"] = config.image
              resolved = base.get("image")
              if not isinstance(resolved, str) or not resolved.strip():
                  raise SystemExit(
                      "no image could be resolved: provide devcontainer-image, or a "
                      'devcontainer-config-file whose JSON sets "image". devcontainer-run '
                      "runs a prebuilt image and never builds from a Dockerfile."
                  )
              if config.remote_user:
                  base["remoteUser"] = config.remote_user
              return base


          def build_exec_override_config(base_override: dict, secrets: Mapping[str, str]) -> dict:
              """The exec-phase override config: the base plus the run-secrets in remoteEnv.

              Secrets go in remoteEnv only for ``exec`` (never ``up``): exec injects them via
              a transient ``docker exec -e``, so they never land in the persisted
              ``devcontainer.metadata`` label an ``up`` remoteEnv would leak. Any caller
              remoteEnv is preserved; a shallow copy leaves the base (for ``up``) unchanged.
              """
              exec_override = dict(base_override)
              existing = exec_override.get("remoteEnv")
              remote_env = dict(existing) if isinstance(existing, dict) else {}
              remote_env.update(secrets)
              exec_override["remoteEnv"] = remote_env
              return exec_override


          def state_dir(runner_temp: Path) -> Path:
              """RUNNER_TEMP subdirectory holding this run's generated override + secret files."""
              return runner_temp / _STATE_SUBDIR


          def secret_file_paths(runner_temp: Path) -> tuple[Path, Path]:
              """The two 0600 secret files (up --secrets-file, exec override); cleanup shreds them."""
              directory = state_dir(runner_temp)
              return directory / "secrets.json", directory / "exec-config.json"


          def write_secret_file(path: Path, data: object) -> None:
              """Serialize ``data`` to a 0600 JSON file, never briefly readable and never echoed."""
              text = json.dumps(data, indent=2) + "\n"
              fd = os.open(path, os.O_WRONLY | os.O_CREAT | os.O_TRUNC, 0o600)
              try:
                  os.write(fd, text.encode("utf-8"))
              finally:
                  os.close(fd)
              os.chmod(path, 0o600)


          def shred_file(path: Path) -> bool:
              """Overwrite with zeros, fsync, then unlink; True if removed, tolerant of absence."""
              try:
                  size = path.stat().st_size
              except FileNotFoundError:
                  return False
              try:
                  fd = os.open(path, os.O_WRONLY)
                  try:
                      os.write(fd, b"\0" * size)
                      os.fsync(fd)
                  finally:
                      os.close(fd)
              except OSError:
                  pass
              try:
                  path.unlink()
              except FileNotFoundError:
                  return False
              return True


          def cli_invocation(config: Config) -> list[str]:
              """The pinned @devcontainers/cli invocation prefix.

              Run ephemerally with npx (the npm peer of uvx), never `npm install -g`: the
              version is pinned, nothing is installed into a global prefix, and it keeps the
              generated workflow free of an ad-hoc package-install step.
              """
              return ["npx", "--yes", f"@devcontainers/cli@{config.cli_version}"]


          def build_up_argv(
              config: Config,
              workspace: Path,
              config_path: Path,
              id_label: str,
              secrets_file: Path | None = None,
          ) -> list[str]:
              """Assemble the `devcontainer up` argv (JSON result, no rebuild).

              When run-secrets carried values, ``secrets_file`` is the 0600 JSON file added
              as ``--secrets-file`` so the creation HOOKS get the secrets (only ``up`` accepts
              the flag; ``exec`` rejects it). Only the PATH lands in the argv, never a value,
              and secrets are kept out of this config's remoteEnv (which ``up`` would bake
              into the persisted devcontainer.metadata label).
              """
              argv = [
                  *cli_invocation(config),
                  "up",
                  "--workspace-folder",
                  str(workspace),
                  "--override-config",
                  str(config_path),
                  "--id-label",
                  id_label,
                  "--log-format",
                  "json",
                  "--update-remote-user-uid-default",
                  config.update_remote_user_uid,
                  "--remove-existing-container",
              ]
              if config.skip_post_create:
                  argv.append("--skip-post-create")
              if config.features:
                  argv += ["--additional-features", config.features]
              for entry in config.container_env:
                  argv += ["--remote-env", entry]
              if secrets_file is not None:
                  argv += ["--secrets-file", str(secrets_file)]
              return argv


          def build_exec_command(config: Config) -> str:
              """The single -c payload the container shell runs.

              With run-working-directory empty (the default) this is the caller's
              run-command verbatim, so it stays one literal argv token. When a working
              directory is set, a validated `cd` is prepended (the directory is
              shell-quoted; the caller command still owns everything after &&).
              """
              if config.run_working_directory:
                  return f"cd {shlex.quote(config.run_working_directory)} && {config.run_command}"
              return config.run_command


          def build_exec_argv(config: Config, workspace: Path, config_path: Path, id_label: str) -> list[str]:
              """Assemble the `devcontainer exec` argv for the caller's run-command.

              The same --override-config and --id-label as `up` correlate exec to the one
              container started this run. The run-command is passed env-mediated as a single
              argv token to `<run-shell> -c`, never interpolated into a workflow run: block.
              """
              argv = [
                  *cli_invocation(config),
                  "exec",
                  "--workspace-folder",
                  str(workspace),
                  "--override-config",
                  str(config_path),
                  "--id-label",
                  id_label,
              ]
              for entry in config.container_env:
                  argv += ["--remote-env", entry]
              argv += ["--", config.run_shell, "-c", build_exec_command(config)]
              return argv


          def parse_up_result(stdout: str) -> dict:
              """Return the JSON result object from `devcontainer up --log-format json`.

              The result is the last stdout line that parses as a JSON object carrying an
              "outcome" key; any preceding structured log lines are skipped.
              """
              result: dict | None = None
              for line in stdout.splitlines():
                  text = line.strip()
                  if not text.startswith("{"):
                      continue
                  try:
                      parsed = json.loads(text)
                  except json.JSONDecodeError:
                      continue
                  if isinstance(parsed, dict) and "outcome" in parsed:
                      result = parsed
              if result is None:
                  raise SystemExit("devcontainer up did not emit a JSON result object; cannot continue.")
              return result


          def build_cleanup_ids_command(id_label: str) -> list[str]:
              """The `docker ps` argv that lists containers labelled for this run."""
              return ["docker", "ps", "-aq", "--filter", f"label={id_label}"]


          def _truthy(value: str) -> bool:
              return value.strip().lower() in {"1", "true", "yes", "on"}
          DEVFLOWS_SCRIPT_EOF
          mkdir -p "$RUNNER_TEMP/devflows/devcontainer-run"
          cat > "$RUNNER_TEMP/devflows/devcontainer-run/run-devcontainer.py" <<'DEVFLOWS_SCRIPT_EOF'
          """Start a prebuilt devcontainer and run the caller's command inside it.

          Modelled on paper-openjournals' run-inara.py: every input arrives through
          os.environ, the `devcontainer up`/`devcontainer exec` argv are assembled
          programmatically (never a shell string), and subprocess.run is called without a
          shell. The flow is:

            1. Resolve the override config (minimal {"image": ref} or the caller's
               devcontainer.json merged with the image/remote-user overrides), written to
               RUNNER_TEMP so the checkout is never mutated. When the caller supplied a
               run-secrets bundle (a declared, GitHub-masked secret), it is parsed here and
               materialized into two 0600 files under RUNNER_TEMP: a secrets.json (the
               `up --secrets-file` shape, delivering secrets to the lifecycle HOOKS) and a
               secrets-bearing exec override-config whose remoteEnv carries the secrets to
               the COMMAND via a transient `docker exec -e` (no persisted label). Secrets
               are never placed in the `up` override-config remoteEnv or in any argv.
            2. `docker pull` the resolved image first for a clean, fast failure when the
               ref is wrong or the registry credentials are missing (the pull otherwise
               rides `docker run` auto-pull deep inside `up`).
            3. `devcontainer up --override-config ... --id-label ... --log-format json`
               (+ --secrets-file when run-secrets was set) against the workspace bind mount
               (no rebuild), parse the JSON result, emit container-id / remote-user
               outputs, and fail loudly on outcome: error.
            4. `devcontainer exec --override-config <exec-config> ... -- <run-shell> -c
               <run-command>` and exit with the command's own exit code (faithful
               propagation: a nonzero command fails the step, so a misconfigured call can
               never silently pass).

          Cleanup (docker rm + shredding the two secret files) is a separate always()
          step, so a container or secret file left behind by a failed `up` is still
          removed.
          """

          from __future__ import annotations

          import json
          import os
          import shlex
          import subprocess
          import sys
          import tempfile
          from pathlib import Path

          import dcrun


          def main() -> int:
              config = dcrun.parse_and_validate(os.environ, require_workspace=True)
              workspace = Path(os.environ["GITHUB_WORKSPACE"]).resolve()
              id_label = os.environ.get("ID_LABEL", "").strip()
              if not id_label:
                  raise SystemExit("ID_LABEL is required; it correlates `up`, `exec`, and cleanup.")

              # RUN_SECRETS is a DECLARED, GitHub-masked workflow_call secret (never an
              # input, so never in the inputs-only validate job). Parse+validate here, in
              # the run step, with no echo/set -x around it; a bad KEY fails the step loudly.
              run_secrets = dcrun.parse_run_secrets(os.environ.get("RUN_SECRETS", ""))

              caller_config = dcrun.load_caller_config(workspace, config.config_file)
              override = dcrun.synthesize_override_config(config, caller_config)

              runner_temp = Path(os.environ.get("RUNNER_TEMP") or tempfile.gettempdir())
              state_dir = dcrun.state_dir(runner_temp)
              state_dir.mkdir(parents=True, exist_ok=True)
              # The `up` override config carries NO secrets in remoteEnv (that would leak
              # them into the persisted devcontainer.metadata label).
              config_path = state_dir / "devcontainer.json"
              config_path.write_text(json.dumps(override, indent=2) + "\n", encoding="utf-8")

              secrets_file: Path | None = None
              exec_config_path = config_path
              if run_secrets:
                  secrets_path, exec_path = dcrun.secret_file_paths(runner_temp)
                  # up --secrets-file shape: {"KEY": "VALUE", ...}, injected into the hooks.
                  dcrun.write_secret_file(secrets_path, run_secrets)
                  secrets_file = secrets_path
                  # exec override-config: base + secrets in remoteEnv, reaching the command
                  # via a transient `docker exec -e` (no persisted label). 0600, cleaned up.
                  dcrun.write_secret_file(exec_path, dcrun.build_exec_override_config(override, run_secrets))
                  exec_config_path = exec_path

              resolved_image = override["image"]
              _pull(resolved_image)

              up_result = _up(config, workspace, config_path, id_label, secrets_file)
              _emit_outputs(up_result)
              outcome = up_result.get("outcome")
              if outcome != "success":
                  message = (
                      up_result.get("message")
                      or up_result.get("description")
                      or "devcontainer up reported a non-success outcome."
                  )
                  raise SystemExit(f"devcontainer up failed ({outcome}): {message}")

              return _exec(config, workspace, exec_config_path, id_label)


          def _pull(image: str) -> None:
              print(f"Pulling image {image} (fail-fast before container setup)...", flush=True)
              completed = subprocess.run(["docker", "pull", image])  # noqa: PLW1510
              if completed.returncode != 0:
                  raise SystemExit(
                      f"Failed to pull image {image!r}. Verify the reference, the runner architecture "
                      "matches a manifest entry, and (for a private image) that docker-login-enabled "
                      "and docker-password / the ghcr.io github.token fallback are set."
                  )


          def _up(
              config: dcrun.Config,
              workspace: Path,
              config_path: Path,
              id_label: str,
              secrets_file: Path | None,
          ) -> dict:
              argv = dcrun.build_up_argv(config, workspace, config_path, id_label, secrets_file)
              # The echoed argv carries only the --secrets-file PATH, never a secret value.
              print("+ " + " ".join(shlex.quote(part) for part in argv), flush=True)
              completed = subprocess.run(argv, stdout=subprocess.PIPE, text=True)  # noqa: PLW1510
              sys.stdout.write(completed.stdout)
              sys.stdout.flush()
              if completed.returncode != 0 and not completed.stdout.strip():
                  raise SystemExit(
                      f"devcontainer up exited {completed.returncode} without a JSON result; cannot continue."
                  )
              return dcrun.parse_up_result(completed.stdout)


          def _exec(config: dcrun.Config, workspace: Path, config_path: Path, id_label: str) -> int:
              argv = dcrun.build_exec_argv(config, workspace, config_path, id_label)
              print("+ " + " ".join(shlex.quote(part) for part in argv), flush=True)
              completed = subprocess.run(argv)  # noqa: PLW1510
              return completed.returncode


          def _emit_outputs(result: dict) -> None:
              output_file = os.environ.get("GITHUB_OUTPUT")
              if not output_file:
                  return
              container_id = str(result.get("containerId") or "")
              remote_user = str(result.get("remoteUser") or "")
              with open(output_file, "a", encoding="utf-8") as handle:
                  handle.write(f"container-id={container_id}\n")
                  handle.write(f"remote-user={remote_user}\n")


          if __name__ == "__main__":
              sys.exit(main())
          DEVFLOWS_SCRIPT_EOF
          mkdir -p "$RUNNER_TEMP/devflows/devcontainer-run"
          cat > "$RUNNER_TEMP/devflows/devcontainer-run/cleanup.py" <<'DEVFLOWS_SCRIPT_EOF'
          """Remove the devcontainer this run started (always() cleanup step).

          Two responsibilities, both under if: always():

            1. Shred the run-secrets files. run-devcontainer.py may have written two 0600
               files under RUNNER_TEMP (the up --secrets-file secrets.json and the
               secrets-bearing exec override-config). They are overwritten and unlinked
               here UNCONDITIONALLY -- before, and independent of, remove-container -- so a
               failed run or remove-container: false never leaves secret material on disk.
            2. Remove the container. There is no `devcontainer down`, so cleanup is by
               Docker: list every container carrying this run's id-label and `docker rm -f`
               each one. Container-written files persist on the host bind mount, so the
               artifact-upload channel that runs after this still sees them.

          Runs even when `up` or the caller command failed, and is tolerant of a missing
          secret file, "none found", and a container already gone.
          """

          from __future__ import annotations

          import os
          import subprocess
          import sys
          import tempfile
          from pathlib import Path

          import dcrun


          def main() -> int:
              _shred_secret_files()

              if not _truthy(os.environ.get("REMOVE_CONTAINER", "")):
                  print("remove-container is false; leaving the container in place.")
                  return 0

              id_label = os.environ.get("ID_LABEL", "").strip()
              if not id_label:
                  print("No id-label provided; nothing to clean up.")
                  return 0

              completed = subprocess.run(  # noqa: PLW1510
                  dcrun.build_cleanup_ids_command(id_label),
                  stdout=subprocess.PIPE,
                  text=True,
              )
              container_ids = [line.strip() for line in completed.stdout.splitlines() if line.strip()]
              if not container_ids:
                  print(f"No containers matched label {id_label!r}; nothing to remove.")
                  return 0

              for container_id in container_ids:
                  removed = subprocess.run(["docker", "rm", "-f", container_id])  # noqa: PLW1510
                  if removed.returncode == 0:
                      print(f"Removed container {container_id}.")
                  else:
                      print(f"Warning: could not remove container {container_id} (already gone?).")
              return 0


          def _shred_secret_files() -> None:
              """Overwrite and unlink the run-secrets files, if run-devcontainer.py wrote them."""
              runner_temp = Path(os.environ.get("RUNNER_TEMP") or tempfile.gettempdir())
              for path in dcrun.secret_file_paths(runner_temp):
                  if dcrun.shred_file(path):
                      print(f"Shredded secret file {path.name}.")


          def _truthy(value: str) -> bool:
              return value.strip().lower() in {"1", "true", "yes", "on"}


          if __name__ == "__main__":
              sys.exit(main())
          DEVFLOWS_SCRIPT_EOF
          echo "script-root=$RUNNER_TEMP/devflows" >> "$GITHUB_OUTPUT"
      - name: Restore workspace cache
        if: ${{ inputs.cache-enabled }}
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        with:
          path: ${{ inputs.cache-paths }}
          key: ${{ inputs.cache-key }}
          restore-keys: ${{ inputs.cache-restore-keys }}
      - name: Docker login
        if: inputs.docker-login-enabled
        uses: docker/login-action@af1e73f918a031802d376d3c8bbc3fe56130a9b0  # v4.4.0
        with:
          registry: ${{ inputs.docker-registry }}
          username: ${{ inputs.docker-username }}
          password: ${{ secrets.docker-password || (inputs.docker-registry == 'ghcr.io' && github.token) || '' }}
      - name: Set up devcontainer CLI
        shell: bash
        env:
          DEVCONTAINER_CLI_VERSION: ${{ inputs.devcontainer-cli-version }}
        run: npx --yes "@devcontainers/cli@${DEVCONTAINER_CLI_VERSION}" --version
      - name: Run command in devcontainer
        id: run
        shell: bash
        env:
          DEVFLOWS_SCRIPT_ROOT: ${{ steps.devflows-runtime.outputs.script-root }}
          ID_LABEL: devflows.run=${{ github.run_id }}-${{ github.run_attempt }}
          DEVCONTAINER_IMAGE: ${{ inputs.devcontainer-image }}
          DEVCONTAINER_CONFIG_FILE: ${{ inputs.devcontainer-config-file }}
          RUN_COMMAND: ${{ inputs.run-command }}
          RUN_SHELL: ${{ inputs.run-shell }}
          RUN_WORKING_DIRECTORY: ${{ inputs.run-working-directory }}
          REMOTE_USER: ${{ inputs.remote-user }}
          SKIP_POST_CREATE: ${{ inputs.skip-post-create }}
          UPDATE_REMOTE_USER_UID: ${{ inputs.update-remote-user-uid }}
          CONTAINER_ENV: ${{ inputs.container-env }}
          DEVCONTAINER_FEATURES: ${{ inputs.devcontainer-features }}
          DEVCONTAINER_CLI_VERSION: ${{ inputs.devcontainer-cli-version }}
          RUN_SECRETS: ${{ secrets.run-secrets }}
        run: |
          # imports ${DEVFLOWS_SCRIPT_ROOT}/devcontainer-run/dcrun.py
          python "${DEVFLOWS_SCRIPT_ROOT}/devcontainer-run/run-devcontainer.py"
      - name: Clean up container
        if: always()
        shell: bash
        env:
          DEVFLOWS_SCRIPT_ROOT: ${{ steps.devflows-runtime.outputs.script-root }}
          ID_LABEL: devflows.run=${{ github.run_id }}-${{ github.run_attempt }}
          REMOVE_CONTAINER: ${{ inputs.remove-container }}
        run: |
          # imports ${DEVFLOWS_SCRIPT_ROOT}/devcontainer-run/dcrun.py
          python "${DEVFLOWS_SCRIPT_ROOT}/devcontainer-run/cleanup.py"
      - name: Upload artifact
        if: inputs.artifact-upload-enabled && inputs.artifact-upload-path != ''
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: ${{ inputs.artifact-upload-name }}
          path: ${{ inputs.artifact-upload-path }}
          if-no-files-found: ${{ inputs.artifact-upload-if-no-files-found }}
          retention-days: ${{ inputs.artifact-upload-retention-days }}
          compression-level: ${{ inputs.artifact-upload-compression-level }}
          overwrite: ${{ inputs.artifact-upload-overwrite }}
          include-hidden-files: ${{ inputs.artifact-upload-include-hidden-files }}
          archive: ${{ inputs.artifact-upload-archive }}

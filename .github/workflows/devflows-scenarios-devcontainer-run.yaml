# GENERATED by devflows test-generate — do not edit; source: workflows/*/devflow.yaml.
# Scenarios whose reusable-workflow call needs a write permission are gated with a
# fork-safe `if:` (and so are the jobs that depend on them): fork pull requests skip
# them because GitHub caps the fork token to read and would fail the nested-permission
# check at startup. Same-repo pull requests, pushes to main, and workflow_dispatch
# still run every scenario.
name: 'DevFlows Hosted Scenarios: Devcontainer Run'
'on':
  pull_request: null
  push:
    branches:
      - main
  workflow_dispatch: null
permissions:
  contents: read
  actions: read
jobs:
  devcontainer_run_hooks_and_exec_call:
    name: 'Devcontainer Run: Pull a public image, run its postCreate hook, and exec a command'
    uses: ./.github/workflows/devcontainer-run.yaml
    with:
      devcontainer-config-file: tests/scenarios/devcontainer-run/hooks/devcontainer.json
      run-shell: sh
      run-command: mkdir -p .devflows-test/devcontainer-run/hooks && test -f /tmp/devflows-postcreate.marker && printf 'hook-ran-and-command-ran\n' > .devflows-test/devcontainer-run/hooks/result.txt
      artifact-upload-enabled: true
      artifact-upload-name: devcontainer-run-hooks
      artifact-upload-path: .devflows-test/devcontainer-run/hooks
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: read
      packages: read
  devcontainer_run_hooks_and_exec_assert:
    name: 'Devcontainer Run: Pull a public image, run its postCreate hook, and exec a command assertions'
    runs-on: ubuntu-latest
    needs: devcontainer_run_hooks_and_exec_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.devcontainer_run_hooks_and_exec_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: devcontainer-run-hooks
          path: .devflows-test-artifacts/devcontainer-run/hooks
      - name: 'Assert file matches glob: result.txt'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/devcontainer-run/hooks/result.txt
          ASSERT_GLOB: '1'
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: result.txt'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/devcontainer-run/hooks/result.txt
          ASSERT_TEXT: hook-ran-and-command-ran
        run: python harness/scenarios/assert-file-contains.py
  devcontainer_run_invalid_image_validate:
    name: 'Devcontainer Run: A malformed devcontainer-image fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/devcontainer-run/scripts/validate-inputs.py
          DEVCONTAINER_IMAGE: alpine:bad:ref
          DEVCONTAINER_CONFIG_FILE: ''
          RUN_COMMAND: echo hi
          RUN_SHELL: bash
          RUN_WORKING_DIRECTORY: ''
          REMOTE_USER: ''
          SKIP_POST_CREATE: 'false'
          UPDATE_REMOTE_USER_UID: 'on'
          CONTAINER_ENV: ''
          DEVCONTAINER_FEATURES: ''
          DEVCONTAINER_CLI_VERSION: 0.87.0
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: valid image reference
        run: python harness/scenarios/assert-validation-failure.py
  devcontainer_run_neither_image_nor_config_validate:
    name: 'Devcontainer Run: Neither devcontainer-image nor devcontainer-config-file fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/devcontainer-run/scripts/validate-inputs.py
          DEVCONTAINER_IMAGE: ''
          DEVCONTAINER_CONFIG_FILE: ''
          RUN_COMMAND: echo hi
          RUN_SHELL: bash
          RUN_WORKING_DIRECTORY: ''
          REMOTE_USER: ''
          SKIP_POST_CREATE: 'false'
          UPDATE_REMOTE_USER_UID: 'on'
          CONTAINER_ENV: ''
          DEVCONTAINER_FEATURES: ''
          DEVCONTAINER_CLI_VERSION: 0.87.0
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: at least one of devcontainer-image or devcontainer-config-file
        run: python harness/scenarios/assert-validation-failure.py
  devcontainer_run_invalid_run_shell_validate:
    name: 'Devcontainer Run: An unsupported run-shell fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/devcontainer-run/scripts/validate-inputs.py
          DEVCONTAINER_IMAGE: alpine:3.20
          DEVCONTAINER_CONFIG_FILE: ''
          RUN_COMMAND: echo hi
          RUN_SHELL: zsh
          RUN_WORKING_DIRECTORY: ''
          REMOTE_USER: ''
          SKIP_POST_CREATE: 'false'
          UPDATE_REMOTE_USER_UID: 'on'
          CONTAINER_ENV: ''
          DEVCONTAINER_FEATURES: ''
          DEVCONTAINER_CLI_VERSION: 0.87.0
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: run-shell must be one of
        run: python harness/scenarios/assert-validation-failure.py
  devcontainer_run_unsafe_config_file_validate:
    name: 'Devcontainer Run: A devcontainer-config-file that escapes the workspace fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/devcontainer-run/scripts/validate-inputs.py
          DEVCONTAINER_IMAGE: ''
          DEVCONTAINER_CONFIG_FILE: ../../../etc/passwd
          RUN_COMMAND: echo hi
          RUN_SHELL: bash
          RUN_WORKING_DIRECTORY: ''
          REMOTE_USER: ''
          SKIP_POST_CREATE: 'false'
          UPDATE_REMOTE_USER_UID: 'on'
          CONTAINER_ENV: ''
          DEVCONTAINER_FEATURES: ''
          DEVCONTAINER_CLI_VERSION: 0.87.0
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: must stay inside
        run: python harness/scenarios/assert-validation-failure.py
  devcontainer_run_invalid_features_json_validate:
    name: 'Devcontainer Run: Non-JSON devcontainer-features fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/devcontainer-run/scripts/validate-inputs.py
          DEVCONTAINER_IMAGE: alpine:3.20
          DEVCONTAINER_CONFIG_FILE: ''
          RUN_COMMAND: echo hi
          RUN_SHELL: bash
          RUN_WORKING_DIRECTORY: ''
          REMOTE_USER: ''
          SKIP_POST_CREATE: 'false'
          UPDATE_REMOTE_USER_UID: 'on'
          CONTAINER_ENV: ''
          DEVCONTAINER_FEATURES: '{not json'
          DEVCONTAINER_CLI_VERSION: 0.87.0
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: devcontainer-features must be valid JSON
        run: python harness/scenarios/assert-validation-failure.py
  devcontainer_run_empty_run_command_validate:
    name: 'Devcontainer Run: An empty run-command fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/devcontainer-run/scripts/validate-inputs.py
          DEVCONTAINER_IMAGE: alpine:3.20
          DEVCONTAINER_CONFIG_FILE: ''
          RUN_COMMAND: ''
          RUN_SHELL: bash
          RUN_WORKING_DIRECTORY: ''
          REMOTE_USER: ''
          SKIP_POST_CREATE: 'false'
          UPDATE_REMOTE_USER_UID: 'on'
          CONTAINER_ENV: ''
          DEVCONTAINER_FEATURES: ''
          DEVCONTAINER_CLI_VERSION: 0.87.0
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: run-command is required
        run: python harness/scenarios/assert-validation-failure.py

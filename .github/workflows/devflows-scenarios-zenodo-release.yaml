# GENERATED by devflows test-generate — do not edit; source: workflows/*/devflow.yaml.
# Scenarios whose reusable-workflow call needs a write permission are gated with a
# fork-safe `if:` (and so are the jobs that depend on them): fork pull requests skip
# them because GitHub caps the fork token to read and would fail the nested-permission
# check at startup. Same-repo pull requests, pushes to main, and workflow_dispatch
# still run every scenario.
name: 'DevFlows Hosted Scenarios: Zenodo Release'
'on':
  pull_request: null
  push:
    branches:
      - main
  workflow_dispatch: null
permissions:
  contents: read
  actions: read
jobs:
  zenodo_release_dry_run_explicit_setup:
    name: 'Zenodo Release: Dry-run with explicit metadata verifies globs and digests without a token setup'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Create setup files
        shell: bash
        env:
          DEVFLOWS_SETUP_FILES: |-
            [
              {
                "path": ".devflows-test/zenodo-release/assets/zenodo_release_fixture-0.1.0.tar.gz",
                "content": "zenodo-release sdist fixture\n"
              },
              {
                "path": ".devflows-test/zenodo-release/assets/paper.pdf",
                "content": "zenodo-release paper fixture\n"
              }
            ]
        run: python harness/scenarios/create-setup-files.py
      - name: Upload setup artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: zenodo-release-dry-assets
          path: .devflows-test/zenodo-release/assets
          if-no-files-found: error
          retention-days: 1
          overwrite: true
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  zenodo_release_dry_run_explicit_call:
    name: 'Zenodo Release: Dry-run with explicit metadata verifies globs and digests without a token'
    uses: ./.github/workflows/zenodo-release.yaml
    with:
      release-tag: v0.1.0
      release-enabled: true
      release-asset-globs: '*.tar.gz'
      zenodo-enabled: true
      zenodo-environment-name: zenodo
      zenodo-title: Zenodo Release Fixture
      zenodo-creators: Doe, Jane | DevFlows | 0000-0002-1825-0097
      zenodo-description: A dry-run fixture deposition.
      zenodo-asset-globs: |
        *.tar.gz
        *.pdf
      publish-dry-run-enabled: true
      publish-dist-manifest: '{"schema":1,"files":[{"name":"zenodo_release_fixture-0.1.0.tar.gz","sha256":"cb6fb5c419a852a3f126b9676d43952b46f1ecc0837597227bd9577173d77662","size":29,"kind":"sdist"},{"name":"paper.pdf","sha256":"ac68639147e871041ae2dfd1fd252ee7579bf0161f643b82929ace51b520006c","size":29,"kind":"other"}]}'
      checkout-enabled: false
      artifact-download-enabled: true
      artifact-download-name: zenodo-release-dry-assets
      artifact-download-path: download-assets
    permissions:
      actions: read
      contents: write
      discussions: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    needs: zenodo_release_dry_run_explicit_setup
  zenodo_release_dry_run_explicit_assert:
    name: 'Zenodo Release: Dry-run with explicit metadata verifies globs and digests without a token assertions'
    runs-on: ubuntu-latest
    needs: zenodo_release_dry_run_explicit_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.zenodo_release_dry_run_explicit_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Assert output package-version
        shell: bash
        env:
          ASSERT_NAME: package-version
          EXPECTED: 0.1.0
          ACTUAL: ${{ needs.zenodo_release_dry_run_explicit_call.outputs.package-version }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output release-tag
        shell: bash
        env:
          ASSERT_NAME: release-tag
          EXPECTED: v0.1.0
          ACTUAL: ${{ needs.zenodo_release_dry_run_explicit_call.outputs.release-tag }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output release-url
        shell: bash
        env:
          ASSERT_NAME: release-url
          EXPECTED: ''
          ACTUAL: ${{ needs.zenodo_release_dry_run_explicit_call.outputs.release-url }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output zenodo-doi
        shell: bash
        env:
          ASSERT_NAME: zenodo-doi
          EXPECTED: ''
          ACTUAL: ${{ needs.zenodo_release_dry_run_explicit_call.outputs.zenodo-doi }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output zenodo-state
        shell: bash
        env:
          ASSERT_NAME: zenodo-state
          EXPECTED: ''
          ACTUAL: ${{ needs.zenodo_release_dry_run_explicit_call.outputs.zenodo-state }}
        run: python harness/scenarios/assert-equals.py
  zenodo_release_dry_run_cff_setup:
    name: 'Zenodo Release: Dry-run derives Zenodo metadata from CITATION.cff without a token setup'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Create setup files
        shell: bash
        env:
          DEVFLOWS_SETUP_FILES: |-
            [
              {
                "path": ".devflows-test/zenodo-release/cff-assets/zenodo_release_fixture-0.1.0.tar.gz",
                "content": "zenodo-release sdist fixture\n"
              }
            ]
        run: python harness/scenarios/create-setup-files.py
      - name: Upload setup artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: zenodo-release-cff-assets
          path: .devflows-test/zenodo-release/cff-assets
          if-no-files-found: error
          retention-days: 1
          overwrite: true
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  zenodo_release_dry_run_cff_call:
    name: 'Zenodo Release: Dry-run derives Zenodo metadata from CITATION.cff without a token'
    uses: ./.github/workflows/zenodo-release.yaml
    with:
      release-tag: v0.1.0
      release-enabled: false
      zenodo-enabled: true
      zenodo-environment-name: zenodo
      zenodo-metadata-cff-path: tests/scenarios/zenodo-release/CITATION.cff
      zenodo-asset-globs: '*.tar.gz'
      publish-dry-run-enabled: true
      checkout-enabled: true
      artifact-download-enabled: true
      artifact-download-name: zenodo-release-cff-assets
      artifact-download-path: download-assets
    permissions:
      actions: read
      contents: write
      discussions: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    needs: zenodo_release_dry_run_cff_setup
  zenodo_release_dry_run_cff_assert:
    name: 'Zenodo Release: Dry-run derives Zenodo metadata from CITATION.cff without a token assertions'
    runs-on: ubuntu-latest
    needs: zenodo_release_dry_run_cff_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.zenodo_release_dry_run_cff_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Assert output package-version
        shell: bash
        env:
          ASSERT_NAME: package-version
          EXPECTED: 0.1.0
          ACTUAL: ${{ needs.zenodo_release_dry_run_cff_call.outputs.package-version }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output zenodo-doi
        shell: bash
        env:
          ASSERT_NAME: zenodo-doi
          EXPECTED: ''
          ACTUAL: ${{ needs.zenodo_release_dry_run_cff_call.outputs.zenodo-doi }}
        run: python harness/scenarios/assert-equals.py
  zenodo_release_nothing_to_do_validate:
    name: 'Zenodo Release: Neither target enabled fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/zenodo-release/scripts/validate-inputs.py
          PUBLISH_DRY_RUN_ENABLED: 'false'
          PUBLISH_TIMEOUT_MINUTES: '15'
          RELEASE_ENABLED: 'false'
          RELEASE_TAG: v1.0.0
          RELEASE_NOTES: ''
          RELEASE_NOTES_FILE: ''
          RELEASE_DRAFT_ENABLED: 'false'
          RELEASE_DISCUSSION_CATEGORY: ''
          RELEASE_EXISTING_MODE: fail
          RELEASE_ASSET_GLOBS: ''
          RELEASE_ASSET_SOURCE_PATH: ''
          RELEASE_ASSET_IF_NO_FILES_FOUND: error
          RELEASE_ENVIRONMENT_NAME: ''
          ZENODO_ENABLED: 'false'
          ZENODO_SANDBOX_ENABLED: 'false'
          ZENODO_PUBLISH_ENABLED: 'false'
          ZENODO_PUBLISH_CONFIRM: ''
          ZENODO_NEW_VERSION_FILE_MODE: replace
          ZENODO_ENVIRONMENT_NAME: ''
          ZENODO_UPLOAD_TYPE: software
          ZENODO_TITLE: ''
          ZENODO_CREATORS: ''
          ZENODO_DESCRIPTION: ''
          ZENODO_METADATA_CFF_PATH: ''
          ZENODO_METADATA_EXTRA: ''
          ZENODO_ASSET_GLOBS: ''
          ZENODO_ASSET_SOURCE_PATH: ''
          ZENODO_ASSET_IF_NO_FILES_FOUND: error
          PUBLISH_DIST_MANIFEST: ''
          ARTIFACT_DOWNLOAD_ENABLED: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: Nothing to do
        run: python harness/scenarios/assert-validation-failure.py
  zenodo_release_publish_confirm_mismatch_validate:
    name: 'Zenodo Release: A real publish without a matching confirm fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/zenodo-release/scripts/validate-inputs.py
          PUBLISH_DRY_RUN_ENABLED: 'false'
          PUBLISH_TIMEOUT_MINUTES: '15'
          RELEASE_ENABLED: 'false'
          RELEASE_TAG: v1.0.0
          RELEASE_NOTES: ''
          RELEASE_NOTES_FILE: ''
          RELEASE_DRAFT_ENABLED: 'false'
          RELEASE_DISCUSSION_CATEGORY: ''
          RELEASE_EXISTING_MODE: fail
          RELEASE_ASSET_GLOBS: ''
          RELEASE_ASSET_SOURCE_PATH: ''
          RELEASE_ASSET_IF_NO_FILES_FOUND: error
          RELEASE_ENVIRONMENT_NAME: ''
          ZENODO_ENABLED: 'true'
          ZENODO_SANDBOX_ENABLED: 'false'
          ZENODO_PUBLISH_ENABLED: 'true'
          ZENODO_PUBLISH_CONFIRM: v9.9.9
          ZENODO_NEW_VERSION_FILE_MODE: replace
          ZENODO_ENVIRONMENT_NAME: zenodo
          ZENODO_UPLOAD_TYPE: software
          ZENODO_TITLE: Fixture
          ZENODO_CREATORS: Doe, Jane
          ZENODO_DESCRIPTION: A fixture deposition.
          ZENODO_METADATA_CFF_PATH: ''
          ZENODO_METADATA_EXTRA: ''
          ZENODO_ASSET_GLOBS: '*.tar.gz'
          ZENODO_ASSET_SOURCE_PATH: ''
          ZENODO_ASSET_IF_NO_FILES_FOUND: error
          PUBLISH_DIST_MANIFEST: ''
          ARTIFACT_DOWNLOAD_ENABLED: 'true'
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: zenodo-publish-confirm must equal release-tag
        run: python harness/scenarios/assert-validation-failure.py
  zenodo_release_metadata_extra_owned_key_validate:
    name: 'Zenodo Release: An owned key in zenodo-metadata-extra fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/zenodo-release/scripts/validate-inputs.py
          PUBLISH_DRY_RUN_ENABLED: 'false'
          PUBLISH_TIMEOUT_MINUTES: '15'
          RELEASE_ENABLED: 'true'
          RELEASE_TAG: v1.0.0
          RELEASE_NOTES: ''
          RELEASE_NOTES_FILE: ''
          RELEASE_DRAFT_ENABLED: 'false'
          RELEASE_DISCUSSION_CATEGORY: ''
          RELEASE_EXISTING_MODE: fail
          RELEASE_ASSET_GLOBS: ''
          RELEASE_ASSET_SOURCE_PATH: ''
          RELEASE_ASSET_IF_NO_FILES_FOUND: error
          RELEASE_ENVIRONMENT_NAME: ''
          ZENODO_ENABLED: 'true'
          ZENODO_SANDBOX_ENABLED: 'false'
          ZENODO_PUBLISH_ENABLED: 'false'
          ZENODO_PUBLISH_CONFIRM: ''
          ZENODO_NEW_VERSION_FILE_MODE: replace
          ZENODO_ENVIRONMENT_NAME: zenodo
          ZENODO_UPLOAD_TYPE: software
          ZENODO_TITLE: ''
          ZENODO_CREATORS: ''
          ZENODO_DESCRIPTION: ''
          ZENODO_METADATA_CFF_PATH: ''
          ZENODO_METADATA_EXTRA: '{"doi": "10.5281/zenodo.1"}'
          ZENODO_ASSET_GLOBS: '*.tar.gz'
          ZENODO_ASSET_SOURCE_PATH: ''
          ZENODO_ASSET_IF_NO_FILES_FOUND: error
          PUBLISH_DIST_MANIFEST: ''
          ARTIFACT_DOWNLOAD_ENABLED: 'true'
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: owned
        run: python harness/scenarios/assert-validation-failure.py

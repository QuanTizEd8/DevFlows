# GENERATED by devflows test-generate — do not edit; source: workflows/*/devflow.yaml.
# Scenarios whose reusable-workflow call needs a write permission are gated with a
# fork-safe `if:` (and so are the jobs that depend on them): fork pull requests skip
# them because GitHub caps the fork token to read and would fail the nested-permission
# check at startup. Same-repo pull requests, pushes to main, and workflow_dispatch
# still run every scenario.
name: DevFlows Hosted Scenario Tests
'on':
  pull_request: null
  push:
    branches:
      - main
  workflow_dispatch: null
permissions:
  contents: read
  actions: read
jobs:
  build_devcontainer_build_only_minimal_call:
    name: 'Build Devcontainer: Build a minimal devcontainer without pushing'
    uses: ./.github/workflows/build-devcontainer.yaml
    with:
      image-name: devflows-e2e-devcontainer
      image-tag: e2e
      devcontainer-config-file: tests/scenarios/build-devcontainer/minimal/devcontainer.json
      docker-login-enabled: false
      devcontainer-push: never
      devcontainer-cache-registry-enabled: false
      merge-enabled: false
      cache-enabled: false
      build-digest-artifact-name: build-only-digest
    permissions:
      actions: read
      contents: read
      packages: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  build_devcontainer_build_only_minimal_assert:
    name: 'Build Devcontainer: Build a minimal devcontainer without pushing assertions'
    runs-on: ubuntu-latest
    needs: build_devcontainer_build_only_minimal_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.build_devcontainer_build_only_minimal_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: build-only-digest-linux-amd64
          path: .devflows-test-artifacts/build-devcontainer/build-only
      - name: Assert output image-ref
        shell: bash
        env:
          ASSERT_NAME: image-ref
          EXPECTED: ''
          ACTUAL: ${{ needs.build_devcontainer_build_only_minimal_call.outputs.image-ref }}
        run: python harness/scenarios/assert-equals.py
      - name: 'Assert file contains: linux-amd64'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/build-devcontainer/build-only/linux-amd64
          ASSERT_TEXT: 'sha256:'
        run: python harness/scenarios/assert-file-contains.py
  deploy_pages_package_only_call:
    name: 'Deploy Pages: Package a site directory into a Pages artifact without deploying'
    uses: ./.github/workflows/deploy-pages.yaml
    with:
      pages-path: tests/scenarios/deploy-pages/site
      pages-artifact-enabled: true
      pages-deploy-enabled: false
      pages-artifact-name: devflows-deploy-pages-package-only
    permissions:
      actions: read
      contents: read
      id-token: write
      pages: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  deploy_pages_package_only_assert:
    name: 'Deploy Pages: Package a site directory into a Pages artifact without deploying assertions'
    runs-on: ubuntu-latest
    needs: deploy_pages_package_only_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.deploy_pages_package_only_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: devflows-deploy-pages-package-only
          path: .devflows-test-artifacts/deploy-pages/package-only
      - name: Assert output pages-url
        shell: bash
        env:
          ASSERT_NAME: pages-url
          EXPECTED: ''
          ACTUAL: ${{ needs.deploy_pages_package_only_call.outputs.pages-url }}
        run: python harness/scenarios/assert-equals.py
      - name: 'Assert file exists: artifact.tar'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/deploy-pages/package-only/artifact.tar
        run: python harness/scenarios/assert-file-exists.py
  deploy_pages_package_from_artifact_setup:
    name: 'Deploy Pages: Package a downloaded site artifact into a Pages artifact setup'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Create setup files
        shell: bash
        env:
          DEVFLOWS_SETUP_FILES: |-
            [
              {
                "path": ".devflows-test/deploy-pages/from-artifact-input/index.html",
                "content": "<!doctype html>\n<html lang=\"en\">\n  <head>\n    <meta charset=\"utf-8\" />\n    <title>DevFlows deploy-pages chained site</title>\n  </head>\n  <body>\n    <h1>Chained from a downloaded artifact</h1>\n  </body>\n</html>\n"
              }
            ]
        run: python harness/scenarios/create-setup-files.py
      - name: Upload setup artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: devflows-deploy-pages-input-site
          path: .devflows-test/deploy-pages/from-artifact-input
          if-no-files-found: error
          retention-days: 1
          overwrite: true
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  deploy_pages_package_from_artifact_call:
    name: 'Deploy Pages: Package a downloaded site artifact into a Pages artifact'
    uses: ./.github/workflows/deploy-pages.yaml
    with:
      checkout-enabled: false
      artifact-download-enabled: true
      artifact-download-name: devflows-deploy-pages-input-site
      artifact-download-path: site-input
      pages-path: site-input
      pages-artifact-enabled: true
      pages-deploy-enabled: false
      pages-artifact-name: devflows-deploy-pages-from-artifact
    permissions:
      actions: read
      contents: read
      id-token: write
      pages: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    needs: deploy_pages_package_from_artifact_setup
  deploy_pages_package_from_artifact_assert:
    name: 'Deploy Pages: Package a downloaded site artifact into a Pages artifact assertions'
    runs-on: ubuntu-latest
    needs: deploy_pages_package_from_artifact_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.deploy_pages_package_from_artifact_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: devflows-deploy-pages-from-artifact
          path: .devflows-test-artifacts/deploy-pages/from-artifact
      - name: 'Assert file exists: artifact.tar'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/deploy-pages/from-artifact/artifact.tar
        run: python harness/scenarios/assert-file-exists.py
  deploy_pages_pages_path_missing_validate:
    name: 'Deploy Pages: Packaging enabled without a pages-path fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/deploy-pages/scripts/validate-inputs.py
          PAGES_PATH: ''
          PAGES_ARTIFACT_ENABLED: 'true'
          PAGES_DEPLOY_ENABLED: 'true'
          PAGES_ARTIFACT_NAME: github-pages
          PAGES_TIMEOUT_MINUTES: '10'
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: pages-path is required when pages-artifact-enabled is true
        run: python harness/scenarios/assert-validation-failure.py
  deploy_pages_nothing_to_do_validate:
    name: 'Deploy Pages: Neither packaging nor deploying fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/deploy-pages/scripts/validate-inputs.py
          PAGES_PATH: ''
          PAGES_ARTIFACT_ENABLED: 'false'
          PAGES_DEPLOY_ENABLED: 'false'
          PAGES_ARTIFACT_NAME: github-pages
          PAGES_TIMEOUT_MINUTES: '10'
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: Nothing to do
        run: python harness/scenarios/assert-validation-failure.py
  deploy_pages_deploy_only_with_path_validate:
    name: 'Deploy Pages: Deploy-only mode with a pages-path fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/deploy-pages/scripts/validate-inputs.py
          PAGES_PATH: site
          PAGES_ARTIFACT_ENABLED: 'false'
          PAGES_DEPLOY_ENABLED: 'true'
          PAGES_ARTIFACT_NAME: github-pages
          PAGES_TIMEOUT_MINUTES: '10'
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: pages-path must be empty when pages-artifact-enabled is false
        run: python harness/scenarios/assert-validation-failure.py
  docs_build_sphinx_container_artifact_call:
    name: 'Build Documentation: Sphinx build in a container image with artifact upload'
    uses: ./.github/workflows/docs-build.yaml
    with:
      docs-tool: sphinx
      docs-environment: container
      container-image: sphinxdoc/sphinx:8.1.3
      sphinx-source-directory: tests/scenarios/docs-build/sphinx-site
      docs-output-directory: .devflows-test/docs-build/sphinx-container-artifact/site
      artifact-upload-enabled: true
      artifact-upload-name: docs-build-sphinx-container
      artifact-upload-path: .devflows-test/docs-build/sphinx-container-artifact/site
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: read
  docs_build_sphinx_container_artifact_assert:
    name: 'Build Documentation: Sphinx build in a container image with artifact upload assertions'
    runs-on: ubuntu-latest
    needs: docs_build_sphinx_container_artifact_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.docs_build_sphinx_container_artifact_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: docs-build-sphinx-container
          path: .devflows-test-artifacts/docs-build/sphinx-container
      - name: 'Assert file exists: index.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/docs-build/sphinx-container/index.html
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: index.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/docs-build/sphinx-container/index.html
          ASSERT_TEXT: DevFlows Docs Build Fixture
        run: python harness/scenarios/assert-file-contains.py
  docs_build_mkdocs_uv_artifact_call:
    name: 'Build Documentation: MkDocs build in a uv environment with artifact upload'
    uses: ./.github/workflows/docs-build.yaml
    with:
      docs-tool: mkdocs
      docs-environment: uv
      docs-working-directory: tests/scenarios/docs-build/mkdocs-uv
      uv-sync-groups: |
        docs
      uv-sync-locked: false
      mkdocs-config-file: tests/scenarios/docs-build/mkdocs-uv/mkdocs.yml
      docs-output-directory: .devflows-test/docs-build/mkdocs-uv/site
      artifact-upload-enabled: true
      artifact-upload-name: docs-build-mkdocs-uv
      artifact-upload-path: .devflows-test/docs-build/mkdocs-uv/site
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: read
  docs_build_mkdocs_uv_artifact_assert:
    name: 'Build Documentation: MkDocs build in a uv environment with artifact upload assertions'
    runs-on: ubuntu-latest
    needs: docs_build_mkdocs_uv_artifact_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.docs_build_mkdocs_uv_artifact_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: docs-build-mkdocs-uv
          path: .devflows-test-artifacts/docs-build/mkdocs-uv
      - name: 'Assert file exists: index.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/docs-build/mkdocs-uv/index.html
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: index.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/docs-build/mkdocs-uv/index.html
          ASSERT_TEXT: DevFlows Docs Build MkDocs Fixture
        run: python harness/scenarios/assert-file-contains.py
  docs_build_sphinx_micromamba_artifact_call:
    name: 'Build Documentation: Sphinx build in a micromamba environment with artifact upload'
    uses: ./.github/workflows/docs-build.yaml
    with:
      docs-tool: sphinx
      docs-environment: micromamba
      micromamba-environment-file: tests/scenarios/docs-build/sphinx-micromamba/environment.yml
      sphinx-source-directory: tests/scenarios/docs-build/sphinx-site
      docs-output-directory: .devflows-test/docs-build/sphinx-micromamba/site
      artifact-upload-enabled: true
      artifact-upload-name: docs-build-sphinx-micromamba
      artifact-upload-path: .devflows-test/docs-build/sphinx-micromamba/site
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: read
  docs_build_sphinx_micromamba_artifact_assert:
    name: 'Build Documentation: Sphinx build in a micromamba environment with artifact upload assertions'
    runs-on: ubuntu-latest
    needs: docs_build_sphinx_micromamba_artifact_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.docs_build_sphinx_micromamba_artifact_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: docs-build-sphinx-micromamba
          path: .devflows-test-artifacts/docs-build/sphinx-micromamba
      - name: 'Assert file exists: index.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/docs-build/sphinx-micromamba/index.html
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: index.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/docs-build/sphinx-micromamba/index.html
          ASSERT_TEXT: DevFlows Docs Build Fixture
        run: python harness/scenarios/assert-file-contains.py
  docs_build_pages_artifact_chain_call:
    name: 'Build Documentation: Pages artifact upload exposes the deploy-pages handoff outputs'
    uses: ./.github/workflows/docs-build.yaml
    with:
      docs-tool: sphinx
      docs-environment: pip
      pip-install-targets: |
        sphinx
      sphinx-source-directory: tests/scenarios/docs-build/sphinx-site
      docs-output-directory: .devflows-test/docs-build/pages-chain/site
      pages-artifact-enabled: true
      pages-artifact-name: docs-build-pages-chain
    permissions:
      actions: read
      contents: read
  docs_build_pages_artifact_chain_assert:
    name: 'Build Documentation: Pages artifact upload exposes the deploy-pages handoff outputs assertions'
    runs-on: ubuntu-latest
    needs: docs_build_pages_artifact_chain_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.docs_build_pages_artifact_chain_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Assert output pages-artifact-name
        shell: bash
        env:
          ASSERT_NAME: pages-artifact-name
          EXPECTED: docs-build-pages-chain
          ACTUAL: ${{ needs.docs_build_pages_artifact_chain_call.outputs.pages-artifact-name }}
        run: python harness/scenarios/assert-equals.py
  docs_build_linkcheck_mkdocs_invalid_validate:
    name: 'Build Documentation: Link check requested with MkDocs fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/docs-build/scripts/validate-inputs.py
          DOCS_TOOL: mkdocs
          DOCS_ENVIRONMENT: uv
          DOCS_BUILD_COMMAND: ''
          DOCS_WORKING_DIRECTORY: .
          DOCS_OUTPUT_DIRECTORY: _site
          DOCS_WARNINGS_AS_ERRORS: 'false'
          DOCS_LINKCHECK_ENABLED: 'true'
          SPHINX_BUILDER: html
          SPHINX_ARGUMENTS: ''
          MKDOCS_ARGUMENTS: ''
          UV_CACHE_MODE: auto
          PIP_REQUIREMENTS_FILE: ''
          PIP_INSTALL_TARGETS: ''
          PIXI_LOCKED: 'true'
          PIXI_FROZEN: 'false'
          MICROMAMBA_ENVIRONMENT_FILE: ''
          CONTAINER_IMAGE: ''
          PAGES_ARTIFACT_ENABLED: 'false'
          PAGES_ARTIFACT_NAME: github-pages
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: docs-linkcheck-enabled requires docs-tool sphinx
        run: python harness/scenarios/assert-validation-failure.py
  docs_build_container_missing_image_validate:
    name: 'Build Documentation: Container mode without an image fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/docs-build/scripts/validate-inputs.py
          DOCS_TOOL: sphinx
          DOCS_ENVIRONMENT: container
          DOCS_BUILD_COMMAND: ''
          DOCS_WORKING_DIRECTORY: .
          DOCS_OUTPUT_DIRECTORY: _site
          DOCS_WARNINGS_AS_ERRORS: 'false'
          DOCS_LINKCHECK_ENABLED: 'false'
          SPHINX_BUILDER: html
          SPHINX_ARGUMENTS: ''
          MKDOCS_ARGUMENTS: ''
          UV_CACHE_MODE: auto
          PIP_REQUIREMENTS_FILE: ''
          PIP_INSTALL_TARGETS: ''
          PIXI_LOCKED: 'true'
          PIXI_FROZEN: 'false'
          MICROMAMBA_ENVIRONMENT_FILE: ''
          CONTAINER_IMAGE: ''
          PAGES_ARTIFACT_ENABLED: 'false'
          PAGES_ARTIFACT_NAME: github-pages
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: container mode requires container-image
        run: python harness/scenarios/assert-validation-failure.py
  pandoc_markdown_html_artifact_call:
    name: 'Pandoc: Markdown to HTML with artifact upload'
    uses: ./.github/workflows/pandoc.yaml
    with:
      pandoc-image: pandoc/core:3.8
      pandoc-arguments: --standalone --output=.devflows-test/pandoc/markdown-html-artifact/example.html README.md
      artifact-upload-enabled: true
      artifact-upload-name: pandoc-markdown-html
      artifact-upload-path: .devflows-test/pandoc/markdown-html-artifact/example.html
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  pandoc_markdown_html_artifact_assert:
    name: 'Pandoc: Markdown to HTML with artifact upload assertions'
    runs-on: ubuntu-latest
    needs: pandoc_markdown_html_artifact_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.pandoc_markdown_html_artifact_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: pandoc-markdown-html
          path: .devflows-test-artifacts/pandoc/markdown-html-artifact
      - name: 'Assert file exists: example.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/markdown-html-artifact/example.html
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: example.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/markdown-html-artifact/example.html
          ASSERT_TEXT: <h1
        run: python harness/scenarios/assert-file-contains.py
  pandoc_working_directory_artifact_call:
    name: 'Pandoc: Working directory conversion with artifact upload'
    uses: ./.github/workflows/pandoc.yaml
    with:
      pandoc-image: pandoc/core:3.8
      pandoc-working-directory: tests/scenarios/pandoc/working-directory
      pandoc-arguments: --standalone --output=output.html input.md
      artifact-upload-enabled: true
      artifact-upload-name: pandoc-working-directory
      artifact-upload-path: tests/scenarios/pandoc/working-directory/output.html
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  pandoc_working_directory_artifact_assert:
    name: 'Pandoc: Working directory conversion with artifact upload assertions'
    runs-on: ubuntu-latest
    needs: pandoc_working_directory_artifact_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.pandoc_working_directory_artifact_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: pandoc-working-directory
          path: .devflows-test-artifacts/pandoc/working-directory-artifact
      - name: 'Assert file exists: output.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/working-directory-artifact/output.html
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: output.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/working-directory-artifact/output.html
          ASSERT_TEXT: Pandoc Working Directory
        run: python harness/scenarios/assert-file-contains.py
  pandoc_artifact_download_html_setup:
    name: 'Pandoc: Markdown to HTML from downloaded artifact setup'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Create setup files
        shell: bash
        env:
          DEVFLOWS_SETUP_FILES: |-
            [
              {
                "path": ".devflows-test/pandoc/artifact-download/source.md",
                "content": "# Pandoc Artifact Input\n\nThis file was provided through an artifact download.\n"
              }
            ]
        run: python harness/scenarios/create-setup-files.py
      - name: Upload setup artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: pandoc-input-markdown
          path: .devflows-test/pandoc/artifact-download/source.md
          if-no-files-found: error
          retention-days: 1
          overwrite: true
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  pandoc_artifact_download_html_call:
    name: 'Pandoc: Markdown to HTML from downloaded artifact'
    uses: ./.github/workflows/pandoc.yaml
    with:
      checkout-enabled: false
      pandoc-image: pandoc/core:3.8
      artifact-download-enabled: true
      artifact-download-name: pandoc-input-markdown
      artifact-download-path: input
      pandoc-arguments: --standalone --output=.devflows-test/pandoc/artifact-download/example.html input/source.md
      artifact-upload-enabled: true
      artifact-upload-name: pandoc-artifact-download-html
      artifact-upload-path: .devflows-test/pandoc/artifact-download/example.html
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    needs: pandoc_artifact_download_html_setup
  pandoc_artifact_download_html_assert:
    name: 'Pandoc: Markdown to HTML from downloaded artifact assertions'
    runs-on: ubuntu-latest
    needs: pandoc_artifact_download_html_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.pandoc_artifact_download_html_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: pandoc-artifact-download-html
          path: .devflows-test-artifacts/pandoc/artifact-download-html
      - name: 'Assert file exists: example.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/artifact-download-html/example.html
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: example.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/artifact-download-html/example.html
          ASSERT_TEXT: Pandoc Artifact Input
        run: python harness/scenarios/assert-file-contains.py
  python_build_pure_wheelhouse_call:
    name: 'Python Build: Pure uv build produces a wheelhouse and sdist'
    uses: ./.github/workflows/python-build.yaml
    with:
      build-package-path: tests/scenarios/python-build/pure-package
      dist-artifact-prefix: python-build-pure
    permissions:
      actions: read
      contents: read
  python_build_pure_wheelhouse_assert:
    name: 'Python Build: Pure uv build produces a wheelhouse and sdist assertions'
    runs-on: ubuntu-latest
    needs: python_build_pure_wheelhouse_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_build_pure_wheelhouse_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-build-pure-wheels
          path: .devflows-test-artifacts/python-build/pure
      - name: 'Assert file exists: python_build_fixture-0.1.0-py3-none-any.whl'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-build/pure/python_build_fixture-0.1.0-py3-none-any.whl
        run: python harness/scenarios/assert-file-exists.py
      - name: Assert output package-version
        shell: bash
        env:
          ASSERT_NAME: package-version
          EXPECTED: 0.1.0
          ACTUAL: ${{ needs.python_build_pure_wheelhouse_call.outputs.package-version }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output wheels-artifact-name
        shell: bash
        env:
          ASSERT_NAME: wheels-artifact-name
          EXPECTED: python-build-pure-wheels
          ACTUAL: ${{ needs.python_build_pure_wheelhouse_call.outputs.wheels-artifact-name }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output conda-artifact-name
        shell: bash
        env:
          ASSERT_NAME: conda-artifact-name
          EXPECTED: ''
          ACTUAL: ${{ needs.python_build_pure_wheelhouse_call.outputs.conda-artifact-name }}
        run: python harness/scenarios/assert-equals.py
  python_build_python_build_tool_call:
    name: 'Python Build: python -m build tool parity'
    uses: ./.github/workflows/python-build.yaml
    with:
      build-tool: python-build
      build-python-version: '3.12'
      build-package-path: tests/scenarios/python-build/pure-package
      dist-artifact-prefix: python-build-pybuild
    permissions:
      actions: read
      contents: read
  python_build_python_build_tool_assert:
    name: 'Python Build: python -m build tool parity assertions'
    runs-on: ubuntu-latest
    needs: python_build_python_build_tool_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_build_python_build_tool_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-build-pybuild-sdist
          path: .devflows-test-artifacts/python-build/pybuild
      - name: 'Assert file exists: python_build_fixture-0.1.0.tar.gz'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-build/pybuild/python_build_fixture-0.1.0.tar.gz
        run: python harness/scenarios/assert-file-exists.py
      - name: Assert output sdist-artifact-name
        shell: bash
        env:
          ASSERT_NAME: sdist-artifact-name
          EXPECTED: python-build-pybuild-sdist
          ACTUAL: ${{ needs.python_build_python_build_tool_call.outputs.sdist-artifact-name }}
        run: python harness/scenarios/assert-equals.py
  python_build_sdist_only_call:
    name: 'Python Build: sdist-only build leaves the wheelhouse empty'
    uses: ./.github/workflows/python-build.yaml
    with:
      build-wheel-enabled: false
      build-package-path: tests/scenarios/python-build/pure-package
      dist-artifact-prefix: python-build-sdist
    permissions:
      actions: read
      contents: read
  python_build_sdist_only_assert:
    name: 'Python Build: sdist-only build leaves the wheelhouse empty assertions'
    runs-on: ubuntu-latest
    needs: python_build_sdist_only_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_build_sdist_only_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-build-sdist-sdist
          path: .devflows-test-artifacts/python-build/sdist
      - name: 'Assert file exists: python_build_fixture-0.1.0.tar.gz'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-build/sdist/python_build_fixture-0.1.0.tar.gz
        run: python harness/scenarios/assert-file-exists.py
      - name: Assert output wheels-artifact-name
        shell: bash
        env:
          ASSERT_NAME: wheels-artifact-name
          EXPECTED: ''
          ACTUAL: ${{ needs.python_build_sdist_only_call.outputs.wheels-artifact-name }}
        run: python harness/scenarios/assert-equals.py
  python_build_cibw_manylinux_call:
    name: 'Python Build: cibuildwheel builds a manylinux wheel into the wheelhouse'
    uses: ./.github/workflows/python-build.yaml
    with:
      build-sdist-enabled: false
      build-wheel-enabled: false
      cibw-enabled: true
      cibw-matrix: '[{"runner": "ubuntu-latest", "only": "cp313-manylinux_x86_64"}]'
      build-package-path: tests/scenarios/python-build/native-package
      dist-artifact-prefix: python-build-cibw
    permissions:
      actions: read
      contents: read
  python_build_cibw_manylinux_assert:
    name: 'Python Build: cibuildwheel builds a manylinux wheel into the wheelhouse assertions'
    runs-on: ubuntu-latest
    needs: python_build_cibw_manylinux_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_build_cibw_manylinux_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-build-cibw-wheels
          path: .devflows-test-artifacts/python-build/cibw
      - name: 'Assert file exists: devflows_cext_fixture-0.1.0-cp313-cp313-manylinux_2_17_x86_64.manylinux2014_x86_64.whl'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-build/cibw/devflows_cext_fixture-0.1.0-cp313-cp313-manylinux_2_17_x86_64.manylinux2014_x86_64.whl
        run: python harness/scenarios/assert-file-exists.py
      - name: Assert output wheels-artifact-name
        shell: bash
        env:
          ASSERT_NAME: wheels-artifact-name
          EXPECTED: python-build-cibw-wheels
          ACTUAL: ${{ needs.python_build_cibw_manylinux_call.outputs.wheels-artifact-name }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output package-version
        shell: bash
        env:
          ASSERT_NAME: package-version
          EXPECTED: 0.1.0
          ACTUAL: ${{ needs.python_build_cibw_manylinux_call.outputs.package-version }}
        run: python harness/scenarios/assert-equals.py
  python_build_conda_noarch_call:
    name: 'Python Build: rattler-build noarch package with a re-indexed channel'
    uses: ./.github/workflows/python-build.yaml
    with:
      build-sdist-enabled: false
      build-wheel-enabled: false
      conda-enabled: true
      conda-recipe-path: tests/scenarios/python-build/conda-recipe/recipe.yaml
      dist-artifact-prefix: python-build-conda
    permissions:
      actions: read
      contents: read
  python_build_conda_noarch_assert:
    name: 'Python Build: rattler-build noarch package with a re-indexed channel assertions'
    runs-on: ubuntu-latest
    needs: python_build_conda_noarch_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_build_conda_noarch_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-build-conda-conda-channel
          path: .devflows-test-artifacts/python-build/conda
      - name: 'Assert file contains: noarch/repodata.json'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-build/conda/noarch/repodata.json
          ASSERT_TEXT: python-build-conda-fixture
        run: python harness/scenarios/assert-file-contains.py
      - name: Assert output conda-artifact-name
        shell: bash
        env:
          ASSERT_NAME: conda-artifact-name
          EXPECTED: python-build-conda-conda-channel
          ACTUAL: ${{ needs.python_build_conda_noarch_call.outputs.conda-artifact-name }}
        run: python harness/scenarios/assert-equals.py
  python_build_nothing_to_build_validate:
    name: 'Python Build: All build flavors disabled fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: uv
          BUILD_SDIST_ENABLED: 'false'
          BUILD_WHEEL_ENABLED: 'false'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'false'
          CIBW_MATRIX: '[]'
          CONDA_ENABLED: 'false'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: python-build
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: Nothing to build
        run: python harness/scenarios/assert-validation-failure.py
  python_build_cibw_empty_matrix_validate:
    name: 'Python Build: cibw enabled with an empty matrix fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: uv
          BUILD_SDIST_ENABLED: 'true'
          BUILD_WHEEL_ENABLED: 'true'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'true'
          CIBW_MATRIX: '[]'
          CONDA_ENABLED: 'false'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: python-build
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: cibw-enabled is true but cibw-matrix is empty
        run: python harness/scenarios/assert-validation-failure.py
  python_build_conda_missing_recipe_validate:
    name: 'Python Build: conda enabled without a recipe path fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: uv
          BUILD_SDIST_ENABLED: 'true'
          BUILD_WHEEL_ENABLED: 'true'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'false'
          CIBW_MATRIX: '[]'
          CONDA_ENABLED: 'true'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: python-build
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: conda-recipe-path is empty
        run: python harness/scenarios/assert-validation-failure.py
  python_build_unknown_build_tool_validate:
    name: 'Python Build: Unknown build tool fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: cargo
          BUILD_SDIST_ENABLED: 'true'
          BUILD_WHEEL_ENABLED: 'true'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'false'
          CIBW_MATRIX: '[]'
          CONDA_ENABLED: 'false'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: python-build
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: build-tool must be 'uv' or 'python-build'
        run: python harness/scenarios/assert-validation-failure.py
  python_build_malformed_matrix_json_validate:
    name: 'Python Build: Malformed cibw-matrix JSON fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: uv
          BUILD_SDIST_ENABLED: 'true'
          BUILD_WHEEL_ENABLED: 'true'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'false'
          CIBW_MATRIX: '{not json'
          CONDA_ENABLED: 'false'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: python-build
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: cibw-matrix is not valid JSON
        run: python harness/scenarios/assert-validation-failure.py
  python_build_unsafe_artifact_prefix_validate:
    name: 'Python Build: Unsafe dist-artifact-prefix fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-build/scripts/validate-inputs.py
          BUILD_TOOL: uv
          BUILD_SDIST_ENABLED: 'true'
          BUILD_WHEEL_ENABLED: 'true'
          UV_CACHE_MODE: auto
          ARTIFACT_DOWNLOAD_ENABLED: ''
          CIBW_ENABLED: 'false'
          CIBW_MATRIX: '[]'
          CONDA_ENABLED: 'false'
          CONDA_RECIPE_PATH: ''
          CONDA_MATRIX: '[{"runner": "ubuntu-latest"}]'
          DIST_ARTIFACT_PREFIX: has space
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: dist-artifact-prefix must be a non-empty artifact-name-safe string
        run: python harness/scenarios/assert-validation-failure.py
  python_lint_clean_report_hosted_call:
    name: 'Python Lint: Clean project reports success and uploads the report artifact'
    uses: ./.github/workflows/python-lint.yaml
    with:
      lint-working-directory: tests/scenarios/python-lint/clean
      lint-report-directory: .devflows-test/python-lint/clean-report
      artifact-upload-enabled: true
      artifact-upload-name: python-lint-clean-report
      artifact-upload-path: .devflows-test/python-lint/clean-report
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: read
  python_lint_clean_report_hosted_assert:
    name: 'Python Lint: Clean project reports success and uploads the report artifact assertions'
    runs-on: ubuntu-latest
    needs: python_lint_clean_report_hosted_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_lint_clean_report_hosted_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-lint-clean-report
          path: .devflows-test-artifacts/python-lint/clean-report
      - name: Assert output lint-outcome
        shell: bash
        env:
          ASSERT_NAME: lint-outcome
          EXPECTED: success
          ACTUAL: ${{ needs.python_lint_clean_report_hosted_call.outputs.lint-outcome }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output ruff-check-outcome
        shell: bash
        env:
          ASSERT_NAME: ruff-check-outcome
          EXPECTED: success
          ACTUAL: ${{ needs.python_lint_clean_report_hosted_call.outputs.ruff-check-outcome }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output ruff-format-outcome
        shell: bash
        env:
          ASSERT_NAME: ruff-format-outcome
          EXPECTED: success
          ACTUAL: ${{ needs.python_lint_clean_report_hosted_call.outputs.ruff-format-outcome }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output typecheck-outcome
        shell: bash
        env:
          ASSERT_NAME: typecheck-outcome
          EXPECTED: success
          ACTUAL: ${{ needs.python_lint_clean_report_hosted_call.outputs.typecheck-outcome }}
        run: python harness/scenarios/assert-equals.py
      - name: 'Assert file exists: results.json'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-lint/clean-report/results.json
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: results.json'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-lint/clean-report/results.json
          ASSERT_TEXT: '"version": 1'
        run: python harness/scenarios/assert-file-contains.py
  python_lint_violations_advisory_hosted_call:
    name: 'Python Lint: Advisory mode reports failure but keeps the run green'
    uses: ./.github/workflows/python-lint.yaml
    with:
      lint-working-directory: tests/scenarios/python-lint/violations
      lint-enforce: false
      typecheck-enabled: false
    permissions:
      actions: read
      contents: read
  python_lint_violations_advisory_hosted_assert:
    name: 'Python Lint: Advisory mode reports failure but keeps the run green assertions'
    runs-on: ubuntu-latest
    needs: python_lint_violations_advisory_hosted_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_lint_violations_advisory_hosted_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Assert output lint-outcome
        shell: bash
        env:
          ASSERT_NAME: lint-outcome
          EXPECTED: failure
          ACTUAL: ${{ needs.python_lint_violations_advisory_hosted_call.outputs.lint-outcome }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output ruff-check-outcome
        shell: bash
        env:
          ASSERT_NAME: ruff-check-outcome
          EXPECTED: failure
          ACTUAL: ${{ needs.python_lint_violations_advisory_hosted_call.outputs.ruff-check-outcome }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output ruff-format-outcome
        shell: bash
        env:
          ASSERT_NAME: ruff-format-outcome
          EXPECTED: failure
          ACTUAL: ${{ needs.python_lint_violations_advisory_hosted_call.outputs.ruff-format-outcome }}
        run: python harness/scenarios/assert-equals.py
      - name: Assert output typecheck-outcome
        shell: bash
        env:
          ASSERT_NAME: typecheck-outcome
          EXPECTED: skipped
          ACTUAL: ${{ needs.python_lint_violations_advisory_hosted_call.outputs.typecheck-outcome }}
        run: python harness/scenarios/assert-equals.py
  python_lint_invalid_typecheck_tool_validate:
    name: 'Python Lint: Unknown typecheck tool fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-lint/scripts/validate-inputs.py
          LINT_PATHS: .
          LINT_WORKING_DIRECTORY: .
          LINT_REPORT_DIRECTORY: ''
          LINT_UV_CACHE_MODE: auto
          LINT_UV_SYNC_ENABLED: 'false'
          LINT_UV_SYNC_ARGUMENTS: ''
          LINT_RUFF_CHECK_ENABLED: 'true'
          LINT_RUFF_CHECK_ARGUMENTS: ''
          LINT_RUFF_FORMAT_ENABLED: 'true'
          LINT_RUFF_FORMAT_ARGUMENTS: ''
          LINT_RUFF_VERSION: ''
          LINT_TYPECHECK_ENABLED: 'true'
          LINT_TYPECHECK_TOOL: pytype
          LINT_TYPECHECK_VERSION: ''
          LINT_TYPECHECK_ARGUMENTS: ''
          LINT_TYPECHECK_PYTHON_VERSIONS: ''
          LINT_TYPECHECK_WITH: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: typecheck-tool must be one of
        run: python harness/scenarios/assert-validation-failure.py
  python_lint_no_lint_tools_validate:
    name: 'Python Lint: All lint tools disabled fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-lint/scripts/validate-inputs.py
          LINT_PATHS: .
          LINT_WORKING_DIRECTORY: .
          LINT_REPORT_DIRECTORY: ''
          LINT_UV_CACHE_MODE: auto
          LINT_UV_SYNC_ENABLED: 'false'
          LINT_UV_SYNC_ARGUMENTS: ''
          LINT_RUFF_CHECK_ENABLED: 'false'
          LINT_RUFF_CHECK_ARGUMENTS: ''
          LINT_RUFF_FORMAT_ENABLED: 'false'
          LINT_RUFF_FORMAT_ARGUMENTS: ''
          LINT_RUFF_VERSION: ''
          LINT_TYPECHECK_ENABLED: 'false'
          LINT_TYPECHECK_TOOL: mypy
          LINT_TYPECHECK_VERSION: ''
          LINT_TYPECHECK_ARGUMENTS: ''
          LINT_TYPECHECK_PYTHON_VERSIONS: ''
          LINT_TYPECHECK_WITH: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: At least one of ruff-check-enabled
        run: python harness/scenarios/assert-validation-failure.py
  python_lint_invalid_uv_cache_mode_validate:
    name: 'Python Lint: Invalid uv-cache-mode fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-lint/scripts/validate-inputs.py
          LINT_PATHS: .
          LINT_WORKING_DIRECTORY: .
          LINT_REPORT_DIRECTORY: ''
          LINT_UV_CACHE_MODE: sometimes
          LINT_UV_SYNC_ENABLED: 'false'
          LINT_UV_SYNC_ARGUMENTS: ''
          LINT_RUFF_CHECK_ENABLED: 'true'
          LINT_RUFF_CHECK_ARGUMENTS: ''
          LINT_RUFF_FORMAT_ENABLED: 'true'
          LINT_RUFF_FORMAT_ARGUMENTS: ''
          LINT_RUFF_VERSION: ''
          LINT_TYPECHECK_ENABLED: 'true'
          LINT_TYPECHECK_TOOL: mypy
          LINT_TYPECHECK_VERSION: ''
          LINT_TYPECHECK_ARGUMENTS: ''
          LINT_TYPECHECK_PYTHON_VERSIONS: ''
          LINT_TYPECHECK_WITH: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: uv-cache-mode must be one of
        run: python harness/scenarios/assert-validation-failure.py
  python_test_reports_merge_hosted_call:
    name: 'Python Test: Multi-leg matrix with merged report artifact'
    uses: ./.github/workflows/python-test.yaml
    with:
      test-matrix: '[{"runner": "ubuntu-latest", "python-version": "3.11", "name": "leg-a"}, {"runner": "ubuntu-latest", "python-version": "3.13", "name": "leg-b"}]'
      test-install-source: source
      test-source-directory: tests/scenarios/python-test/pkg
      test-dependencies: pytest
      test-command: pytest -c tests/scenarios/python-test/pkg/pyproject.toml --junitxml=.devflows-test/python-test/reports/report.xml tests/scenarios/python-test/pkg/tests
      report-artifact-enabled: true
      report-merge-enabled: true
      report-artifact-name: python-test-scn-reports
      report-path: .devflows-test/python-test/reports/report.xml
    permissions:
      actions: read
      contents: read
  python_test_reports_merge_hosted_assert:
    name: 'Python Test: Multi-leg matrix with merged report artifact assertions'
    runs-on: ubuntu-latest
    needs: python_test_reports_merge_hosted_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_test_reports_merge_hosted_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-test-scn-reports
          path: .devflows-test-artifacts/python-test/reports
      - name: 'Assert file exists: python-test-scn-reports--leg-leg-a/report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/reports/python-test-scn-reports--leg-leg-a/report.xml
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file exists: python-test-scn-reports--leg-leg-b/report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/reports/python-test-scn-reports--leg-leg-b/report.xml
        run: python harness/scenarios/assert-file-exists.py
      - name: Assert output report-artifact-name
        shell: bash
        env:
          ASSERT_NAME: report-artifact-name
          EXPECTED: python-test-scn-reports
          ACTUAL: ${{ needs.python_test_reports_merge_hosted_call.outputs.report-artifact-name }}
        run: python harness/scenarios/assert-equals.py
  python_test_artifact_install_hosted_setup:
    name: 'Python Test: Install a staged wheel and run pytest setup'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Create setup files
        shell: bash
        env:
          DEVFLOWS_SETUP_FILES: |-
            [
              {
                "path": ".devflows-test/python-test/wheelhouse/devflows_test_fixture-0.1.0-py3-none-any.whl",
                "source-path": "tests/scenarios/python-test/dist/devflows_test_fixture-0.1.0-py3-none-any.whl"
              }
            ]
        run: python harness/scenarios/create-setup-files.py
      - name: Upload setup artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: python-test-wheelhouse
          path: .devflows-test/python-test/wheelhouse
          if-no-files-found: error
          retention-days: 1
          overwrite: true
  python_test_artifact_install_hosted_call:
    name: 'Python Test: Install a staged wheel and run pytest'
    uses: ./.github/workflows/python-test.yaml
    with:
      artifact-download-enabled: true
      artifact-download-name: python-test-wheelhouse
      artifact-download-path: dist
      test-install-source: artifact
      test-dist-path: dist
      test-matrix: '[{"runner": "ubuntu-latest", "python-version": "3.13", "name": "artifact"}]'
      test-dependencies: pytest
      test-command: pytest -c tests/scenarios/python-test/pkg/pyproject.toml --junitxml=.devflows-test/python-test/artifact/report.xml tests/scenarios/python-test/pkg/tests
      report-artifact-enabled: true
      report-merge-enabled: false
      report-artifact-name: python-test-artifact-report
      report-path: .devflows-test/python-test/artifact/report.xml
    permissions:
      actions: read
      contents: read
    needs: python_test_artifact_install_hosted_setup
  python_test_artifact_install_hosted_assert:
    name: 'Python Test: Install a staged wheel and run pytest assertions'
    runs-on: ubuntu-latest
    needs: python_test_artifact_install_hosted_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_test_artifact_install_hosted_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-test-artifact-report--leg-artifact
          path: .devflows-test-artifacts/python-test/artifact
      - name: 'Assert file exists: report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/artifact/report.xml
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/artifact/report.xml
          ASSERT_TEXT: <testsuite
        run: python harness/scenarios/assert-file-contains.py
  python_test_micromamba_source_hosted_call:
    name: 'Python Test: Install from source in a micromamba environment'
    uses: ./.github/workflows/python-test.yaml
    with:
      test-env-manager: micromamba
      test-environment-file: tests/scenarios/python-test/environment.yml
      test-matrix: '[{"runner": "ubuntu-latest", "python-version": "3.12", "name": "mamba"}]'
      test-install-source: source
      test-source-directory: tests/scenarios/python-test/pkg
      test-command: pytest -c tests/scenarios/python-test/pkg/pyproject.toml --junitxml=.devflows-test/python-test/micromamba/report.xml tests/scenarios/python-test/pkg/tests
      report-artifact-enabled: true
      report-merge-enabled: false
      report-artifact-name: python-test-micromamba-report
      report-path: .devflows-test/python-test/micromamba/report.xml
    permissions:
      actions: read
      contents: read
  python_test_micromamba_source_hosted_assert:
    name: 'Python Test: Install from source in a micromamba environment assertions'
    runs-on: ubuntu-latest
    needs: python_test_micromamba_source_hosted_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_test_micromamba_source_hosted_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-test-micromamba-report--leg-mamba
          path: .devflows-test-artifacts/python-test/micromamba
      - name: 'Assert file exists: report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/micromamba/report.xml
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/micromamba/report.xml
          ASSERT_TEXT: <testsuite
        run: python harness/scenarios/assert-file-contains.py
  python_test_invalid_matrix_json_validate:
    name: 'Python Test: Non-JSON test-matrix fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-test/scripts/validate-inputs.py
          TEST_MATRIX: '{not json'
          TEST_ENV_MANAGER: uv
          TEST_ENVIRONMENT_FILE: ''
          TEST_INSTALL_SOURCE: source
          TEST_INSTALL_PREFER: wheel
          TEST_DEPENDENCY_GROUPS: ''
          UV_CACHE_MODE: auto
          REPORT_ARTIFACT_ENABLED: 'false'
          REPORT_PATH: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: test-matrix must be valid JSON
        run: python harness/scenarios/assert-validation-failure.py
  python_test_invalid_uv_cache_mode_validate:
    name: 'Python Test: Invalid uv-cache-mode fails validation'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert validation rejects the inputs
        shell: bash
        env:
          DEVFLOWS_VALIDATE_SCRIPT: workflows/python-test/scripts/validate-inputs.py
          TEST_MATRIX: '[{"runner": "ubuntu-latest", "python-version": "3.13", "name": "leg"}]'
          TEST_ENV_MANAGER: uv
          TEST_ENVIRONMENT_FILE: ''
          TEST_INSTALL_SOURCE: source
          TEST_INSTALL_PREFER: wheel
          TEST_DEPENDENCY_GROUPS: ''
          UV_CACHE_MODE: sometimes
          REPORT_ARTIFACT_ENABLED: 'false'
          REPORT_PATH: ''
          DEVFLOWS_FAILURE_MESSAGE_CONTAINS: uv-cache-mode must be one of
        run: python harness/scenarios/assert-validation-failure.py
  writeback_ephemeral_branch_writeback_setup:
    name: 'Writeback: Writeback to ephemeral branch setup'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      artifact-name: ${{ steps.setup.outputs.artifact-name }}
      base-sha: ${{ steps.setup.outputs.base-sha }}
      branch: ${{ steps.setup.outputs.branch }}
    steps:
      - name: Checkout target repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: true
      - name: Prepare branch and writeback payload
        id: setup
        shell: bash
        env:
          DEVFLOWS_ARTIFACT_NAME: writeback-e2e-payload
          DEVFLOWS_BRANCH_PREFIX: devflows/e2e/writeback
          DEVFLOWS_DELETE_PATHS: |-
            [
              "remove.html",
              "absent-on-branch.html"
            ]
          DEVFLOWS_FIXTURE_PATH: .devflows-e2e/writeback
          DEVFLOWS_INITIAL_FILES: |-
            [
              {
                "path": "generated/stale.html",
                "content": "stale generated content\n"
              },
              {
                "path": "remove.html",
                "content": "remove this file\n"
              }
            ]
          DEVFLOWS_PAYLOAD_FILES: |-
            [
              {
                "path": "generated/index.html",
                "content": "<h1>Updated by writeback</h1>\n"
              }
            ]
          DEVFLOWS_PAYLOAD_PATHS: |-
            [
              "generated"
            ]
        run: python harness/scenarios/setup-ephemeral-writeback.py
      - name: Upload writeback payload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ${{ steps.setup.outputs.artifact-name }}
          path: .devflows-writeback/payload
          if-no-files-found: error
          retention-days: 1
          overwrite: true
          include-hidden-files: true
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  writeback_ephemeral_branch_writeback_call:
    name: 'Writeback: Writeback to ephemeral branch'
    uses: ./.github/workflows/writeback.yaml
    with:
      commit-author-name: DevFlows E2E
      commit-author-email: devflows-e2e@example.test
      commit-message: 'test: writeback e2e'
      commit-push: true
      commit-branch: ${{ needs.writeback_ephemeral_branch_writeback_setup.outputs.branch }}
      commit-expected-base-sha: ${{ needs.writeback_ephemeral_branch_writeback_setup.outputs.base-sha }}
      commit-repository: ${{ github.repository }}
      writeback-artifact-name: ${{ needs.writeback_ephemeral_branch_writeback_setup.outputs.artifact-name }}
    permissions:
      actions: read
      contents: write
    needs: writeback_ephemeral_branch_writeback_setup
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  writeback_ephemeral_branch_writeback_assert:
    name: 'Writeback: Writeback to ephemeral branch assertions'
    runs-on: ubuntu-latest
    needs:
      - writeback_ephemeral_branch_writeback_setup
      - writeback_ephemeral_branch_writeback_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout ephemeral branch
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
          ref: ${{ needs.writeback_ephemeral_branch_writeback_setup.outputs.branch }}
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.writeback_ephemeral_branch_writeback_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Assert ephemeral branch
        shell: bash
        env:
          DEVFLOWS_ASSERTIONS: |-
            [
              {
                "type": "branch-file-contains",
                "path": "generated/index.html",
                "text": "Updated by writeback"
              },
              {
                "type": "branch-file-missing",
                "path": "generated/stale.html"
              },
              {
                "type": "branch-file-missing",
                "path": "remove.html"
              },
              {
                "type": "branch-file-missing",
                "path": "absent-on-branch.html"
              },
              {
                "type": "latest-commit-message-equals",
                "value": "test: writeback e2e"
              }
            ]
          DEVFLOWS_FIXTURE_PATH: .devflows-e2e/writeback
        run: python harness/scenarios/assert-ephemeral-writeback.py
  writeback_ephemeral_branch_writeback_cleanup:
    name: 'Writeback: Writeback to ephemeral branch cleanup'
    runs-on: ubuntu-latest
    needs:
      - writeback_ephemeral_branch_writeback_setup
      - writeback_ephemeral_branch_writeback_call
      - writeback_ephemeral_branch_writeback_assert
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    permissions:
      contents: write
    steps:
      - name: Checkout target repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: true
      - name: Delete ephemeral branch
        shell: bash
        env:
          DEVFLOWS_BRANCH_PREFIX: devflows/e2e/writeback
        run: python harness/scenarios/cleanup-ephemeral-branch.py

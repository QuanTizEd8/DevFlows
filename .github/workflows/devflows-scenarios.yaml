# GENERATED by devflows test-generate — do not edit; source: workflows/*/devflow.yaml.
# Scenarios whose reusable-workflow call needs a write permission are gated with a
# fork-safe `if:` (and so are the jobs that depend on them): fork pull requests skip
# them because GitHub caps the fork token to read and would fail the nested-permission
# check at startup. Same-repo pull requests, pushes to main, and workflow_dispatch
# still run every scenario.
name: DevFlows Hosted Scenario Tests
'on':
  pull_request: null
  push:
    branches:
      - main
  workflow_dispatch: null
permissions:
  contents: read
  actions: read
jobs:
  build_devcontainer_build_only_minimal_call:
    name: 'Build Devcontainer: Build a minimal devcontainer without pushing'
    uses: ./.github/workflows/build-devcontainer.yaml
    with:
      image-name: devflows-e2e-devcontainer
      image-tag: e2e
      devcontainer-config-file: tests/scenarios/build-devcontainer/minimal/devcontainer.json
      docker-login-enabled: false
      devcontainer-push: never
      devcontainer-cache-registry-enabled: false
      merge-enabled: false
      cache-enabled: false
      build-digest-artifact-name: build-only-digest
    permissions:
      actions: read
      contents: read
      packages: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  build_devcontainer_build_only_minimal_assert:
    name: 'Build Devcontainer: Build a minimal devcontainer without pushing assertions'
    runs-on: ubuntu-latest
    needs: build_devcontainer_build_only_minimal_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.build_devcontainer_build_only_minimal_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: build-only-digest-linux-amd64
          path: .devflows-test-artifacts/build-devcontainer/build-only
      - name: Assert output image-ref
        shell: bash
        env:
          ASSERT_NAME: image-ref
          EXPECTED: ''
          ACTUAL: ${{ needs.build_devcontainer_build_only_minimal_call.outputs.image-ref }}
        run: python harness/scenarios/assert-equals.py
      - name: 'Assert file contains: linux-amd64'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/build-devcontainer/build-only/linux-amd64
          ASSERT_TEXT: 'sha256:'
        run: python harness/scenarios/assert-file-contains.py
  pandoc_markdown_html_artifact_call:
    name: 'Pandoc: Markdown to HTML with artifact upload'
    uses: ./.github/workflows/pandoc.yaml
    with:
      pandoc-image: pandoc/core:3.8
      pandoc-arguments: --standalone --output=.devflows-test/pandoc/markdown-html-artifact/example.html README.md
      artifact-upload-enabled: true
      artifact-upload-name: pandoc-markdown-html
      artifact-upload-path: .devflows-test/pandoc/markdown-html-artifact/example.html
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  pandoc_markdown_html_artifact_assert:
    name: 'Pandoc: Markdown to HTML with artifact upload assertions'
    runs-on: ubuntu-latest
    needs: pandoc_markdown_html_artifact_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.pandoc_markdown_html_artifact_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: pandoc-markdown-html
          path: .devflows-test-artifacts/pandoc/markdown-html-artifact
      - name: 'Assert file exists: example.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/markdown-html-artifact/example.html
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: example.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/markdown-html-artifact/example.html
          ASSERT_TEXT: <h1
        run: python harness/scenarios/assert-file-contains.py
  pandoc_working_directory_artifact_call:
    name: 'Pandoc: Working directory conversion with artifact upload'
    uses: ./.github/workflows/pandoc.yaml
    with:
      pandoc-image: pandoc/core:3.8
      pandoc-working-directory: tests/scenarios/pandoc/working-directory
      pandoc-arguments: --standalone --output=output.html input.md
      artifact-upload-enabled: true
      artifact-upload-name: pandoc-working-directory
      artifact-upload-path: tests/scenarios/pandoc/working-directory/output.html
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  pandoc_working_directory_artifact_assert:
    name: 'Pandoc: Working directory conversion with artifact upload assertions'
    runs-on: ubuntu-latest
    needs: pandoc_working_directory_artifact_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.pandoc_working_directory_artifact_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: pandoc-working-directory
          path: .devflows-test-artifacts/pandoc/working-directory-artifact
      - name: 'Assert file exists: output.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/working-directory-artifact/output.html
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: output.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/working-directory-artifact/output.html
          ASSERT_TEXT: Pandoc Working Directory
        run: python harness/scenarios/assert-file-contains.py
  pandoc_artifact_download_html_setup:
    name: 'Pandoc: Markdown to HTML from downloaded artifact setup'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Create setup files
        shell: bash
        env:
          DEVFLOWS_SETUP_FILES: |-
            [
              {
                "path": ".devflows-test/pandoc/artifact-download/source.md",
                "content": "# Pandoc Artifact Input\n\nThis file was provided through an artifact download.\n"
              }
            ]
        run: python harness/scenarios/create-setup-files.py
      - name: Upload setup artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: pandoc-input-markdown
          path: .devflows-test/pandoc/artifact-download/source.md
          if-no-files-found: error
          retention-days: 1
          overwrite: true
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  pandoc_artifact_download_html_call:
    name: 'Pandoc: Markdown to HTML from downloaded artifact'
    uses: ./.github/workflows/pandoc.yaml
    with:
      checkout-enabled: false
      pandoc-image: pandoc/core:3.8
      artifact-download-enabled: true
      artifact-download-name: pandoc-input-markdown
      artifact-download-path: input
      pandoc-arguments: --standalone --output=.devflows-test/pandoc/artifact-download/example.html input/source.md
      artifact-upload-enabled: true
      artifact-upload-name: pandoc-artifact-download-html
      artifact-upload-path: .devflows-test/pandoc/artifact-download/example.html
      artifact-upload-if-no-files-found: error
    permissions:
      actions: read
      contents: write
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    needs: pandoc_artifact_download_html_setup
  pandoc_artifact_download_html_assert:
    name: 'Pandoc: Markdown to HTML from downloaded artifact assertions'
    runs-on: ubuntu-latest
    needs: pandoc_artifact_download_html_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.pandoc_artifact_download_html_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: pandoc-artifact-download-html
          path: .devflows-test-artifacts/pandoc/artifact-download-html
      - name: 'Assert file exists: example.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/artifact-download-html/example.html
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: example.html'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/pandoc/artifact-download-html/example.html
          ASSERT_TEXT: Pandoc Artifact Input
        run: python harness/scenarios/assert-file-contains.py
  python_test_reports_merge_hosted_call:
    name: 'Python Test: Multi-leg matrix with merged report artifact'
    uses: ./.github/workflows/python-test.yaml
    with:
      test-matrix: '[{"runner": "ubuntu-latest", "python-version": "3.11", "name": "leg-a"}, {"runner": "ubuntu-latest", "python-version": "3.13", "name": "leg-b"}]'
      test-install-source: source
      test-source-directory: tests/scenarios/python-test/pkg
      test-dependencies: pytest
      test-command: pytest -c tests/scenarios/python-test/pkg/pyproject.toml --junitxml=.devflows-test/python-test/reports/report.xml tests/scenarios/python-test/pkg/tests
      report-artifact-enabled: true
      report-merge-enabled: true
      report-artifact-name: python-test-scn-reports
      report-path: .devflows-test/python-test/reports/report.xml
    permissions:
      actions: read
      contents: read
  python_test_reports_merge_hosted_assert:
    name: 'Python Test: Multi-leg matrix with merged report artifact assertions'
    runs-on: ubuntu-latest
    needs: python_test_reports_merge_hosted_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_test_reports_merge_hosted_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-test-scn-reports
          path: .devflows-test-artifacts/python-test/reports
      - name: 'Assert file exists: python-test-scn-reports-leg-a/report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/reports/python-test-scn-reports-leg-a/report.xml
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file exists: python-test-scn-reports-leg-b/report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/reports/python-test-scn-reports-leg-b/report.xml
        run: python harness/scenarios/assert-file-exists.py
      - name: Assert output report-artifact-name
        shell: bash
        env:
          ASSERT_NAME: report-artifact-name
          EXPECTED: python-test-scn-reports
          ACTUAL: ${{ needs.python_test_reports_merge_hosted_call.outputs.report-artifact-name }}
        run: python harness/scenarios/assert-equals.py
  python_test_artifact_install_hosted_setup:
    name: 'Python Test: Install a staged wheel and run pytest setup'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Create setup files
        shell: bash
        env:
          DEVFLOWS_SETUP_FILES: |-
            [
              {
                "path": ".devflows-test/python-test/wheelhouse/devflows_test_fixture-0.1.0-py3-none-any.whl",
                "source-path": "tests/scenarios/python-test/dist/devflows_test_fixture-0.1.0-py3-none-any.whl"
              }
            ]
        run: python harness/scenarios/create-setup-files.py
      - name: Upload setup artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: python-test-wheelhouse
          path: .devflows-test/python-test/wheelhouse
          if-no-files-found: error
          retention-days: 1
          overwrite: true
  python_test_artifact_install_hosted_call:
    name: 'Python Test: Install a staged wheel and run pytest'
    uses: ./.github/workflows/python-test.yaml
    with:
      artifact-download-enabled: true
      artifact-download-name: python-test-wheelhouse
      artifact-download-path: dist
      test-install-source: artifact
      test-dist-path: dist
      test-matrix: '[{"runner": "ubuntu-latest", "python-version": "3.13", "name": "artifact"}]'
      test-dependencies: pytest
      test-command: pytest -c tests/scenarios/python-test/pkg/pyproject.toml --junitxml=.devflows-test/python-test/artifact/report.xml tests/scenarios/python-test/pkg/tests
      report-artifact-enabled: true
      report-merge-enabled: false
      report-artifact-name: python-test-artifact-report
      report-path: .devflows-test/python-test/artifact/report.xml
    permissions:
      actions: read
      contents: read
    needs: python_test_artifact_install_hosted_setup
  python_test_artifact_install_hosted_assert:
    name: 'Python Test: Install a staged wheel and run pytest assertions'
    runs-on: ubuntu-latest
    needs: python_test_artifact_install_hosted_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_test_artifact_install_hosted_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-test-artifact-report-artifact
          path: .devflows-test-artifacts/python-test/artifact
      - name: 'Assert file exists: report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/artifact/report.xml
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/artifact/report.xml
          ASSERT_TEXT: <testsuite
        run: python harness/scenarios/assert-file-contains.py
  python_test_micromamba_source_hosted_call:
    name: 'Python Test: Install from source in a micromamba environment'
    uses: ./.github/workflows/python-test.yaml
    with:
      test-env-manager: micromamba
      test-environment-file: tests/scenarios/python-test/environment.yml
      test-matrix: '[{"runner": "ubuntu-latest", "python-version": "3.12", "name": "mamba"}]'
      test-install-source: source
      test-source-directory: tests/scenarios/python-test/pkg
      test-command: pytest -c tests/scenarios/python-test/pkg/pyproject.toml --junitxml=.devflows-test/python-test/micromamba/report.xml tests/scenarios/python-test/pkg/tests
      report-artifact-enabled: true
      report-merge-enabled: false
      report-artifact-name: python-test-micromamba-report
      report-path: .devflows-test/python-test/micromamba/report.xml
    permissions:
      actions: read
      contents: read
  python_test_micromamba_source_hosted_assert:
    name: 'Python Test: Install from source in a micromamba environment assertions'
    runs-on: ubuntu-latest
    needs: python_test_micromamba_source_hosted_call
    if: always()
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.python_test_micromamba_source_hosted_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Download scenario artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: python-test-micromamba-report-mamba
          path: .devflows-test-artifacts/python-test/micromamba
      - name: 'Assert file exists: report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/micromamba/report.xml
        run: python harness/scenarios/assert-file-exists.py
      - name: 'Assert file contains: report.xml'
        shell: bash
        env:
          ASSERT_PATH: .devflows-test-artifacts/python-test/micromamba/report.xml
          ASSERT_TEXT: <testsuite
        run: python harness/scenarios/assert-file-contains.py
  writeback_ephemeral_branch_writeback_setup:
    name: 'Writeback: Writeback to ephemeral branch setup'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      artifact-name: ${{ steps.setup.outputs.artifact-name }}
      base-sha: ${{ steps.setup.outputs.base-sha }}
      branch: ${{ steps.setup.outputs.branch }}
    steps:
      - name: Checkout target repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: true
      - name: Prepare branch and writeback payload
        id: setup
        shell: bash
        env:
          DEVFLOWS_ARTIFACT_NAME: writeback-e2e-payload
          DEVFLOWS_BRANCH_PREFIX: devflows/e2e/writeback
          DEVFLOWS_DELETE_PATHS: |-
            [
              "remove.html",
              "absent-on-branch.html"
            ]
          DEVFLOWS_FIXTURE_PATH: .devflows-e2e/writeback
          DEVFLOWS_INITIAL_FILES: |-
            [
              {
                "path": "generated/stale.html",
                "content": "stale generated content\n"
              },
              {
                "path": "remove.html",
                "content": "remove this file\n"
              }
            ]
          DEVFLOWS_PAYLOAD_FILES: |-
            [
              {
                "path": "generated/index.html",
                "content": "<h1>Updated by writeback</h1>\n"
              }
            ]
          DEVFLOWS_PAYLOAD_PATHS: |-
            [
              "generated"
            ]
        run: python harness/scenarios/setup-ephemeral-writeback.py
      - name: Upload writeback payload
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: ${{ steps.setup.outputs.artifact-name }}
          path: .devflows-writeback/payload
          if-no-files-found: error
          retention-days: 1
          overwrite: true
          include-hidden-files: true
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  writeback_ephemeral_branch_writeback_call:
    name: 'Writeback: Writeback to ephemeral branch'
    uses: ./.github/workflows/writeback.yaml
    with:
      commit-author-name: DevFlows E2E
      commit-author-email: devflows-e2e@example.test
      commit-message: 'test: writeback e2e'
      commit-push: true
      commit-branch: ${{ needs.writeback_ephemeral_branch_writeback_setup.outputs.branch }}
      commit-expected-base-sha: ${{ needs.writeback_ephemeral_branch_writeback_setup.outputs.base-sha }}
      commit-repository: ${{ github.repository }}
      writeback-artifact-name: ${{ needs.writeback_ephemeral_branch_writeback_setup.outputs.artifact-name }}
    permissions:
      actions: read
      contents: write
    needs: writeback_ephemeral_branch_writeback_setup
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
  writeback_ephemeral_branch_writeback_assert:
    name: 'Writeback: Writeback to ephemeral branch assertions'
    runs-on: ubuntu-latest
    needs:
      - writeback_ephemeral_branch_writeback_setup
      - writeback_ephemeral_branch_writeback_call
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    steps:
      - name: Checkout ephemeral branch
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
          ref: ${{ needs.writeback_ephemeral_branch_writeback_setup.outputs.branch }}
      - name: Assert scenario succeeded
        shell: bash
        env:
          ACTUAL_RESULT: ${{ needs.writeback_ephemeral_branch_writeback_call.result }}
        run: python harness/scenarios/assert-result.py
      - name: Assert ephemeral branch
        shell: bash
        env:
          DEVFLOWS_ASSERTIONS: |-
            [
              {
                "type": "branch-file-contains",
                "path": "generated/index.html",
                "text": "Updated by writeback"
              },
              {
                "type": "branch-file-missing",
                "path": "generated/stale.html"
              },
              {
                "type": "branch-file-missing",
                "path": "remove.html"
              },
              {
                "type": "branch-file-missing",
                "path": "absent-on-branch.html"
              },
              {
                "type": "latest-commit-message-equals",
                "value": "test: writeback e2e"
              }
            ]
          DEVFLOWS_FIXTURE_PATH: .devflows-e2e/writeback
        run: python harness/scenarios/assert-ephemeral-writeback.py
  writeback_ephemeral_branch_writeback_cleanup:
    name: 'Writeback: Writeback to ephemeral branch cleanup'
    runs-on: ubuntu-latest
    needs:
      - writeback_ephemeral_branch_writeback_setup
      - writeback_ephemeral_branch_writeback_call
      - writeback_ephemeral_branch_writeback_assert
    if: always() && (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository)
    permissions:
      contents: write
    steps:
      - name: Checkout target repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: true
      - name: Delete ephemeral branch
        shell: bash
        env:
          DEVFLOWS_BRANCH_PREFIX: devflows/e2e/writeback
        run: python harness/scenarios/cleanup-ephemeral-branch.py

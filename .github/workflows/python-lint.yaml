# GENERATED by devflows sync — do not edit; source: workflows/python-lint/
name: '[Reusable]: Python Lint'
'on':
  workflow_call:
    inputs:
      lint-paths:
        description: Newline-separated files or directories (relative to lint-working-directory) passed as target arguments to ruff check, ruff format, and the type checker. Each path must exist and stay inside the workspace; an empty value fails validation.
        type: string
        required: false
        default: .
      lint-working-directory:
        description: Directory under the checked-out workspace where all tools run (monorepo support). Must exist and stay inside GITHUB_WORKSPACE.
        type: string
        required: false
        default: .
      lint-enforce:
        description: When true (default), the job fails if any enabled tool reports findings. When false, findings are still fully reported (job summary, annotations, outputs, report files) but the job succeeds -- advisory mode for gradual adoption. Never suppresses input-validation errors or tool crashes; a misconfigured call always fails loudly.
        type: boolean
        required: false
        default: true
      lint-annotations-enabled:
        description: Whether to emit GitHub inline annotations for findings, capped at 10 per tool. The job summary and outputs are unaffected. Annotations are emitted by the DevFlows scripts from structured tool output, so paths are workspace-relative.
        type: boolean
        required: false
        default: true
      lint-report-directory:
        description: When set, machine-readable reports are written there (results.json, ruff-check.json, ruff-format.diff, typecheck-<pyver>.txt) and the workflow uploads them as the lint-report-artifact-name artifact even when the job fails (see that input). Relative to the workspace; must stay inside it. Empty (default) disables file reports; the job summary is always rendered.
        type: string
        required: false
        default: ''
      lint-report-artifact-name:
        description: Name of the workflow-owned artifact that publishes the lint-report-directory contents. Uploaded on '!cancelled()' whenever lint-report-directory is set, so findings that fail the job (lint-enforce) are still published -- unlike the success-gated generic artifact-upload channel, which stays reserved for build outputs. Callers invoking this workflow more than once in the same run must pass distinct names.
        type: string
        required: false
        default: python-lint-report
      lint-timeout-minutes:
        description: Maximum minutes the lint job may run before it is cancelled.
        type: number
        required: false
        default: 15
      lint-python-version:
        description: Python version passed to setup-uv (sets the interpreter environment). Empty (default) lets uv resolve from the project's .python-version/pyproject.toml, falling back to the latest stable CPython. Per-target-version type checking is controlled separately by typecheck-python-versions.
        type: string
        required: false
        default: ''
      uv-version:
        description: uv version passed to setup-uv. Empty (default) resolves from the caller's uv.toml/pyproject.toml, then the latest release.
        type: string
        required: false
        default: ''
      uv-cache-mode:
        description: setup-uv cache passthrough (its enable-cache input). One of auto, true, or false; auto (default) enables the uv cache only on GitHub-hosted runners.
        type: string
        required: false
        default: auto
      uv-sync-enabled:
        description: When true, runs 'uv sync' in lint-working-directory before the tools, and the type checker resolves first-party imports against that project environment. Requires the caller repo to be a uv project (pyproject.toml present, validated). Default false runs tools in isolated uvx environments with zero project assumptions.
        type: boolean
        required: false
        default: false
      uv-sync-arguments:
        description: Extra arguments appended to 'uv sync' (for example --locked --all-extras --group typing). Shlex-split via env-var mediation; only used when uv-sync-enabled is true (setting it otherwise fails validation).
        type: string
        required: false
        default: ''
      ruff-check-enabled:
        description: Whether to run 'ruff check' (lint rules).
        type: boolean
        required: false
        default: true
      ruff-check-arguments:
        description: Extra arguments appended to 'ruff check' (for example --select, --preview, --config path). Shlex-split via env-var mediation. Validation rejects --fix / --fix-only / --unsafe-fixes (this workflow is read-only) and --output-format (the workflow owns output capture for reporting).
        type: string
        required: false
        default: ''
      ruff-format-enabled:
        description: Whether to run 'ruff format --check --diff' (formatting gate). The diff is rendered into the job summary, truncated to a documented byte cap.
        type: boolean
        required: false
        default: true
      ruff-format-arguments:
        description: Extra arguments appended to 'ruff format' after the fixed --check --diff flags (for example --config path, --preview). Shlex-split via env-var mediation. Arguments incompatible with --check surface as a tool crash, which fails loudly regardless of lint-enforce.
        type: string
        required: false
        default: ''
      ruff-version:
        description: Exact ruff version to run (bare PEP 440 version, for example 0.14.2; no specifiers or whitespace). Empty (default) runs the latest ruff via uvx. Pin this for reproducible gates.
        type: string
        required: false
        default: ''
      typecheck-enabled:
        description: Whether to run the type checker.
        type: boolean
        required: false
        default: true
      typecheck-tool:
        description: 'Type checker to run: mypy (default, mature ecosystem and plugin support), pyright (highest typing-spec conformance; runs via the PyPI wrapper which bootstraps Node on the runner), or ty (Astral''s checker; experimental/beta as of mid-2026). Any other value fails input validation loudly.'
        type: string
        required: false
        default: mypy
      typecheck-version:
        description: Exact version of the selected type checker (bare PEP 440 version; validated). Empty runs the latest release. Pin for reproducible gates.
        type: string
        required: false
        default: ''
      typecheck-arguments:
        description: Extra arguments passed to the type checker (for example --strict, --config-file path, --platform win32 for mypy). Shlex-split via env-var mediation. Target paths come from lint-paths; the per-version flag is owned by typecheck-python-versions (a caller-supplied --python-version/--pythonversion here is rejected to avoid conflicting duplicates).
        type: string
        required: false
        default: ''
      typecheck-python-versions:
        description: Newline-separated target Python versions, for example 3.11 then 3.13 on its own line. Each entry must match a 3.<minor> pattern. Each entry produces one checker invocation with the tool's target-version flag inside the single job -- no job matrix, no interpreter installs per version. Empty (default) runs one invocation with the tool's own default.
        type: string
        required: false
        default: ''
      typecheck-with:
        description: Newline-separated additional PyPI requirement specifiers installed alongside the type checker (stub packages like types-requests, checker plugins, or '.' to install the caller's project into the checker environment). Passed as repeated --with flags to uvx / uv run; each line is validated as a requirement specifier.
        type: string
        required: false
        default: ''
      checkout-enabled:
        description: Whether to run actions/checkout before workflow execution.
        type: boolean
        required: false
        default: true
      checkout-repository:
        description: Repository name with owner to checkout.
        type: string
        required: false
        default: ${{ github.repository }}
      checkout-ref:
        description: Branch, tag, or SHA to checkout.
        type: string
        required: false
        default: ${{ github.ref }}
      checkout-ssh-known-hosts:
        description: Known hosts in addition to the user and global host key database.
        type: string
        required: false
        default: ''
      checkout-ssh-strict:
        description: Whether to perform strict host key checking.
        type: boolean
        required: false
        default: true
      checkout-ssh-user:
        description: SSH user to use when connecting to the remote SSH host.
        type: string
        required: false
        default: git
      checkout-persist-credentials:
        description: Whether to configure the token or SSH key with local git config.
        type: boolean
        required: false
        default: true
      checkout-path:
        description: Relative path under GITHUB_WORKSPACE to place the repository.
        type: string
        required: false
        default: ''
      checkout-clean:
        description: Whether to clean and reset before fetching.
        type: boolean
        required: false
        default: true
      checkout-filter:
        description: Partial clone filter. Overrides sparse checkout when set.
        type: string
        required: false
        default: ''
      checkout-sparse-checkout:
        description: Sparse checkout patterns separated by new lines.
        type: string
        required: false
        default: ''
      checkout-sparse-checkout-cone-mode:
        description: Whether to use cone mode for sparse checkout.
        type: boolean
        required: false
        default: true
      checkout-fetch-depth:
        description: Number of commits to fetch. Use 0 for all history.
        type: number
        required: false
        default: 1
      checkout-fetch-tags:
        description: Whether to fetch tags when fetch-depth is greater than 0.
        type: boolean
        required: false
        default: false
      checkout-show-progress:
        description: Whether to show fetch progress output.
        type: boolean
        required: false
        default: true
      checkout-lfs:
        description: Whether to download Git LFS files.
        type: boolean
        required: false
        default: false
      checkout-submodules:
        description: Whether to checkout submodules; use recursive for nested submodules.
        type: string
        required: false
        default: 'false'
      checkout-set-safe-directory:
        description: Whether to add the repository path as a safe Git directory.
        type: boolean
        required: false
        default: true
      checkout-github-server-url:
        description: GitHub server URL to fetch from.
        type: string
        required: false
        default: ''
      checkout-allow-unsafe-pr-checkout:
        description: Allow checkout of fork pull request code in trusted contexts.
        type: boolean
        required: false
        default: false
      artifact-download-enabled:
        description: Whether to download artifacts before workflow execution.
        type: boolean
        required: false
        default: false
      artifact-download-name:
        description: Name of the artifact to download. Leave empty to download all artifacts.
        type: string
        required: false
        default: ''
      artifact-download-ids:
        description: Comma-separated artifact IDs to download. Mutually exclusive with artifact-download-name.
        type: string
        required: false
        default: ''
      artifact-download-path:
        description: Destination path for downloaded artifacts.
        type: string
        required: false
        default: .
      artifact-download-pattern:
        description: Glob pattern matching artifacts to download when name is unset.
        type: string
        required: false
        default: ''
      artifact-download-merge-multiple:
        description: Whether multiple matched artifacts are extracted into one directory.
        type: boolean
        required: false
        default: false
      artifact-download-repository:
        description: Repository to download artifacts from when a token is set.
        type: string
        required: false
        default: ${{ github.repository }}
      artifact-download-run-id:
        description: Workflow run ID to download artifacts from when a token is set.
        type: string
        required: false
        default: ${{ github.run_id }}
      artifact-download-skip-decompress:
        description: Whether to download the artifact archive without extracting it.
        type: boolean
        required: false
        default: false
      artifact-download-digest-mismatch:
        description: Behavior when a downloaded artifact digest does not match.
        type: string
        required: false
        default: error
      artifact-upload-enabled:
        description: Whether to upload artifacts after workflow execution.
        type: boolean
        required: false
        default: false
      artifact-upload-path:
        description: File, directory, or wildcard pattern to upload after workflow execution.
        type: string
        required: false
        default: ''
      artifact-upload-name:
        description: Artifact name.
        type: string
        required: false
        default: artifact
      artifact-upload-if-no-files-found:
        description: Behavior when no files match artifact-upload-path.
        type: string
        required: false
        default: warn
      artifact-upload-retention-days:
        description: Artifact retention in days. Use 0 for repository default.
        type: number
        required: false
        default: 0
      artifact-upload-compression-level:
        description: Compression level from 0 to 9.
        type: number
        required: false
        default: 6
      artifact-upload-overwrite:
        description: Whether to overwrite an existing artifact with the same name.
        type: boolean
        required: false
        default: false
      artifact-upload-include-hidden-files:
        description: Whether hidden files under artifact-upload-path are included.
        type: boolean
        required: false
        default: false
      artifact-upload-archive:
        description: Whether upload-artifact archives files before upload.
        type: boolean
        required: false
        default: true
    outputs:
      lint-outcome:
        description: '"success" when every enabled tool passed; "failure" when any enabled tool reported findings or crashed. Set even when lint-enforce is false and the job succeeds, so callers can gate downstream jobs in advisory mode (guard needs with if !cancelled()).'
        value: ${{ jobs.python-lint.outputs.lint-outcome }}
      ruff-check-outcome:
        description: '"success", "failure", or "skipped" (when ruff-check-enabled is false).'
        value: ${{ jobs.python-lint.outputs.ruff-check-outcome }}
      ruff-format-outcome:
        description: '"success", "failure", or "skipped".'
        value: ${{ jobs.python-lint.outputs.ruff-format-outcome }}
      typecheck-outcome:
        description: '"success", "failure", or "skipped". Failure if any target-version run failed.'
        value: ${{ jobs.python-lint.outputs.typecheck-outcome }}
      lint-results:
        description: Stable-schema JSON (sorted keys, no timestamps) describing per-tool outcomes and counts. The same document is written to lint-report-directory/results.json when that input is set.
        value: ${{ jobs.python-lint.outputs.lint-results }}
    secrets:
      checkout-token:
        description: Token used by actions/checkout. Defaults to github.token.
        required: false
      checkout-ssh-key:
        description: SSH key used by actions/checkout.
        required: false
      artifact-download-token:
        description: Token used to download artifacts from another repository or run.
        required: false
permissions:
  contents: read
jobs:
  python-lint:
    name: Python Lint
    runs-on: ubuntu-latest
    timeout-minutes: ${{ inputs.lint-timeout-minutes }}
    permissions:
      contents: read
      actions: read
    outputs:
      lint-outcome: ${{ steps.lint.outputs.lint-outcome }}
      ruff-check-outcome: ${{ steps.lint.outputs.ruff-check-outcome }}
      ruff-format-outcome: ${{ steps.lint.outputs.ruff-format-outcome }}
      typecheck-outcome: ${{ steps.lint.outputs.typecheck-outcome }}
      lint-results: ${{ steps.lint.outputs.lint-results }}
    steps:
      - name: Checkout repository
        if: inputs.checkout-enabled
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          repository: ${{ inputs.checkout-repository }}
          ref: ${{ inputs.checkout-ref }}
          token: ${{ secrets.checkout-token || github.token }}
          ssh-key: ${{ secrets.checkout-ssh-key }}
          ssh-known-hosts: ${{ inputs.checkout-ssh-known-hosts }}
          ssh-strict: ${{ inputs.checkout-ssh-strict }}
          ssh-user: ${{ inputs.checkout-ssh-user }}
          persist-credentials: ${{ inputs.checkout-persist-credentials }}
          path: ${{ inputs.checkout-path }}
          clean: ${{ inputs.checkout-clean }}
          filter: ${{ inputs.checkout-filter }}
          sparse-checkout: ${{ inputs.checkout-sparse-checkout }}
          sparse-checkout-cone-mode: ${{ inputs.checkout-sparse-checkout-cone-mode }}
          fetch-depth: ${{ inputs.checkout-fetch-depth }}
          fetch-tags: ${{ inputs.checkout-fetch-tags }}
          show-progress: ${{ inputs.checkout-show-progress }}
          lfs: ${{ inputs.checkout-lfs }}
          submodules: ${{ inputs.checkout-submodules }}
          set-safe-directory: ${{ inputs.checkout-set-safe-directory }}
          github-server-url: ${{ inputs.checkout-github-server-url }}
          allow-unsafe-pr-checkout: ${{ inputs.checkout-allow-unsafe-pr-checkout }}
      - name: Download artifacts
        if: inputs.artifact-download-enabled
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c  # v8.0.1
        with:
          name: ${{ inputs.artifact-download-name }}
          artifact-ids: ${{ inputs.artifact-download-ids }}
          path: ${{ inputs.artifact-download-path }}
          pattern: ${{ inputs.artifact-download-pattern }}
          merge-multiple: ${{ inputs.artifact-download-merge-multiple }}
          github-token: ${{ secrets.artifact-download-token }}
          repository: ${{ inputs.artifact-download-repository }}
          run-id: ${{ inputs.artifact-download-run-id }}
          skip-decompress: ${{ inputs.artifact-download-skip-decompress }}
          digest-mismatch: ${{ inputs.artifact-download-digest-mismatch }}
      - name: Materialize DevFlows runtime scripts
        id: devflows-runtime
        shell: bash
        run: |-
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/devflows/python-lint"
          cat > "$RUNNER_TEMP/devflows/python-lint/validate-inputs.py" <<'DEVFLOWS_SCRIPT_EOF'
          """Fail-fast validation for the python-lint reusable workflow.

          Runs before any tool. Every caller string reaches this script through an env var
          (never interpolated into a run: body) and is validated here so a misconfigured
          call fails loudly with a single-line reason instead of surfacing as an opaque
          tool crash later. All checks are pure Python; nothing is shelled out.
          """

          from __future__ import annotations

          import os
          import re
          import shlex
          from pathlib import Path

          TYPECHECK_TOOLS = ("mypy", "pyright", "ty")
          UV_CACHE_MODES = ("auto", "true", "false")
          # Bare PEP 440-ish version: starts with a digit, no whitespace, no version
          # specifier operators (== >= <= ~= != < > * ,). Enough to reject a specifier or
          # an accidental range while accepting 0.14.2, 1.11.0, 1.2.0rc1, 2.0.0.post1.
          _VERSION = re.compile(r"^[0-9][0-9A-Za-z.+-]*$")
          _TARGET_PYTHON = re.compile(r"^3\.[0-9]+$")
          # ruff arguments that would write files or seize output capture the workflow owns.
          _FORBIDDEN_RUFF_CHECK = ("--fix", "--fix-only", "--unsafe-fixes", "--output-format")
          # per-version flags the workflow owns via typecheck-python-versions.
          _FORBIDDEN_TYPECHECK = ("--python-version", "--pythonversion")


          def main() -> int:
              workspace = _workspace()
              working_directory = _validate_working_directory(workspace)
              _validate_report_directory(workspace)
              _validate_lint_paths(working_directory, workspace)
              _validate_uv_cache_mode()
              _validate_tool_selection()
              _validate_uv_sync(working_directory)
              _validate_ruff_check_arguments()
              _validate_shlex("LINT_RUFF_FORMAT_ARGUMENTS", "ruff-format-arguments")
              _validate_typecheck()
              return 0


          def _validate_working_directory(workspace: Path) -> Path:
              relative = _get("LINT_WORKING_DIRECTORY") or "."
              working_directory = (workspace / relative).resolve()
              if workspace != working_directory and workspace not in working_directory.parents:
                  raise SystemExit("lint-working-directory must stay inside GITHUB_WORKSPACE.")
              if not working_directory.is_dir():
                  raise SystemExit(f"lint-working-directory does not exist: {relative}")
              return working_directory


          def _validate_report_directory(workspace: Path) -> None:
              relative = _get("LINT_REPORT_DIRECTORY")
              if not relative:
                  return
              report_directory = (workspace / relative).resolve()
              if workspace != report_directory and workspace not in report_directory.parents:
                  raise SystemExit("lint-report-directory must stay inside GITHUB_WORKSPACE.")


          def _validate_lint_paths(working_directory: Path, workspace: Path) -> None:
              paths = _split_lines(_get("LINT_PATHS"))
              if not paths:
                  raise SystemExit("lint-paths must not be empty.")
              for raw in paths:
                  if Path(raw).is_absolute():
                      raise SystemExit(f"lint-paths entries must be relative: {raw}")
                  resolved = (working_directory / raw).resolve()
                  if workspace != resolved and workspace not in resolved.parents:
                      raise SystemExit(f"lint-paths entry escapes the workspace: {raw}")
                  if not resolved.exists():
                      raise SystemExit(f"lint-paths entry does not exist: {raw}")


          def _validate_uv_cache_mode() -> None:
              mode = _get("LINT_UV_CACHE_MODE") or "auto"
              if mode not in UV_CACHE_MODES:
                  raise SystemExit(f"uv-cache-mode must be one of {UV_CACHE_MODES}, got {mode!r}.")


          def _validate_tool_selection() -> None:
              if not any(
                  _truthy(_get(name))
                  for name in (
                      "LINT_RUFF_CHECK_ENABLED",
                      "LINT_RUFF_FORMAT_ENABLED",
                      "LINT_TYPECHECK_ENABLED",
                  )
              ):
                  raise SystemExit(
                      "At least one of ruff-check-enabled, ruff-format-enabled, or typecheck-enabled "
                      "must be true; a call that lints nothing is rejected."
                  )
              if _truthy(_get("LINT_RUFF_CHECK_ENABLED")) or _get("LINT_RUFF_VERSION"):
                  _validate_version("LINT_RUFF_VERSION", "ruff-version")


          def _validate_uv_sync(working_directory: Path) -> None:
              sync_enabled = _truthy(_get("LINT_UV_SYNC_ENABLED"))
              if not sync_enabled:
                  if _get("LINT_UV_SYNC_ARGUMENTS"):
                      raise SystemExit("uv-sync-arguments is set but uv-sync-enabled is false.")
                  return
              if not (working_directory / "pyproject.toml").is_file():
                  raise SystemExit(
                      "uv-sync-enabled is true but no pyproject.toml exists in lint-working-directory; "
                      "uv sync requires a uv project."
                  )
              _validate_shlex("LINT_UV_SYNC_ARGUMENTS", "uv-sync-arguments")


          def _validate_ruff_check_arguments() -> None:
              tokens = _validate_shlex("LINT_RUFF_CHECK_ARGUMENTS", "ruff-check-arguments")
              for token in tokens:
                  head = token.split("=", 1)[0]
                  if head in _FORBIDDEN_RUFF_CHECK:
                      raise SystemExit(
                          f"ruff-check-arguments must not contain {head!r}; this workflow is read-only "
                          "and owns output capture for reporting."
                      )


          def _validate_typecheck() -> None:
              # typecheck-python-versions is a newline-separated list (list-input convention),
              # not a JSON array. Each non-empty line must be a bare 3.<minor> target.
              for entry in _split_lines(_get("LINT_TYPECHECK_PYTHON_VERSIONS")):
                  if not _TARGET_PYTHON.match(entry):
                      raise SystemExit(
                          f"typecheck-python-versions entries must match 3.<minor>, got {entry!r}."
                      )
              if not _truthy(_get("LINT_TYPECHECK_ENABLED")):
                  return
              tool = _get("LINT_TYPECHECK_TOOL") or "mypy"
              if tool not in TYPECHECK_TOOLS:
                  raise SystemExit(f"typecheck-tool must be one of {TYPECHECK_TOOLS}, got {tool!r}.")
              _validate_version("LINT_TYPECHECK_VERSION", "typecheck-version")
              tokens = _validate_shlex("LINT_TYPECHECK_ARGUMENTS", "typecheck-arguments")
              for token in tokens:
                  head = token.split("=", 1)[0]
                  if head in _FORBIDDEN_TYPECHECK:
                      raise SystemExit(
                          f"typecheck-arguments must not contain {head!r}; target Python versions are "
                          "controlled by typecheck-python-versions."
                      )
              for requirement in _split_lines(_get("LINT_TYPECHECK_WITH")):
                  if requirement != "." and requirement.startswith("-"):
                      raise SystemExit(
                          "typecheck-with entries must be requirement specifiers, not flags: "
                          f"{requirement!r}."
                      )


          def _validate_version(env_name: str, input_name: str) -> None:
              value = _get(env_name)
              if value and not _VERSION.match(value):
                  raise SystemExit(
                      f"{input_name} must be a bare version string (no specifiers or whitespace): {value!r}."
                  )


          def _validate_shlex(env_name: str, input_name: str) -> list[str]:
              value = _get(env_name)
              if not value:
                  return []
              try:
                  return shlex.split(value)
              except ValueError as error:
                  raise SystemExit(f"Unable to parse {input_name}: {error}") from error


          def _workspace() -> Path:
              # GITHUB_WORKSPACE is always set on a runner; fail fast with a clear message
              # (not a raw KeyError) when it is absent, e.g. a misconfigured local harness.
              raw = os.environ.get("GITHUB_WORKSPACE")
              if not raw:
                  raise SystemExit("GITHUB_WORKSPACE is not set.")
              return Path(raw).resolve()


          def _split_lines(value: str) -> list[str]:
              return [line.strip() for line in value.splitlines() if line.strip()]


          def _get(name: str) -> str:
              return os.environ.get(name, "").strip()


          def _truthy(value: str) -> bool:
              return value.strip().lower() in {"1", "true", "yes", "on"}


          if __name__ == "__main__":
              raise SystemExit(main())
          DEVFLOWS_SCRIPT_EOF
          mkdir -p "$RUNNER_TEMP/devflows/python-lint"
          cat > "$RUNNER_TEMP/devflows/python-lint/run-lint.py" <<'DEVFLOWS_SCRIPT_EOF'
          """Run the python-lint tools, render the report, set outputs, and enforce.

          A single orchestrator step so job outputs and the job summary are always
          populated -- each tool is run, its result recorded, and only then does the step
          decide whether to fail. Tool findings (exit 1) never crash the step on their
          own; the step exits nonzero only when a tool crashed (exit >= 2 / internal
          error, which fails regardless of lint-enforce) or when lint-enforce is true and
          any enabled tool reported findings. All caller strings arrive via env vars and
          are shlex-split here; nothing is interpolated into a shell.
          """

          from __future__ import annotations

          import json
          import os
          import re
          import shlex
          import subprocess
          from dataclasses import dataclass, field
          from pathlib import Path

          # Tool-specific command name, executed argv prefix, and per-target-version flag.
          # The package name equals the command name for all three (so one entry covers both
          # the uvx package spec and the uv-run --with requirement), but the EXECUTED command
          # is the argv prefix: ty's CLI requires a mandatory 'check' subcommand, so its argv
          # is ["ty", "check"] while its package spec stays 'ty@<ver>'. mypy/pyright invoke
          # the bare command. argv[0] is always the command name.
          _TYPECHECKERS = {
              "mypy": {"command": "mypy", "argv": ["mypy"], "version_flag": "--python-version"},
              "pyright": {"command": "pyright", "argv": ["pyright"], "version_flag": "--pythonversion"},
              "ty": {"command": "ty", "argv": ["ty", "check"], "version_flag": "--python-version"},
          }
          _SUMMARY_DIFF_CAP = 30_000  # keep GITHUB_STEP_SUMMARY well under the 1 MiB limit
          _ANNOTATION_CAP = 10  # GitHub renders at most ~10 annotations per step
          _REFORMAT_COUNT = re.compile(r"(\d+)\s+files?\s+would\s+be\s+reformatted")
          # mypy: file:line: error: msg  (column optional). pyright: file:line:col - error: msg.
          _MYPY_ERROR = re.compile(
              r"^(?P<file>[^:\n]+):(?P<line>\d+):(?:(?P<col>\d+):)?\s*error:", re.MULTILINE
          )
          _PYRIGHT_ERROR = re.compile(
              r"^\s*(?P<file>.+?):(?P<line>\d+):(?P<col>\d+)\s+-\s+error:", re.MULTILINE
          )


          @dataclass
          class RunResult:
              returncode: int
              stdout: str
              stderr: str


          @dataclass
          class ToolOutcome:
              outcome: str = "skipped"
              crashed: bool = False
              annotations: list[dict[str, str]] = field(default_factory=list)


          def main() -> int:
              workspace = Path(os.environ["GITHUB_WORKSPACE"]).resolve()
              working_directory = (workspace / (_get("LINT_WORKING_DIRECTORY") or ".")).resolve()
              enforce = _truthy(_get("LINT_ENFORCE"))
              annotations_enabled = _truthy(_get("LINT_ANNOTATIONS_ENABLED"))
              sync_enabled = _truthy(_get("LINT_UV_SYNC_ENABLED"))
              ruff_version = _get("LINT_RUFF_VERSION")
              paths = _split_lines(_get("LINT_PATHS")) or ["."]

              if sync_enabled:
                  _uv_sync(working_directory)

              results: dict[str, dict[str, object]] = {}
              summary_sections: list[str] = []
              annotations: list[dict[str, str]] = []

              ruff_check = ToolOutcome()
              if _truthy(_get("LINT_RUFF_CHECK_ENABLED")):
                  ruff_check, violations, report = _run_ruff_check(
                      working_directory, workspace, paths, ruff_version, sync_enabled
                  )
                  results["ruff-check"] = {"outcome": ruff_check.outcome, "violations": violations}
                  summary_sections.append(_ruff_check_summary(ruff_check.outcome, violations))
                  annotations.extend(ruff_check.annotations)
                  _write_report(report)
              else:
                  results["ruff-check"] = {"outcome": "skipped", "violations": 0}

              ruff_format = ToolOutcome()
              if _truthy(_get("LINT_RUFF_FORMAT_ENABLED")):
                  ruff_format, unformatted, diff, report = _run_ruff_format(
                      working_directory, workspace, paths, ruff_version, sync_enabled
                  )
                  results["ruff-format"] = {"outcome": ruff_format.outcome, "unformatted-files": unformatted}
                  summary_sections.append(_ruff_format_summary(ruff_format.outcome, unformatted, diff))
                  annotations.extend(ruff_format.annotations)
                  _write_report(report)
              else:
                  results["ruff-format"] = {"outcome": "skipped", "unformatted-files": 0}

              tool = _get("LINT_TYPECHECK_TOOL") or "mypy"
              if _truthy(_get("LINT_TYPECHECK_ENABLED")):
                  typecheck, runs, reports = _run_typecheck(working_directory, workspace, paths, sync_enabled)
                  results["typecheck"] = {"outcome": typecheck.outcome, "tool": tool, "runs": runs}
                  summary_sections.append(_typecheck_summary(typecheck.outcome, tool, runs))
                  annotations.extend(typecheck.annotations)
                  for report in reports:
                      _write_report(report)
              else:
                  typecheck = ToolOutcome()
                  results["typecheck"] = {"outcome": "skipped", "tool": tool, "runs": []}

              overall = "failure" if _any_failure(results) else "success"
              document = {"version": 1, "tools": results}
              _write_report(("results.json", json.dumps(document, indent=2, sort_keys=True) + "\n"))
              _render_summary(overall, enforce, summary_sections)
              if annotations_enabled:
                  _emit_annotations(annotations)
              _write_outputs(
                  {
                      "lint-outcome": overall,
                      "ruff-check-outcome": str(results["ruff-check"]["outcome"]),
                      "ruff-format-outcome": str(results["ruff-format"]["outcome"]),
                      "typecheck-outcome": str(results["typecheck"]["outcome"]),
                      "lint-results": json.dumps(document, sort_keys=True, separators=(",", ":")),
                  }
              )

              crashed = ruff_check.crashed or ruff_format.crashed or typecheck.crashed
              if crashed:
                  raise SystemExit(
                      "A lint tool crashed (see the report); failing regardless of lint-enforce."
                  )
              if enforce and overall == "failure":
                  raise SystemExit("Lint findings reported and lint-enforce is true; failing the job.")
              return 0


          # --------------------------------------------------------------------------- #
          # tool runners                                                                 #
          # --------------------------------------------------------------------------- #
          def _run_ruff_check(
              working_directory: Path, workspace: Path, paths: list[str], version: str, sync: bool
          ) -> tuple[ToolOutcome, int, tuple[str, str]]:
              extra = _shlex("LINT_RUFF_CHECK_ARGUMENTS")
              command = _ruff_command(["check", "--output-format=json", *extra, *paths], version, sync)
              result = _run(command, working_directory)
              outcome, crashed = _classify(result.returncode)
              violations, annotations = _parse_ruff_check(result.stdout, workspace)
              return (
                  ToolOutcome(outcome, crashed, annotations),
                  violations,
                  ("ruff-check.json", result.stdout),
              )


          def _run_ruff_format(
              working_directory: Path, workspace: Path, paths: list[str], version: str, sync: bool
          ) -> tuple[ToolOutcome, int, str, tuple[str, str]]:
              extra = _shlex("LINT_RUFF_FORMAT_ARGUMENTS")
              command = _ruff_command(["format", "--check", "--diff", *extra, *paths], version, sync)
              result = _run(command, working_directory)
              outcome, crashed = _classify(result.returncode)
              unformatted = _parse_reformat_count(result.stdout + "\n" + result.stderr)
              annotations = _format_annotations(result.stdout, workspace, working_directory)
              return (
                  ToolOutcome(outcome, crashed, annotations),
                  unformatted,
                  result.stdout,
                  (
                      "ruff-format.diff",
                      result.stdout,
                  ),
              )


          def _run_typecheck(
              working_directory: Path, workspace: Path, paths: list[str], sync: bool
          ) -> tuple[ToolOutcome, list[dict[str, object]], list[tuple[str, str]]]:
              tool = _get("LINT_TYPECHECK_TOOL") or "mypy"
              version = _get("LINT_TYPECHECK_VERSION")
              extra = _shlex("LINT_TYPECHECK_ARGUMENTS")
              with_requirements = _split_lines(_get("LINT_TYPECHECK_WITH"))
              # typecheck-python-versions is a newline-separated list (convention), one checker
              # invocation per entry; empty runs a single invocation with the tool's default.
              target_versions = _split_lines(_get("LINT_TYPECHECK_PYTHON_VERSIONS")) or [None]

              outcome = "success"
              crashed = False
              annotations: list[dict[str, str]] = []
              runs: list[dict[str, object]] = []
              reports: list[tuple[str, str]] = []
              for target in target_versions:
                  command = _typecheck_command(tool, version, target, extra, with_requirements, paths, sync)
                  result = _run(command, working_directory)
                  run_outcome, run_crashed = _classify(result.returncode)
                  if run_outcome == "failure":
                      outcome = "failure"
                  crashed = crashed or run_crashed
                  errors, run_annotations = _parse_typecheck(tool, result.stdout, result.stderr, workspace)
                  annotations.extend(run_annotations)
                  label = target or "default"
                  runs.append({"python-version": label, "errors": errors})
                  reports.append((f"typecheck-{label}.txt", result.stdout + result.stderr))
              return ToolOutcome(outcome, crashed, annotations), runs, reports


          def _uv_sync(working_directory: Path) -> None:
              command = ["uv", "sync", *_shlex("LINT_UV_SYNC_ARGUMENTS")]
              result = _run(command, working_directory)
              if result.returncode != 0:
                  _append_summary(f"## Python lint\n\n`uv sync` failed:\n\n```\n{result.stderr}\n```\n")
                  raise SystemExit(f"uv sync failed with exit code {result.returncode}.")


          # --------------------------------------------------------------------------- #
          # command construction                                                         #
          # --------------------------------------------------------------------------- #
          def _ruff_command(arguments: list[str], version: str, sync: bool) -> list[str]:
              if sync and not version:
                  # Use the project's pinned ruff from the synced environment.
                  return ["uv", "run", "--no-sync", "ruff", *arguments]
              spec = f"ruff@{version}" if version else "ruff"
              return ["uvx", spec, *arguments]


          def _typecheck_command(
              tool: str,
              version: str,
              target: str | None,
              extra: list[str],
              with_requirements: list[str],
              paths: list[str],
              sync: bool,
          ) -> list[str]:
              spec = _TYPECHECKERS[tool]
              command_name = str(spec["command"])
              argv = [str(part) for part in spec["argv"]]  # e.g. ["ty", "check"]; argv[0] == command_name
              version_flag = [str(spec["version_flag"]), target] if target else []
              tail = [*version_flag, *extra, *paths]
              if sync:
                  base = ["uv", "run", "--no-sync"]
                  base += ["--with", f"{command_name}=={version}" if version else command_name]
                  for requirement in with_requirements:
                      base += ["--with", requirement]
                  return [*base, *argv, *tail]
              base = ["uvx"]
              for requirement in with_requirements:
                  base += ["--with", requirement]
              # uvx runs the package's default entry point (argv[0] == command_name), so the
              # package spec carries the version and any subcommand follows as argv[1:].
              tool_spec = f"{command_name}@{version}" if version else command_name
              return [*base, tool_spec, *argv[1:], *tail]


          # --------------------------------------------------------------------------- #
          # result parsing                                                              #
          # --------------------------------------------------------------------------- #
          def _classify(returncode: int) -> tuple[str, bool]:
              if returncode == 0:
                  return "success", False
              if returncode == 1:
                  return "failure", False
              return "failure", True  # exit >= 2, a negative signal, or a launch failure: a crash


          def _parse_ruff_check(stdout: str, workspace: Path) -> tuple[int, list[dict[str, str]]]:
              try:
                  diagnostics = json.loads(stdout) if stdout.strip() else []
              except json.JSONDecodeError:
                  return 0, []
              if not isinstance(diagnostics, list):
                  return 0, []
              annotations: list[dict[str, str]] = []
              for item in diagnostics:
                  if not isinstance(item, dict):
                      continue
                  location = item.get("location") or {}
                  annotations.append(
                      {
                          "file": _relative(str(item.get("filename", "")), workspace),
                          "line": str(location.get("row", 1)),
                          "col": str(location.get("column", 1)),
                          "title": f"ruff {item.get('code', '')}".strip(),
                          "message": str(item.get("message", "")),
                      }
                  )
              return len(diagnostics), annotations


          def _parse_reformat_count(text: str) -> int:
              match = _REFORMAT_COUNT.search(text)
              if match:
                  return int(match.group(1))
              return sum(1 for line in text.splitlines() if line.startswith("--- "))


          def _format_annotations(
              diff: str, workspace: Path, working_directory: Path
          ) -> list[dict[str, str]]:
              annotations: list[dict[str, str]] = []
              for line in diff.splitlines():
                  if line.startswith("--- "):
                      candidate = (working_directory / line[4:].strip()).resolve()
                      annotations.append(
                          {
                              "file": _relative(str(candidate), workspace),
                              "line": "1",
                              "col": "1",
                              "title": "ruff format",
                              "message": "File is not formatted; run `ruff format`.",
                          }
                      )
              return annotations


          def _parse_typecheck(
              tool: str, stdout: str, stderr: str, workspace: Path
          ) -> tuple[int, list[dict[str, str]]]:
              text = stdout + "\n" + stderr
              pattern = _PYRIGHT_ERROR if tool == "pyright" else _MYPY_ERROR
              annotations: list[dict[str, str]] = []
              for match in pattern.finditer(text):
                  annotations.append(
                      {
                          "file": _relative(match.group("file").strip(), workspace),
                          "line": match.group("line"),
                          "col": match.groupdict().get("col") or "1",
                          "title": f"{tool} error",
                          "message": f"{tool} reported a type error.",
                      }
                  )
              errors = len(annotations)
              if tool == "ty" and errors == 0:
                  # ty's diagnostic layout differs; fall back to counting error headers.
                  errors = sum(1 for line in text.splitlines() if line.startswith("error"))
              return errors, annotations


          # --------------------------------------------------------------------------- #
          # reporting                                                                    #
          # --------------------------------------------------------------------------- #
          def _ruff_check_summary(outcome: str, violations: int) -> str:
              return f"### ruff check: {outcome}\n\n{violations} violation(s).\n"


          def _ruff_format_summary(outcome: str, unformatted: int, diff: str) -> str:
              lines = [f"### ruff format: {outcome}\n", f"{unformatted} unformatted file(s)."]
              if diff.strip():
                  capped = diff[:_SUMMARY_DIFF_CAP]
                  if len(diff) > _SUMMARY_DIFF_CAP:
                      capped += "\n... (diff truncated)"
                  lines.append(f"\n```diff\n{capped}\n```")
              return "\n".join(lines) + "\n"


          def _typecheck_summary(outcome: str, tool: str, runs: list[dict[str, object]]) -> str:
              rows = "\n".join(f"- {run['python-version']}: {run['errors']} error(s)" for run in runs)
              return f"### typecheck ({tool}): {outcome}\n\n{rows}\n"


          def _render_summary(overall: str, enforce: bool, sections: list[str]) -> None:
              mode = "enforcing" if enforce else "advisory (report-only)"
              header = f"## Python lint results\n\nOverall: **{overall}** ({mode})\n\n"
              _append_summary(header + "\n".join(sections))


          def _emit_annotations(annotations: list[dict[str, str]]) -> None:
              per_tool: dict[str, int] = {}
              for annotation in annotations:
                  title = annotation["title"]
                  per_tool[title] = per_tool.get(title, 0) + 1
                  if per_tool[title] > _ANNOTATION_CAP:
                      continue
                  file = _escape_property(annotation["file"])
                  title_value = _escape_property(annotation["title"])
                  message = _escape_data(annotation["message"])
                  print(
                      f"::error file={file},line={annotation['line']},"
                      f"col={annotation['col']},title={title_value}::{message}"
                  )


          def _write_report(report: tuple[str, str]) -> None:
              directory = _get("LINT_REPORT_DIRECTORY")
              if not directory:
                  return
              name, content = report
              base = (Path(os.environ["GITHUB_WORKSPACE"]) / directory).resolve()
              base.mkdir(parents=True, exist_ok=True)
              (base / name).write_text(content, encoding="utf-8")


          def _write_outputs(outputs: dict[str, str]) -> None:
              path = os.environ.get("GITHUB_OUTPUT")
              if not path:
                  return
              with open(path, "a", encoding="utf-8") as handle:
                  for key, value in outputs.items():
                      if "\n" in value:
                          raise SystemExit(f"output {key} must not contain a newline.")
                      handle.write(f"{key}={value}\n")


          def _append_summary(text: str) -> None:
              path = os.environ.get("GITHUB_STEP_SUMMARY")
              if not path:
                  return
              with open(path, "a", encoding="utf-8") as handle:
                  handle.write(text.rstrip() + "\n")


          # --------------------------------------------------------------------------- #
          # helpers                                                                      #
          # --------------------------------------------------------------------------- #
          def _run(command: list[str], cwd: Path) -> RunResult:
              try:
                  completed = subprocess.run(command, cwd=cwd, capture_output=True, text=True, check=False)
              except FileNotFoundError as error:
                  return RunResult(127, "", str(error))
              return RunResult(completed.returncode, completed.stdout, completed.stderr)


          def _any_failure(results: dict[str, dict[str, object]]) -> bool:
              return any(tool.get("outcome") == "failure" for tool in results.values())


          def _relative(path: str, workspace: Path) -> str:
              if not path:
                  return path
              try:
                  return os.path.relpath(Path(path).resolve(), workspace)
              except ValueError:
                  return path


          def _escape_property(value: str) -> str:
              return (
                  value.replace("%", "%25")
                  .replace("\r", "%0D")
                  .replace("\n", "%0A")
                  .replace(":", "%3A")
                  .replace(",", "%2C")
              )


          def _escape_data(value: str) -> str:
              return value.replace("%", "%25").replace("\r", "%0D").replace("\n", "%0A")


          def _shlex(env_name: str) -> list[str]:
              value = _get(env_name)
              return shlex.split(value) if value else []


          def _split_lines(value: str) -> list[str]:
              return [line.strip() for line in value.splitlines() if line.strip()]


          def _get(name: str) -> str:
              return os.environ.get(name, "").strip()


          def _truthy(value: str) -> bool:
              return value.strip().lower() in {"1", "true", "yes", "on"}


          if __name__ == "__main__":
              raise SystemExit(main())
          DEVFLOWS_SCRIPT_EOF
          echo "script-root=$RUNNER_TEMP/devflows" >> "$GITHUB_OUTPUT"
      - name: Validate inputs
        shell: bash
        env:
          DEVFLOWS_SCRIPT_ROOT: ${{ steps.devflows-runtime.outputs.script-root }}
          LINT_PATHS: ${{ inputs.lint-paths }}
          LINT_WORKING_DIRECTORY: ${{ inputs.lint-working-directory }}
          LINT_REPORT_DIRECTORY: ${{ inputs.lint-report-directory }}
          LINT_UV_CACHE_MODE: ${{ inputs.uv-cache-mode }}
          LINT_UV_SYNC_ENABLED: ${{ inputs.uv-sync-enabled }}
          LINT_UV_SYNC_ARGUMENTS: ${{ inputs.uv-sync-arguments }}
          LINT_RUFF_CHECK_ENABLED: ${{ inputs.ruff-check-enabled }}
          LINT_RUFF_CHECK_ARGUMENTS: ${{ inputs.ruff-check-arguments }}
          LINT_RUFF_FORMAT_ENABLED: ${{ inputs.ruff-format-enabled }}
          LINT_RUFF_FORMAT_ARGUMENTS: ${{ inputs.ruff-format-arguments }}
          LINT_RUFF_VERSION: ${{ inputs.ruff-version }}
          LINT_TYPECHECK_ENABLED: ${{ inputs.typecheck-enabled }}
          LINT_TYPECHECK_TOOL: ${{ inputs.typecheck-tool }}
          LINT_TYPECHECK_VERSION: ${{ inputs.typecheck-version }}
          LINT_TYPECHECK_ARGUMENTS: ${{ inputs.typecheck-arguments }}
          LINT_TYPECHECK_PYTHON_VERSIONS: ${{ inputs.typecheck-python-versions }}
          LINT_TYPECHECK_WITH: ${{ inputs.typecheck-with }}
        run: python "${DEVFLOWS_SCRIPT_ROOT}/python-lint/validate-inputs.py"
      - name: Set up uv
        uses: astral-sh/setup-uv@11f9893b081a58869d3b5fccaea48c9e9e46f990  # v8.3.2
        with:
          version: ${{ inputs.uv-version }}
          python-version: ${{ inputs.lint-python-version }}
          enable-cache: ${{ inputs.uv-cache-mode }}
          working-directory: ${{ inputs.lint-working-directory }}
      - name: Run lint
        id: lint
        shell: bash
        env:
          DEVFLOWS_SCRIPT_ROOT: ${{ steps.devflows-runtime.outputs.script-root }}
          LINT_PATHS: ${{ inputs.lint-paths }}
          LINT_WORKING_DIRECTORY: ${{ inputs.lint-working-directory }}
          LINT_ENFORCE: ${{ inputs.lint-enforce }}
          LINT_ANNOTATIONS_ENABLED: ${{ inputs.lint-annotations-enabled }}
          LINT_REPORT_DIRECTORY: ${{ inputs.lint-report-directory }}
          LINT_UV_SYNC_ENABLED: ${{ inputs.uv-sync-enabled }}
          LINT_UV_SYNC_ARGUMENTS: ${{ inputs.uv-sync-arguments }}
          LINT_RUFF_CHECK_ENABLED: ${{ inputs.ruff-check-enabled }}
          LINT_RUFF_CHECK_ARGUMENTS: ${{ inputs.ruff-check-arguments }}
          LINT_RUFF_FORMAT_ENABLED: ${{ inputs.ruff-format-enabled }}
          LINT_RUFF_FORMAT_ARGUMENTS: ${{ inputs.ruff-format-arguments }}
          LINT_RUFF_VERSION: ${{ inputs.ruff-version }}
          LINT_TYPECHECK_ENABLED: ${{ inputs.typecheck-enabled }}
          LINT_TYPECHECK_TOOL: ${{ inputs.typecheck-tool }}
          LINT_TYPECHECK_VERSION: ${{ inputs.typecheck-version }}
          LINT_TYPECHECK_ARGUMENTS: ${{ inputs.typecheck-arguments }}
          LINT_TYPECHECK_PYTHON_VERSIONS: ${{ inputs.typecheck-python-versions }}
          LINT_TYPECHECK_WITH: ${{ inputs.typecheck-with }}
        run: python "${DEVFLOWS_SCRIPT_ROOT}/python-lint/run-lint.py"
      - name: Upload lint report
        if: ${{ !cancelled() && inputs.lint-report-directory != '' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: ${{ inputs.lint-report-artifact-name }}
          path: ${{ inputs.lint-report-directory }}
          if-no-files-found: warn
      - name: Upload artifact
        if: inputs.artifact-upload-enabled && inputs.artifact-upload-path != ''
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: ${{ inputs.artifact-upload-name }}
          path: ${{ inputs.artifact-upload-path }}
          if-no-files-found: ${{ inputs.artifact-upload-if-no-files-found }}
          retention-days: ${{ inputs.artifact-upload-retention-days }}
          compression-level: ${{ inputs.artifact-upload-compression-level }}
          overwrite: ${{ inputs.artifact-upload-overwrite }}
          include-hidden-files: ${{ inputs.artifact-upload-include-hidden-files }}
          archive: ${{ inputs.artifact-upload-archive }}

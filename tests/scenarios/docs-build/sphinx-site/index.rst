DevFlows Docs Build Fixture
===========================

This is a minimal, dependency-free Sphinx project used by the DevFlows
``docs-build`` scenario tests. It only references internal content so the build
is network-deterministic.

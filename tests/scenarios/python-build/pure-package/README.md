# python-build-fixture

A tiny pure-Python package used as a build fixture in the DevFlows
`python-build` scenario tests. It has no runtime dependencies and builds an
`sdist` and a `py3-none-any` wheel with either `uv build` or `python -m build`.
